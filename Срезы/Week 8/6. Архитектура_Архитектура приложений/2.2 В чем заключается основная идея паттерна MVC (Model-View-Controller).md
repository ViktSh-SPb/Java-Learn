Паттерн **MVC (Model–View–Controller)** разделяет приложение на три независимых компонента — **Model**, **View**, **Controller**, — чтобы упростить поддержку, масштабирование и тестирование.
# ⭐ Основная идея MVC

👉 **Разделить данные, интерфейс и логику обработки запросов так, чтобы изменения в одном компоненте минимально влияли на остальные.**
# 🔹 Компоненты MVC
### **1. Model (Модель)**
Отвечает за:
- данные
- бизнес-логику
- операции с БД
Содержит: сущности, доменные модели, сервисы, репозитории.
👉 Модель _не знает_ о том, как данные отображаются.
### **2. View (Представление)**
Отвечает за отображение данных пользователю.
Это UI-слой:
- HTML/JSP
- шаблоны Thymeleaf
- JSON-ответы, формируемые контроллером
👉 View _не содержит бизнес-логики_.
### **3. Controller (Контроллер)**
Принимает запрос, вызывает нужную модель и выбирает View.
Функции контроллера:
- получает входные данные
- вызывает методы модели
- передаёт результат в View
👉 Контроллер _не содержит данных_ и _не отвечает за их хранение_.
# ⭐ Взаимодействие (в двух словах)
1. Пользователь делает запрос.
2. **Controller** принимает его и обращается к **Model**.
3. **Model** обрабатывает данные и возвращает результат.
4. **Controller** передает результат в **View**.
5. **View** отображает пользователю.
# ⭐ Зачем MVC?
✔ уменьшает связанность кода  
✔ улучшает читаемость и поддержку  
✔ отделяет бизнес-логику от UI  
✔ позволяет менять UI без изменения логики  
✔ упрощает тестирование