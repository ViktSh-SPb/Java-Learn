В экосистеме **SOAP** безопасность реализуется по строгим **спецификациям WS-*** (Web Services Specifications), которые определяют, _как именно_ обеспечивать конфиденциальность, целостность и аутентификацию при обмене XML-сообщениями.
## 🧱 1. Основные спецификации безопасности для SOAP

|Спецификация|Назначение|
|---|---|
|**WS-Security**|Базовый стандарт безопасности SOAP-сообщений. Определяет, как встраивать подписи, шифрование и токены в заголовки SOAP.|
|**WS-Policy**|Позволяет описать _политику безопасности_ веб-сервиса (какие механизмы требуются: подписи, токены, шифрование и т.п.).|
|**WS-Trust**|Определяет, как происходит выдача, проверка и обмен _токенами безопасности_ через Security Token Service (STS).|
|**WS-SecureConversation**|Позволяет устанавливать _долгосрочную безопасную сессию_ между клиентом и сервисом (чтобы не пересылать токены при каждом запросе).|
|**WS-Federation**|Используется для _единого входа (SSO)_ между разными организациями или доменами, объединяя несколько систем аутентификации.|
## 🔒 2. Что делает **WS-Security**
Это основа всей безопасности SOAP.
Она добавляет в **заголовок SOAP (Header)** специальные XML-элементы, например:
```xml
<soap:Header>
  <wsse:Security>
    <wsse:UsernameToken>
      <wsse:Username>alice</wsse:Username>
      <wsse:Password Type="...#PasswordDigest">...</wsse:Password>
    </wsse:UsernameToken>

    <ds:Signature>
      ...
    </ds:Signature>

    <xenc:EncryptedData>
      ...
    </xenc:EncryptedData>
  </wsse:Security>
</soap:Header>
```
**Что можно делать:**
- 🪪 Передавать токены (UsernameToken, X.509, Kerberos, SAML и др.)
- ✍️ Подписывать сообщение (XML Signature)
- 🔐 Шифровать части SOAP (XML Encryption)
- 🕓 Добавлять временные метки (`<wsu:Timestamp>`) для защиты от повторных атак
## ⚙️ 3. Как это реализуется на практике
В Java, .NET и других языках — через __WS-_ библиотеки_*:

|Среда|Библиотека / Технология|
|---|---|
|**Java (Jakarta EE / Spring)**|Apache CXF, Metro (GlassFish), Axis2, WSS4J|
|**.NET**|Windows Communication Foundation (WCF)|
|**C++ / Python**|gSOAP, Zeep, suds|

Пример (Apache CXF, Java):
```xml
<policies>
  <ws-security>
    <usernameToken>
      <passwordDigest>true</passwordDigest>
      <nonce>true</nonce>
      <created>true</created>
    </usernameToken>
    <signature>true</signature>
    <encryption>true</encryption>
  </ws-security>
</policies>
```
## 🧠 4. Ключевые уровни защиты

|Уровень|Что защищает|Реализация|
|---|---|---|
|**Transport-level**|Канал передачи|HTTPS (SSL/TLS)|
|**Message-level**|Само SOAP-сообщение|WS-Security (подписи, шифрование, токены)|
|**Policy-level**|Политики взаимодействия|WS-Policy|
## 🚨 5. За что SOAP не отвечает
SOAP + WS-Security **не обеспечивают**:
- управление пользователями (это делает STS или внешняя IAM-система);
- контроль доступа (Access Control Lists / RBAC — на уровне приложения);
- аудит и логирование (нужно реализовывать отдельно).