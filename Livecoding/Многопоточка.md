# Многопоточка
### Задача 1
#### Условие:
Рассмотрите программу:
```java
public class StopThreadTest {  
    private static boolean stopRequested;  
  
    public static void main(String[] args) throws InterruptedException{  
        Thread backgroundThread = new Thread(()->{  
            int i = 0;  
            while (!stopRequested){  
                i++;  
            }  
        });  
        backgroundThread.start();  
        TimeUnit.SECONDS.sleep(1);  
        stopRequested = true;  
    }  
}
```
1. Закончится ли выполнение программы?
2. Как происходит выполнение программы (компилятор, jvm, процессор)? Какие есть гарантии?
3. Когда гарантируется happens before? Start, Join? Как еще кроме volatile?
4. Какие способы сделать код атомарным?
5. Как happens before работает с synchronized?
6. Заблокируем ли мы сами себя?
#### Решение:
```java
```
### Задача 2
#### Условие:
Почему не происходит блокировка самого себя в `forEach` при вызове `addAll`?
```java
public static void main(String[] args) {  
    List<Integer> a = Collections.synchronizedList(new ArrayList<>());  
    a.addAll(...);  
}  
  
synchronized void addAll(...) {  
    ...forEach(this::add);  
}  
  
synchronized void add(...) {  
    ...forEach(this::add);  
}
```
