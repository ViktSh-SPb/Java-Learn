В Java экосистема для SOAP-сервисов хорошо развита — многие инструменты стандартизированы и поддерживаются уже много лет.  
Cписок **основных инструментов и библиотек**, которые применяются для **создания и работы с SOAP веб-сервисами в Java**:
## 🧱 1. **JAX-WS (Java API for XML Web Services)** — стандарт Java EE / Jakarta EE
🔹 Это **основной стандарт** для разработки SOAP-веб-сервисов в Java.  
Он входит в состав Java SE 6+ и Jakarta EE.
Позволяет:
- создавать SOAP-сервисы из Java-классов (`@WebService`, `@WebMethod`);
- генерировать Java-клиентов по WSDL (`wsimport`);
- публиковать сервисы без внешнего контейнера.
📘 Пример:
```java
import javax.jws.WebService;
import javax.jws.WebMethod;

@WebService
public class HelloService {
    @WebMethod
    public String sayHello(String name) {
        return "Привет, " + name + "!";
    }
}
```
Публикация:
```java
import javax.xml.ws.Endpoint;

public class Main {
    public static void main(String[] args) {
        Endpoint.publish("http://localhost:8080/hello", new HelloService());
    }
}
```
## ⚙️ 2. **Apache CXF**
🔹 Один из самых популярных **фреймворков для SOAP и REST**.  
Поддерживает JAX-WS и JAX-RS (для REST).
Возможности:
- генерация кода из WSDL (`wsdl2java`);
- поддержка WS-Security, WS-Policy, WS-ReliableMessaging;
- интеграция со Spring Boot.
📘 Пример публикации через Spring:
```java
@Bean
public Endpoint endpoint() {
    EndpointImpl endpoint = new EndpointImpl(bus, new HelloServiceImpl());
    endpoint.publish("/hello");
    return endpoint;
}
```
## ⚙️ 3. **Apache Axis2**
🔹 Более ранний, но всё ещё используемый SOAP-движок.  
Поддерживает как Java-классы → WSDL, так и WSDL → Java.
Плюсы:
- высокая совместимость с SOAP 1.1 и 1.2;
- есть готовый контейнер для развертывания сервисов.
Минусы:
- проект считается «устаревающим» по сравнению с CXF.
## ⚙️ 4. **Metro (JAX-WS Reference Implementation)**
🔹 Это официальная реализация JAX-WS от Oracle / Eclipse Foundation.  
Используется, например, в GlassFish и Payara.
Особенности:
- хорошая поддержка WS-Security (через WSIT);
- полная совместимость с JAX-WS API;
- легко интегрируется с Maven.
## 📦 5. **Spring Web Services (Spring-WS)**
🔹 Фреймворк от Spring Team для SOAP-сервисов, основанных на контракте (WSDL-first).  
В отличие от CXF, Spring-WS акцентирует внимание на **гибкости XML и конфигурации XSD**.
Преимущества:
- мощная интеграция со Spring Security;
- валидация входящих XML по XSD;
- поддержка SOAP-Headers и WS-Addressing.
📘 Пример контроллера:
```java
@Endpoint
public class HelloEndpoint {

    private static final String NAMESPACE_URI = "http://example.com/hello";

    @PayloadRoot(namespace = NAMESPACE_URI, localPart = "HelloRequest")
    @ResponsePayload
    public HelloResponse handleHello(@RequestPayload HelloRequest request) {
        HelloResponse response = new HelloResponse();
        response.setMessage("Привет, " + request.getName() + "!");
        return response;
    }
}
```
## 🧰 6. **Инструменты командной строки**

|Инструмент|Назначение|
|---|---|
|`wsimport`|Генерация Java-клиента по WSDL|
|`wsgen`|Генерация WSDL по Java-классу|
|`wsdl2java` (CXF, Axis2)|Генерация Java-кода из WSDL|
|`soapui`|Тестирование SOAP-сервисов (GUI)|
## 🧩 7. **Контейнеры приложений с поддержкой SOAP**
SOAP-сервисы можно разворачивать в:
- **GlassFish / Payara** — Metro встроен;
- **WildFly / JBoss** — поддержка JAX-WS и CXF;
- **Tomcat** — при добавлении CXF или Axis2 библиотек.
## 📊 Итого — популярные варианты

|Цель|Инструмент|
|---|---|
|Стандартная реализация SOAP|**JAX-WS / Metro**|
|Продвинутые возможности (WS-Security, REST + SOAP)|**Apache CXF**|
|Контракт-первый подход, Spring интеграция|**Spring-WS**|
|Старые проекты|**Apache Axis2**|
|Тестирование|**SoapUI**, **Postman (SOAP mode)**|
