Docker обеспечивает изоляцию ресурсов **на уровне ядра Linux**, используя два ключевых механизма:
# 🔹 1. **Namespaces — изоляция окружения**
Namespaces создают _отдельные виртуальные представления системных ресурсов_ для каждого контейнера.
Контейнер «видит» только то, что находится в его namespace.
Docker использует следующие типы namespaces:

|Namespace|За что отвечает|
|---|---|
|**PID**|изоляция процессов (контейнер видит только свои процессы)|
|**NET**|собственные сетевые интерфейсы, порты, маршруты|
|**IPC**|разделяемая память, очереди сообщений|
|**UTS**|hostname и доменное имя контейнера|
|**MNT**|файловые системы, точки монтирования внутри контейнера|
|**USER**|отображение UID/GID контейнера в UID/GID хоста|

📌 Пример:  
Если запустить `ps aux` внутри контейнера — вы увидите **только процессы контейнера**, потому что PID namespace скрывает остальные.
# 🔹 2. **cgroups — контроль и ограничение ресурсов**
cgroups (control groups) позволяют ограничивать контейнер:
- по CPU,
- по RAM,
- по дисковому вводу/выводу,
- по сетевой пропускной способности,
- по числу процессов.
Примеры использования:
### Ограничение CPU:
```bash
docker run --cpus="1.5" nginx
```
### Ограничение памяти:
```bash
docker run -m 512m redis
```
### Ограничение числа процессов:
```bash
docker run --pids-limit 100 myapp
```
cgroups гарантируют, что контейнер:
- **не сможет использовать больше заданных ресурсов**,
- не «задушит» другие контейнеры или хост.
# 🔹 3. **Copy-on-write файловая система**
Docker использует layered файловые системы (overlay2, AUFS, btrfs):
- каждый контейнер получает **собственный верхний слой**,
- базовые слои образа — **read-only**,
- изменения — только в контейнерном слое.
Это обеспечивает:
- файловую изоляцию,
- защиту базового образа,
- лёгкость снапшотов.
# 🔹 4. **Network isolation (виртуальные сети Docker)**
Docker создаёт собственные виртуальные сети:
- **bridge** (по умолчанию),
- **host**,
- **overlay** (в swarm),
- **macvlan** и др.
Каждый контейнер получает:
- собственный виртуальный интерфейс,
- собственный IP,
- изолированный namespace сети.
# 🔹 5. **Capabilities — ограничение прав**
Linux capabilities позволяют ограничивать суперправа root внутри контейнера.
Например, контейнер с root _не сможет_:
- менять ядро,
- изменять сетевые интерфейсы на хосте,
- управлять модулями ядра.
# 📌 Итог
Docker обеспечивает изоляцию через комбинацию:

|Механизм|Что делает|
|---|---|
|**Namespaces**|Изоляция окружения (процессы, сеть, файловая система)|
|**cgroups**|Ограничение ресурсов|
|**Copy-on-write FS**|Изоляция файловой системы|
|**Capabilities**|Ограничение прав root|
|**Virtual networks**|Изоляция сетевого трафика|
