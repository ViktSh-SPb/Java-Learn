## 1. **Установите Jenkins и необходимые плагины**
1. Установите Jenkins (обычно через Docker или пакетный менеджер).
2. Установите плагины:
    - **Git** (для клонирования репозитория)
    - **Pipeline** (для Jenkinsfile)
    - **JUnit** (для отображения тестов)
    - **Blue Ocean** (удобный интерфейс, не обязательно)
    - Плагины для вашего языка (например, Maven, Gradle или NodeJS)
## 2. **Настройте подключение к системе контроля версий**
Обычно используется GitHub / GitLab / Bitbucket.
Настройки:
1. **Manage Jenkins → System → Git** — добавьте SSH ключ или токен.
2. В репозитории добавьте Jenkinsfile (если выбираете Pipeline).
## 3. **Создайте новый проект**
Есть два варианта:
## 🅰️ Вариант 1. Freestyle job (старый метод)
### 1) Создайте задачу
**New Item → Freestyle project**
### 2) Настройте Git
Раздел **Source Code Management → Git**:
- URL репозитория
- credentials (токен или ключ)
### 3) Настройте сборку
Раздел **Build**:
- Для Maven:  
    `Invoke top-level Maven targets → mvn clean test`
- Для Gradle:  
    `gradle clean test`
- Для Node.js:  
    `npm install && npm test`
### 4) Настройте тесты
Раздел **Post-build actions**:
- Publish JUnit test result report  
    Например: `**/target/surefire-reports/*.xml`
Теперь Jenkins будет собирать и тестировать проект при каждом запуске.
## 🅱️ Вариант 2. Pipeline job с Jenkinsfile (рекомендуется)
### Процесс
1. **New Item → Pipeline**
2. Источник: SCM → Git → ваш репозиторий
3. Jenkins автоматически найдет `Jenkinsfile` в корне.
### Пример Jenkinsfile для Maven
```groovy
pipeline {
    agent any
    
    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/your/repo.git'
            }
        }
        stage('Build') {
            steps {
                sh 'mvn clean install'
            }
        }
        stage('Test') {
            steps {
                sh 'mvn test'
            }
            post {
                always {
                    junit 'target/surefire-reports/*.xml'
                }
            }
        }
    }
}
```
## 4. **Настройте триггеры запуска**
Jenkins может запускать сборку автоматически:
### 🔹 1) При пуше в GitHub
Включите Webhooks:
- GitHub → Settings → Webhooks
- URL: `http://your-jenkins-url/github-webhook/`
### 🔹 2) По расписанию
В разделе **Build Triggers**:
```
H/5 * * * *
```
(каждые 5 минут)
### 🔹 3) На каждый merge request
Через GitHub App или GitLab integration.
## 5. **Проверьте сборку**
Передайте commit → Jenkins должен автоматически:
1. Клонировать проект
2. Установить зависимости
3. Собрать проект
4. Запустить тесты
5. Показать результат
# 🎉 Готово!
Теперь у вас настроена автоматическая система CI, которая:
- Автоматически собирает проект
- Автоматически запускает тесты
- Показывает результаты в Jenkins UI
- Может запускать сборки по пушу / расписанию / PR