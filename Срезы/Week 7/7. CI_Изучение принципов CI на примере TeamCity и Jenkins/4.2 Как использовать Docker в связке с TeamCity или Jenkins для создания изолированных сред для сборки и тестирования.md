Использование **Docker вместе с TeamCity или Jenkins** — один из самых распространённых способов создать **изолированные, воспроизводимые и стандартизированные среды** для сборки и тестирования.
# 🔹 Зачем использовать Docker с CI-системами?
### ✔ Полная изоляция окружения
Каждая сборка выполняется в собственном контейнере — никакие зависимости не конфликтуют.
### ✔ Гарантированная воспроизводимость
Одна и та же сборка даёт одинаковый результат на любой машине CI.
### ✔ Простота подключения зависимостей
Можно легко менять JDK/Node/Python/библиотеки через `Dockerfile`, а не настраивать агента вручную.
### ✔ Возможность тестирования в многоконтейнерной среде
Через Docker Compose (например, поднять PostgreSQL, Redis, Kafka во время тестов).
# 🟦 Использование Docker в **Jenkins**
## 🔹 Способ 1: Выполнение сборки внутри Docker-контейнера
### Требования
На agent должна быть установлена Docker Engine.
### Jenkinsfile пример:
```groovy
pipeline {
    agent {
        docker {
            image 'maven:3.9.6-eclipse-temurin-21'
            args '-v $HOME/.m2:/root/.m2'
        }
    }

    stages {
        stage('Build') {
            steps {
                sh 'mvn clean package'
            }
        }
    }
}
```
📌 Jenkins автоматически:
- скачает образ
- запустит контейнер
- выполнит команды внутри него
## 🔹 Способ 2: Построение Docker-образов внутри Jenkins
### Добавление шага сборки:
```groovy
stage('Build Docker Image') {
    steps {
        sh 'docker build -t myapp:latest .'
    }
}
```
### Пуш в Docker Registry:
```groovy
stage('Push Image') {
    steps {
        withCredentials([string(credentialsId: 'docker-token', variable: 'TOKEN')]) {
            sh """
                echo $TOKEN | docker login -u myuser --password-stdin
                docker push myapp:latest
            """
        }
    }
}
```
# 🟦 Использование Docker в **TeamCity**
TeamCity работает с Docker через **Docker Support plugin** (обычно включён по умолчанию).
## 🔹 Способ 1: Сборка в Docker-контейнере
В Build Step выбираем:
**Runner type: Docker**  
и указываем образ, например:
```
maven:3.9.6-eclipse-temurin-21
```
Внутри шага команда:
```
mvn clean test
```
## 🔹 Способ 2: Docker Wrapper — выполнение любого шага внутри контейнера
Любой Build Step (Maven, Gradle, Command Line) можно обернуть:
✔ Add → Docker Wrapper
Настраивается образ:
```
maven:latest
```
TeamCity автоматически запускает контейнер → выполняет шаг → завершает контейнер.
## 🔹 Способ 3: Создание Docker-образов в TeamCity
Добавьте Build Step:
**Runner type: Docker**
Действие: **Build image**
Параметры:
- Dockerfile
- Context dir
- Имя образа (`myapp:build-1`)
# 🟦 Пример пайплайна TeamCity с Docker Compose
TeamCity build step (Command line):
```
docker compose up -d
./gradlew test
docker compose down
```
Используется для интеграционных тестов, например PostgreSQL + сервис.
# 🟩 Лучшие практики использования Docker в CI
### ✔ Один Dockerfile → для разработки и CI
Даёт одинаковое окружение.
### ✔ Кэширование зависимостей
Для Maven/Gradle/Node лучше монтировать кеш каталога:
Jenkins:
```groovy
args '-v $HOME/.m2:/root/.m2'
```
TeamCity:
```
Volume mappings:
~/.m2 => /root/.m2
```
### ✔ Минимизация размера образа
Используйте multi-stage build.
### ✔ Скейлинг агентов
Можно запускать CI-агентов как Docker-контейнеры.
# 🟧 Итог
Использование Docker с Jenkins или TeamCity позволяет:
- создавать изолированные среды для сборки и тестов
- стандартизировать окружение
- легко обновлять зависимости
- запускать интеграционные тесты с Docker Compose
- собирать и публиковать собственные Docker-образы