SOAP и REST — это два **подхода (архитектурных стиля)** для построения **веб-сервисов** — систем, которые позволяют приложениям обмениваться данными через сеть (обычно по HTTP).
## 🧩 1. SOAP (Simple Object Access Protocol)
**SOAP** — это **протокол**, определяющий строгие правила для обмена структурированными сообщениями в формате **XML**.
### 📘 Основные характеристики:
- 🔹 **Формат сообщений:** всегда XML.
- 🔹 **Строгий контракт:** сервис описывается с помощью **WSDL (Web Services Description Language)** — это формальное описание операций, типов данных и адресов.
- 🔹 **Протокол транспортировки:** обычно HTTP/HTTPS, но может использовать и другие (SMTP, JMS и т. д.).
- 🔹 **Расширяемость:** поддерживает __WS-_ стандарты_* — например:
    - WS-Security (шифрование, подпись);
    - WS-ReliableMessaging (гарантированная доставка);
    - WS-Transaction (транзакции между сервисами).
### ✅ Преимущества SOAP:
- Хорошо подходит для **корпоративных систем**, где нужны безопасность, транзакции и строгие контракты.
- Поддерживает **сложные типы данных** и **stateful операции**.
### ❌ Недостатки:
- Много “бумажной работы”: XML, WSDL, namespaces.
- Менее гибкий и “тяжелее” в настройке.
## 🌐 2. REST (Representational State Transfer)
**REST** — это **архитектурный стиль**, а не протокол. Он использует простые принципы HTTP и ориентируется на **ресурсы** (объекты данных), а не на операции.
### 📗 Основные характеристики:
- 🔹 **Простые HTTP-методы:**
    - `GET` — получить данные,
    - `POST` — создать,
    - `PUT` — обновить,
    - `DELETE` — удалить.
- 🔹 **Формат данных:** обычно **JSON**, но может быть XML, YAML, HTML и т. д.
- 🔹 **Без состояния (stateless):** каждый запрос независим и содержит всю нужную информацию (например, токен авторизации).
- 🔹 **URI** описывает ресурсы, например:
    ```
    GET /api/users/15
    POST /api/users
    ```
### ✅ Преимущества REST:
- Легче разрабатывать и тестировать.
- Быстрее, меньше накладных данных.
- Хорошо подходит для **веба, микросервисов и мобильных приложений**.
### ❌ Недостатки:
- Нет строгого контракта (можно нарушить структуру ответа).
- Нет стандартов для сложных функций (например, транзакций между сервисами).
## ⚖️ Сравнение кратко:

|Критерий|SOAP|REST|
|---|---|---|
|Тип|Протокол|Архитектурный стиль|
|Формат сообщений|XML|JSON, XML, др.|
|Контракт|WSDL (обязателен)|Обычно нет, но можно OpenAPI|
|Передача данных|Любой транспорт|Обычно HTTP|
|Стейт|Может быть stateful|Stateless|
|Безопасность|WS-Security|HTTPS + токены (OAuth, JWT)|
|Скорость и простота|Медленнее, сложнее|Быстрее, проще|
|Типичная область применения|Корпоративные системы, банки|Веб- и мобильные API|
