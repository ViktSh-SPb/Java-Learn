В распределённой среде **инвалидация кэша** — одна из самых сложных задач, потому что данные могут храниться одновременно на нескольких узлах, и нужно поддерживать их согласованность. Существует несколько основных подходов:
## 1️⃣ **Временная инвалидация (Time-to-Live, TTL)**
- Каждый объект в кэше хранится ограниченное время.
- После истечения TTL объект считается устаревшим и удаляется.
- **Плюсы:** простая реализация, подходит для данных, где небольшая задержка актуальности допустима.
- **Минусы:** возможны «грязные» чтения — данные устаревшие до момента удаления.
**Пример:** Redis с настройкой `EXPIRE` для ключа.
## 2️⃣ **Явная инвалидация (Explicit Invalidation)**
- Приложение или сервис явно сообщает кэшу, что данные устарели.
- Обычно через API кэша (`delete`, `invalidate`) или через специальный брокер сообщений.
- **Плюсы:** точное удаление устаревших данных.
- **Минусы:** увеличивает сложность, нужно отслеживать все изменения.
**Пример:**
- Микросервис обновляет БД → публикует событие «обновление объекта X» → все кэши подписанные на событие удаляют объект.
## 3️⃣ **Инвалидация по событию (Event-driven / Pub-Sub)**
- Распределённые кэши подписаны на события изменений данных.
- При изменении данных сервис публикует событие, кэши удаляют соответствующие ключи.
- **Плюсы:** минимальная задержка, масштабируемо.
- **Минусы:** необходимость надёжного брокера сообщений, возможна потеря событий.
**Пример:** Kafka, RabbitMQ, Redis Pub/Sub.
## 4️⃣ **Версионирование (Versioning / Cache Key Versioning)**
- Ключи кэша содержат версию данных.
- При обновлении данных версия увеличивается → старые ключи больше не используются.
- **Плюсы:** легко масштабируется, можно хранить старые версии для отката.
- **Минусы:** рост числа ключей, требует контроля версий.
**Пример:** `user:123:v2` вместо `user:123`.
## 5️⃣ **Write-through / Write-behind (Согласованное кэширование)**
- **Write-through:** запись сначала в кэш, затем в БД. Кэш всегда актуален.
- **Write-behind (asynchronous):** запись в кэш, затем асинхронно в БД.
- **Плюсы:** автоматическая актуализация.
- **Минусы:** повышенная нагрузка на кэш, риск потери данных при сбое (write-behind).
## 6️⃣ **Read-through + Lazy Invalidation**
- Кэш сам загружает данные при запросе (read-through).
- Инвалидация происходит «лениво», например по TTL или при записи.
- **Плюсы:** кэш всегда содержит актуальные данные при первом чтении.
- **Минусы:** может быть задержка в распространении изменений.
### 🔑 Вывод
В распределённой среде обычно комбинируют несколько подходов:
- TTL + Event-driven (для баланса простоты и актуальности)
- Версионирование + явная инвалидация (для критичных данных)
- Write-through (для строго консистентных систем)