## 1️⃣ Подготовка проекта
1. Добавьте зависимость JUnit в проект:
**Maven**
```xml
<dependency>
    <groupId>org.junit.jupiter</groupId>
    <artifactId>junit-jupiter</artifactId>
    <version>5.10.0</version>
    <scope>test</scope>
</dependency>
```
**Gradle**
```gradle
testImplementation 'org.junit.jupiter:junit-jupiter:5.10.0'
```
2. Создайте структуру каталогов для тестов:
```
src/main/java/...  # основной код
src/test/java/...  # тесты
```
## 2️⃣ Создание тестового класса
- Создайте отдельный класс для тестов каждого класса приложения:
```java
class CalculatorTest {
    // тестовые методы будут здесь
}
```
- Обычно тестовый класс **имя класса + Test** (`CalculatorTest` для `Calculator`).
## 3️⃣ Написание тестовых методов
- Метод помечается аннотацией `@Test`.
- Внутри метода:
    1. **Arrange** — подготовка данных.
    2. **Act** — вызов метода, который тестируется.
    3. **Assert** — проверка результата с помощью `Assertions`.
Пример:
```java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertEquals;

class CalculatorTest {

    @Test
    void testAdd() {
        Calculator calc = new Calculator();      // Arrange
        int result = calc.add(2, 3);            // Act
        assertEquals(5, result);                // Assert
    }
}
```
## 4️⃣ Настройка и очистка окружения
- Используются аннотации для жизненного цикла теста:

|Аннотация|Когда выполняется|Для чего|
|---|---|---|
|`@BeforeEach`|перед каждым тестом|Инициализация объектов, подготовка данных|
|`@AfterEach`|после каждого теста|Очистка ресурсов|
|`@BeforeAll`|один раз перед всеми тестами класса|Общая настройка (статические ресурсы)|
|`@AfterAll`|один раз после всех тестов класса|Освобождение общих ресурсов|
Пример:
```java
@BeforeEach
void setUp() {
    calculator = new Calculator();
}
```
## 5️⃣ Использование вспомогательных библиотек
- **Mockito** — для мокирования зависимостей и изоляции тестируемого класса.
- **AssertJ / Hamcrest** — для читаемых и мощных assertions.
## 6️⃣ Запуск тестов
- **IDE** (IntelliJ, Eclipse) позволяет запускать тесты через контекстное меню.
- **Maven / Gradle** выполняет автоматический запуск тестов:
```bash
mvn test
gradle test
```
- Test Runner выполняет:
    1. Подготовку окружения (`@BeforeAll`, `@BeforeEach`).
    2. Запуск тестового метода (`@Test`).
    3. Проверку результата (`Assertions`).
    4. Очистку окружения (`@AfterEach`, `@AfterAll`).
- Формирует **отчёт о результатах тестирования** (успех, провал, пропуск).
## 7️⃣ Отладка и исправление
- Если assertion не прошла → тест считается **неуспешным**.
- Разработчик исправляет код или тест до успешного прохождения.
## 🔹 Итоговый workflow
```
1. Подготовка проекта (JUnit зависимость)
2. Создание тестового класса (ClassNameTest)
3. Написание тестовых методов (@Test)
4. Настройка окружения (@BeforeEach / @AfterEach)
5. Использование Mockito / AssertJ (опционально)
6. Запуск тестов (IDE или Maven/Gradle)
7. Анализ результатов → исправление кода
```
- Основная идея: **каждый тест проверяет маленький кусочек функциональности изолированно**, что облегчает поддержку и отладку приложения.