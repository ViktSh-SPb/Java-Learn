HTTP/2 — это **обновлённая версия протокола HTTP**, которая сохраняет семантику HTTP/1.1 (методы, статус-коды, заголовки), но вводит ряд улучшений для **повышения производительности и уменьшения задержек**.
### 🔹 Основные нововведения HTTP/2
1. **Бинарный протокол**
    - HTTP/1.1 использовал текстовый формат (парсинг строк), HTTP/2 — бинарный.
    - Упрощает обработку, снижает вероятность ошибок и ускоряет парсинг.
2. **Мультиплексирование (Multiplexing)**
    - Несколько запросов и ответов могут передаваться **параллельно через одно TCP-соединение**.
    - В HTTP/1.1 приходилось открывать несколько соединений или ждать завершения одного запроса (head-of-line blocking).
3. **Сжатие заголовков (Header Compression)**
    - Заголовки HTTP (которые часто повторяются) сжимаются с помощью **HPACK**.
    - Снижает объём передаваемых данных и ускоряет передачу запросов.
4. **Приоритизация потоков (Stream Prioritization)**
    - Клиент может указать, какие запросы важнее, чтобы сервер отдавал их раньше.
5. **Server Push (Принудительная отправка ресурсов)**
    - Сервер может отправлять ресурсы клиенту **ещё до того, как клиент их запросит** (например, CSS или JS для страницы).
    - Уменьшает задержку при загрузке страниц.
6. **Более эффективное использование TCP**
    - Одно соединение может обслуживать множество запросов, уменьшая накладные расходы на открытие новых соединений.
### 🔹 Итог
HTTP/2 улучшает:
- **Скорость загрузки страниц** (меньше задержек, мультиплексирование)
- **Эффективность сети** (сжатие заголовков, меньше соединений)
- **Контроль приоритетов ресурсов** (приоритизация и Server Push)