В **SOAP (Simple Object Access Protocol)** обмен данными между клиентом и сервером осуществляется строго по формализованным правилам с использованием **XML-сообщений**, передаваемых по стандартным интернет-протоколам — чаще всего **HTTP** или **HTTPS**.
## 🔄 Механизм обмена данными в SOAP
### 1. 📦 Формат сообщения
SOAP-сообщение — это **XML-документ**, состоящий из стандартных частей:
```xml
<soap:Envelope xmlns:soap="http://www.w3.org/2003/05/soap-envelope">
  <soap:Header>
    <!-- Метаданные, например, авторизация, токены, маршрутизация -->
  </soap:Header>
  <soap:Body>
    <!-- Основное содержимое запроса или ответа -->
  </soap:Body>
</soap:Envelope>
```
- **Envelope** — контейнер всего сообщения.
- **Header** — необязательная секция с дополнительной служебной информацией.
- **Body** — содержит саму операцию и данные (запрос или ответ).
### 2. 🚀 Отправка запроса (Request)
Клиент формирует SOAP-запрос — XML-документ с операцией, например:
```xml
<soap:Envelope xmlns:soap="http://schemas.xmlsoap.org/soap/envelope/">
  <soap:Body>
    <GetUserDetails xmlns="http://example.com/users">
      <UserId>123</UserId>
    </GetUserDetails>
  </soap:Body>
</soap:Envelope>
```
Этот XML отправляется на сервер (обычно POST-запросом по HTTP):
```
POST /UserService HTTP/1.1
Content-Type: text/xml; charset=utf-8
SOAPAction: "http://example.com/users/GetUserDetails"
```
### 3. 🖥 Обработка на сервере
- Сервер (SOAP endpoint) принимает XML.
- Декодирует запрос в объект (например, через JAXB).
- Выполняет нужную бизнес-логику.
- Формирует SOAP-ответ (тоже XML).
### 4. 📬 Ответ (Response)
Сервер возвращает XML в формате SOAP:
```xml
<soap:Envelope xmlns:soap="http://schemas.xmlsoap.org/soap/envelope/">
  <soap:Body>
    <GetUserDetailsResponse xmlns="http://example.com/users">
      <User>
        <Id>123</Id>
        <Name>John Doe</Name>
      </User>
    </GetUserDetailsResponse>
  </soap:Body>
</soap:Envelope>
```
### 5. ⚙️ Транспорт
SOAP чаще всего работает поверх:
- **HTTP/HTTPS** — стандартный вариант.
- **SMTP, JMS** и др. — поддерживаются, но реже используются.
### 6. 💡 Особенности обмена
- Вся структура строго **определена WSDL** (Web Services Description Language).
- Сообщения можно **подписывать и шифровать** (через **WS-Security**).
- Возможна **асинхронная** или **синхронная** передача.
- Все данные **типизированы** и соответствуют **XML Schema (XSD)**.