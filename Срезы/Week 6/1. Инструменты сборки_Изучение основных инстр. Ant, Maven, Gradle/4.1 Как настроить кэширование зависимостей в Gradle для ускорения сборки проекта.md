## 🧩 1. Как работает кэширование зависимостей в Gradle
Gradle автоматически кэширует все зависимости, скачанные из репозиториев (Maven Central, JCenter, etc.) в локальный кэш:
```
~/.gradle/caches/modules-2/files-2.1/
```
Плюсы:
- Если зависимость уже скачана → Gradle не загружает её повторно.
- Если проект пересобирается → Gradle использует кэшированные `.jar` и `.pom` файлы.
## 🧩 2. Инкрементная сборка и кэш задач
Кроме зависимостей, Gradle кэширует **результаты задач** (compileJava, test, jar):
```bash
gradle build --build-cache
```
- `--build-cache` позволяет Gradle использовать **локальный и удалённый кэш**, чтобы задачи, которые не изменились, не выполнялись повторно.
- Это особенно полезно на CI/CD.
## 🧩 3. Настройка кэша зависимостей
В `build.gradle` (Groovy DSL):
```groovy
repositories {
    mavenCentral()
}

// Включаем проверку на обновления зависимостей каждые 24 часа
configurations.all {
    resolutionStrategy.cacheChangingModulesFor 0, 'seconds' // всегда проверять
    resolutionStrategy.cacheDynamicVersionsFor 24, 'hours'  // динамические версии (например 1.0.+)
}
```
- `cacheChangingModulesFor` – для SNAPSHOT-зависимостей, которые меняются часто.
- `cacheDynamicVersionsFor` – для зависимостей типа `1.0.+`, чтобы Gradle проверял новые версии раз в 24 часа.
## 🧩 4. Включение build cache в `settings.gradle`
```groovy
gradle.settingsEvaluated {
    buildCache {
        local {
            enabled = true   // локальный кэш
        }
        remote(HttpBuildCache) {
            url = 'https://gradle-cache-server.example.com/'
            push = true      // пушить результаты сборки на удалённый сервер
        }
    }
}
```
- **Локальный кэш** ускоряет сборку на одной машине.
- **Удалённый кэш** позволяет разделять результаты между разными разработчиками или CI.
## 🧩 5. Параллельная сборка
Для ускорения сборки можно включить параллельное выполнение задач:
```bash
gradle build --parallel
```
- Работает вместе с кэшированием → Gradle не будет выполнять задачи, которые уже есть в кэше.
## 🧩 6. Проверка использования кэша
```bash
gradle build --build-cache --info
```
- В логах можно увидеть, какие задачи использовали кэш.
- Задачи, взятые из кэша, помечены как `FROM-CACHE`.
## ✅ Итог
1. **Gradle кэширует зависимости автоматически** → просто указываем `mavenCentral()` или другие репозитории.
2. **Динамические и SNAPSHOT-зависимости** можно настраивать через `resolutionStrategy`.
3. **Build cache** (`--build-cache`) позволяет кэшировать результаты сборки, ускоряя повторные сборки и CI.
4. **Параллельная сборка** (`--parallel`) + кэш → максимальное ускорение.