Монолитная архитектура становится проблемой при масштабировании проекта, потому что **всё приложение — единый блок**, и его нельзя масштабировать выборочно. Это приводит к перегрузкам, задержкам в разработке и снижению производительности.
# 🟥 1. Масштабируется всё приложение целиком, а не его части
Если нагрузка растёт только на один модуль (например, отчетность или загрузка файлов), вы **вынуждены увеличивать ресурсы всего монолита**, даже тех частей, которым это не нужно.
Это приводит к:
- росту стоимости инфраструктуры,
- неэффективному использованию ресурсов.
# 🟥 2. Огромный код → медленные изменения
В больших монолитах тысячи классов и модулей.  
Со временем появляются:
- высокая связность,
- сложные зависимости,
- риски сломать что-то при изменениях.
Каждое изменение становится дорогим и опасным.
# 🟥 3. Длительные сборки и выкладки
Монолит = один артефакт → один pipeline.
Когда проект большой, сборка может занимать:
- минуты,
- иногда десятки минут.
А выкладка требует:
- остановки всего приложения,
- либо сложного "безостановочного" деплоя.
# 🟥 4. Ограничения для командной разработки
Большие команды сталкиваются с:
- конфликтами при мерджах,
- необходимостью согласовывать архитектуру,
- зависимостями между задачами.
Монолит плохо масштабирует **команды**.
# 🟥 5. Невозможно выбирать технологии под конкретные задачи
В монолите обычно один стек:
- одна версия Java,
- один фреймворк,
- одно решение для хранения данных.
Если один модуль нуждается в другой БД или языке — это невозможно без глобального переделывания.
# 🟥 6. Ограниченная отказоустойчивость
Если один модуль упал, он может потянуть за собой **всё приложение**:
- OutOfMemory,
- Deadlock,
- бесконечные циклы,
- утечки памяти.
Монолит — это единый процесс → единая точка отказа.
# 🟥 7. Трудности с глобальным масштабированием под высокие нагрузки
Когда проект растёт до уровня:
- миллионов пользователей,
- десятков тысяч RPS,
- географически распределённых серверов,
монолитная архитектура начинает «ломаться», потому что:
- требует большой и дорогой вертикальной масштабируемости (scale-up),
- плохо работает при нагрузках, требующих масштабирования по частям (scale-out).
# 🟦 Итог
### 👉 Главная проблема монолита при масштабировании:
**невозможность независимо увеличивать ресурсы для разных частей системы и сложности, возникающие из-за огромного единого кода и зависимостей.**