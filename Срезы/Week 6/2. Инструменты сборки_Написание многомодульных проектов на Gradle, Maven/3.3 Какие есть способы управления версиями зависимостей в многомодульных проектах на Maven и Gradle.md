# 🧩 **1. Maven — управление версиями зависимостей**
## ✅ Основные способы:
### **Способ 1. `dependencyManagement` в родительском POM**
Это самый распространённый и правильный способ.
Родительский POM (`my-app/pom.xml`):
```xml
<project>
    <groupId>com.example</groupId>
    <artifactId>my-app</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>

    <modules>
        <module>core</module>
        <module>web</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-starter</artifactId>
                <version>3.3.0</version>
            </dependency>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-starter-web</artifactId>
                <version>3.3.0</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
</project>
```
Теперь в дочерних POM можно **не указывать версии**:
```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-web</artifactId>
</dependency>
```
🔹 **Преимущества:**
- Централизованный контроль всех версий;
- Нет дублирования номеров версий в каждом модуле;
- Гарантируется согласованность зависимостей между модулями.
### **Способ 2. BOM (Bill of Materials)**
Если проект использует много библиотек одной экосистемы (например, Spring Boot, Jakarta EE), можно подключить **BOM**.
Пример:
```xml
<dependencyManagement>
    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-dependencies</artifactId>
            <version>3.3.0</version>
            <type>pom</type>
            <scope>import</scope>
        </dependency>
    </dependencies>
</dependencyManagement>
```
Теперь все артефакты из Spring Boot будут иметь согласованные версии — их можно указывать **без номера версии**:
```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-test</artifactId>
</dependency>
```
🔹 **Преимущества:**
- Лёгкое обновление экосистемы одной строкой (обновив BOM);
- Гарантия совместимости библиотек.
### **Способ 3. Использование переменных (properties)**
Можно вынести версии зависимостей в секцию `<properties>`:
```xml
<properties>
    <spring.version>3.3.0</spring.version>
    <junit.version>5.10.0</junit.version>
</properties>

<dependencies>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-web</artifactId>
        <version>${spring.version}</version>
    </dependency>
</dependencies>
```
🔹 **Преимущества:**
- Просто и наглядно;
- Можно переопределять версии через профили или `settings.xml`.
# ⚙️ **2. Gradle — управление версиями зависимостей**
Gradle даёт больше гибкости, поскольку использует **Groovy или Kotlin DSL**.
## ✅ Основные способы:
### **Способ 1. Централизованное объявление в корневом `build.gradle`**
В корневом `build.gradle` задаются версии как константы:
```groovy
ext {
    springBootVersion = '3.3.0'
    junitVersion = '5.10.0'
}

subprojects {
    repositories {
        mavenCentral()
    }

    dependencies {
        testImplementation "org.junit.jupiter:junit-jupiter:${junitVersion}"
    }
}
```
🔹 **Преимущества:**
- Все версии видны в одном месте;
- Общие зависимости можно объявить сразу для всех модулей.
### **Способ 2. `gradle.properties`**
Можно вынести версии в отдельный файл `gradle.properties`:
```properties
springBootVersion=3.3.0
junitVersion=5.10.0
```
А использовать в `build.gradle` так:
```groovy
dependencies {
    implementation "org.springframework.boot:spring-boot-starter-web:${springBootVersion}"
    testImplementation "org.junit.jupiter:junit-jupiter:${junitVersion}"
}
```
🔹 **Преимущества:**
- Версии можно переопределять локально (через пользовательский `~/.gradle/gradle.properties`);
- Удобно для CI/CD.
### **Способ 3. Version Catalogs (`libs.versions.toml`) — современный способ**
📁 Файл `gradle/libs.versions.toml`:
```toml
[versions]
spring-boot = "3.3.0"
junit = "5.10.0"

[libraries]
spring-boot-web = { module = "org.springframework.boot:spring-boot-starter-web", version.ref = "spring-boot" }
junit-jupiter = { module = "org.junit.jupiter:junit-jupiter", version.ref = "junit" }
```
📘 Использование в `build.gradle`:
```groovy
dependencies {
    implementation libs.spring.boot.web
    testImplementation libs.junit.jupiter
}
```
🔹 **Преимущества:**
- Современный и рекомендуемый подход (Gradle 7+);
- Упрощает обновления и автодополнение в IDE;
- Поддерживает версии, группы и алиасы.
### **Способ 4. Использование BOM (как в Maven)**
Gradle тоже поддерживает BOM:
```groovy
dependencies {
    implementation platform("org.springframework.boot:spring-boot-dependencies:3.3.0")
    implementation "org.springframework.boot:spring-boot-starter-web"
    testImplementation "org.springframework.boot:spring-boot-starter-test"
}
```
🔹 **Преимущества:**
- Точно такие же, как в Maven: согласованные версии в рамках одной экосистемы.
# 🧾 **Итого — сравнение подходов**

|Цель|Maven|Gradle|
|---|---|---|
|Централизованное управление|`<dependencyManagement>`|`libs.versions.toml` или `gradle.properties`|
|Импорт согласованных версий|BOM (`<scope>import</scope>`)|`platform()`|
|Объявление версий через переменные|`<properties>`|`ext {}` или `gradle.properties`|
|Автоматическая совместимость библиотек|BOM|BOM|
|Современный рекомендованный способ|BOM + dependencyManagement|Version Catalogs (`libs.versions.toml`)|
✅ **Вывод:**
- В **Maven** главный инструмент — `dependencyManagement` и BOM.
- В **Gradle** — `Version Catalogs` (`libs.versions.toml`) и `platform()`.
- Оба подхода дают **централизованное, согласованное и предсказуемое управление версиями**.
