Версионирование REST API — это важный аспект, который позволяет **вносить изменения в сервис без ломки существующих клиентов**. Существует несколько подходов, каждый со своими плюсами и минусами.
## 🔹 1. **Версионирование через URI**
### 📘 Смысл:
Версия API указывается прямо в пути ресурса.
**Пример:**
```
/api/v1/users
/api/v2/users
```
### ✅ Плюсы:
- Простое и очевидное для клиентов.
- Легко управлять разными версиями на сервере.
- Совместимо с кешированием HTTP.
### ❌ Минусы:
- Версия становится частью URI (не всегда “чистый” REST).
- Много версий → много дублирования кода/маршрутов.
## 🔹 2. **Версионирование через заголовки HTTP**
### 📘 Смысл:
Версия передаётся через специальный заголовок, например `Accept` или `X-API-Version`.
**Пример:**
```http
GET /users
Accept: application/vnd.myapi.v1+json
```
### ✅ Плюсы:
- URI остаётся “чистым”.
- Гибкость — можно менять версии без изменения маршрутов.
### ❌ Минусы:
- Меньше очевидности для пользователя.
- Не все инструменты/клиенты удобно поддерживают кастомные заголовки.
## 🔹 3. **Версионирование через параметры запроса**
### 📘 Смысл:
Версия передаётся как query-параметр.
**Пример:**
```
GET /users?version=1
GET /users?version=2
```
### ✅ Плюсы:
- Просто внедрить.
- Легко тестировать через браузер.
### ❌ Минусы:
- Не такой “чистый” REST.
- Некоторые кеши и прокси могут игнорировать query-параметры.
## 🔹 4. **Версионирование через медиа-типы (content negotiation)**
### 📘 Смысл:
Используются кастомные MIME-типы с указанием версии.
**Пример:**
```http
Accept: application/vnd.myapi.user-v1+json
```
### ✅ Плюсы:
- Чистый REST-подход.
- Позволяет поддерживать разные представления одного ресурса.
### ❌ Минусы:
- Более сложная реализация.
- Клиенты должны корректно обрабатывать custom MIME-типы.
## 🔹 5. **Общие рекомендации**
1. **Сохраняйте старые версии до их обоснованного устаревания**.
2. **Используйте семантическое версионирование** (v1, v2, v3…).
3. **Документируйте изменения между версиями** (Swagger/OpenAPI).
4. **Если возможно — делайте backward-compatible изменения** без создания новой версии:
    - добавление новых полей;
    - ненарушение старого контракта.