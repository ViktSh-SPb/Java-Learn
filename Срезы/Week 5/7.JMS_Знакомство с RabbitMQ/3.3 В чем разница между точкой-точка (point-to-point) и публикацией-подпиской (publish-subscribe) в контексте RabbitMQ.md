Разница между **Point-to-Point (P2P)** и **Publish/Subscribe (Pub/Sub)** в RabbitMQ связана с **тем, как сообщения доставляются потребителям** и сколько потребителей получают одно сообщение.
## 1️⃣ Point-to-Point (Очередь / Work Queue)
**Сценарий:** одно сообщение доставляется **только одному потребителю**.
- **Компоненты:**
    - Producer → Queue → Consumer
- **Принцип работы:**
    - Сообщения помещаются в **очередь**.
    - RabbitMQ распределяет сообщения **между подписанными consumer’ами**.
    - Каждый consumer получает **только часть сообщений** (load balancing).
**Особенности:**
- Используется для **распределения задач** между воркерами.
- Гарантируется, что **каждое сообщение обработается один раз** (при правильной настройке ack).
**Пример:** обработка заказов в магазине, где каждый заказ должен обработать один worker.
```
Producer → Queue "orders" → Consumer 1
                              → Consumer 2
```
- Сообщение 1 → Consumer 1
- Сообщение 2 → Consumer 2
## 2️⃣ Publish/Subscribe (Pub/Sub)
**Сценарий:** одно сообщение может быть доставлено **нескольким потребителям одновременно**.
- **Компоненты:**
    - Producer → Exchange → Множество Queues → Множество Consumers
- **Принцип работы:**
    - Producer отправляет сообщение в **exchange** (fanout или topic).
    - Exchange **маршрутизирует** сообщение в одну или несколько очередей.
    - Каждая очередь имеет своего consumer’a → **один и тот же message обрабатывается несколько раз**.
**Особенности:**
- Используется для **рассылки событий нескольким подписчикам**.
- Идеально для **уведомлений, логирования, событийной архитектуры**.
**Пример:** уведомление о новом заказе, которое получают:
1. Сервис email-уведомлений
2. Сервис аналитики
3. Сервис доставки
```
Producer → Exchange "orders_created" → Queue 1 → Consumer A
                                      → Queue 2 → Consumer B
                                      → Queue 3 → Consumer C
```
- Сообщение 1 → Consumer A, B и C одновременно
## 3️⃣ Краткое сравнение

|Особенность|Point-to-Point (Queue)|Publish/Subscribe (Exchange)|
|---|---|---|
|Сколько получателей|Одно сообщение → один consumer|Одно сообщение → несколько consumer’ов|
|Цель|Распределение работы, load balancing|Рассылка событий, уведомления|
|Очереди|Обычно одна очередь на задачу|Несколько очередей на одно событие|
|Повторная доставка|При падении consumer, message → другой consumer|Каждая очередь получает копию сообщения|
### 🔹 Итог
- **P2P** → распределение задач между потребителями, каждое сообщение обрабатывается **один раз**.
- **Pub/Sub** → рассылка сообщений нескольким подписчикам, каждое сообщение может быть обработано **несколькими consumer’ами**.