## ⚙️ 1. Структура проекта
Допустим, у нас проект состоит из двух модулей:
```
my-app/
├── settings.gradle
├── build.gradle
├── core/
│   └── build.gradle
└── web/
    └── build.gradle
```
- `core` — библиотека с общей логикой;
- `web` — основной модуль, который использует `core`.
## 🧩 2. Файл `settings.gradle`
Gradle должен знать, какие модули входят в проект.  
Для этого указываем их в `settings.gradle`:
```groovy
rootProject.name = 'my-app'

include 'core', 'web'
```
Теперь `core` и `web` зарегистрированы как **подпроекты** (subprojects).
## ⚙️ 3. Родительский `build.gradle`
Часто на верхнем уровне (`my-app/build.gradle`) задают общие настройки и репозитории:
```groovy
allprojects {
    repositories {
        mavenCentral()
    }
}

subprojects {
    apply plugin: 'java'

    group = 'com.example'
    version = '1.0.0'
}
```
## 🧱 4. Модуль `core`
Файл `core/build.gradle`:
```groovy
plugins {
    id 'java-library'
}

dependencies {
    // зависимости самого core, если нужны
}
```
> ⚠️ Плагин `java-library` предпочтительнее для библиотечных модулей,  
> потому что он правильно экспортирует только `api`-зависимости наружу.
## 🌐 5. Модуль `web` — зависимость от `core`
Теперь в `web/build.gradle` добавляем зависимость **на внутренний модуль** с помощью конструкции `project(':имя_модуля')`:
```groovy
plugins {
    id 'application'
}

dependencies {
    implementation project(':core')

    implementation 'org.springframework.boot:spring-boot-starter-web:3.3.0'
    testImplementation 'org.junit.jupiter:junit-jupiter:5.10.0'
}

application {
    mainClass = 'com.example.Main'
}
```
## 🔍 6. Как это работает
- Gradle понимает, что `:core` — это **другой модуль того же проекта**, и не ищет его в репозиториях.
- При сборке (`gradle build`) он **сначала соберёт `core`**, затем `web`.
## 🧠 7. Пример с несколькими зависимостями
Если есть три модуля: `core`, `service`, `web`:
```groovy
// settings.gradle
include 'core', 'service', 'web'
```
```groovy
// web/build.gradle
dependencies {
    implementation project(':service')
    implementation project(':core')
}
```
```groovy
// service/build.gradle
dependencies {
    implementation project(':core')
}
```
Gradle автоматически выстроит правильный порядок сборки:
```
core → service → web
```
## 🧾 8. Итого

|Задача|Файл|Пример|
|---|---|---|
|Определить модули|`settings.gradle`|`include 'core', 'web'`|
|Добавить зависимость|`web/build.gradle`|`implementation project(':core')`|
|Общие настройки|`build.gradle` в корне|`allprojects { repositories { ... } }`|
|Тип сборки библиотеки|`java-library`|экспорт API-зависимостей|
✅ **Вывод:**  
В Gradle зависимость между модулями задаётся просто:
```groovy
implementation project(':имя_модуля')
```
и это автоматически гарантирует правильный порядок сборки и связь между ними.