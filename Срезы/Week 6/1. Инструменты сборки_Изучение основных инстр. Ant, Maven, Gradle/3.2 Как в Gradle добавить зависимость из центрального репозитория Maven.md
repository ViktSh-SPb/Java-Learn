## 🧩 Основной принцип
Чтобы добавить зависимость из **центрального репозитория Maven (Maven Central)**, в Gradle нужно:
1. Указать репозиторий (`repositories { ... }`)
2. Добавить зависимость (`dependencies { ... }`)
## ✅ Пример для Groovy DSL (`build.gradle`)
```groovy
plugins {
    id 'java'
}

repositories {
    mavenCentral()   // ➊ Используем центральный репозиторий Maven
}

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web:3.3.2' // ➋ Зависимость
    testImplementation 'org.junit.jupiter:junit-jupiter:5.10.2'              // ➌ Зависимость для тестов
}
```
## ✅ Пример для Kotlin DSL (`build.gradle.kts`)
```kotlin
plugins {
    java
}

repositories {
    mavenCentral() // ➊
}

dependencies {
    implementation("org.springframework.boot:spring-boot-starter-web:3.3.2") // ➋
    testImplementation("org.junit.jupiter:junit-jupiter:5.10.2")             // ➌
}
```
## 🔍 Что тут происходит

|Элемент|Описание|
|---|---|
|`mavenCentral()`|подключает публичный центральный репозиторий Maven ([https://repo.maven.apache.org/maven2](https://repo.maven.apache.org/maven2))|
|`implementation`|основная зависимость, будет использоваться в продакшене|
|`testImplementation`|зависимость только для тестов|
|`runtimeOnly`|используется только во время выполнения|
|`compileOnly`|нужна только на этапе компиляции, не попадает в JAR|
## 🧱 Пример проекта полностью
```groovy
plugins {
    id 'java'
}

group = 'com.example'
version = '1.0.0'

repositories {
    mavenCentral()
}

dependencies {
    implementation 'com.google.guava:guava:33.0.0-jre'
    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.10.2'
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine:5.10.2'
}

test {
    useJUnitPlatform()
}
```
## 💡 Совет
Если тебе нужно подключить **дополнительные репозитории**, можно добавить, например:
```groovy
repositories {
    mavenCentral()
    maven { url 'https://jitpack.io' }
    mavenLocal() // подключает локальный ~/.m2/repository
}
```
## 📦 Проверка зависимостей
Команда для просмотра всех зависимостей:
```bash
gradle dependencies
```