## 1️⃣ Автоматическое управление контейнерами
- Kubernetes сам запускает, перезапускает и распределяет контейнеры.
- Если контейнер падает — K8s автоматически восстанавливает его.
## 2️⃣ Масштабирование
- Горизонтальное масштабирование контейнеров (`replicas`) по нагрузке.
- Автоматическое масштабирование на основе ресурсов (CPU/Memory) с помощью **Horizontal Pod Autoscaler**.
## 3️⃣ Высокая доступность (HA)
- Поддержка отказоустойчивости: если узел или контейнер падает, workload переносится на другой узел.
- Контролирует состояние приложений и кластера.
## 4️⃣ Автоматические обновления и откаты
- Rolling Updates: обновление контейнеров без простоя сервиса.
- Возможность **откатить** версию приложения, если новая работает некорректно.
## 5️⃣ Управление конфигурациями и секретами
- **ConfigMap** для конфигурационных данных.
- **Secrets** для паролей, токенов, ключей.
- Настройки можно менять без пересборки контейнера.
## 6️⃣ Эффективное использование ресурсов
- Контейнеры распределяются по узлам оптимально.
- Возможность задавать лимиты ресурсов (CPU, память) для контейнеров.
## 7️⃣ Универсальность и портативность
- Один и тот же кластер можно запускать локально, в облаке или гибридно.
- Поддержка любых контейнеров и микросервисов.
## 8️⃣ Распределённая сеть и балансировка нагрузки
- **Service** автоматически распределяет трафик между подами.
- Поддержка DNS внутри кластера для упрощения связи между сервисами.
## 9️⃣ Самовосстановление
- Если контейнер не отвечает или упал — K8s автоматически перезапускает или переносит его.
- Поддержка ReplicaSet и StatefulSet для контроля числа реплик.
## 🔟 Интеграция с CI/CD
- Идеально подходит для DevOps-процессов: автоматический деплой новых версий приложений.