**Spring Cloud** — это общий набор инструментов и библиотек для построения распределённых систем и микросервисной архитектуры на базе Spring.
**Spring Cloud Netflix** — это **подмножество** Spring Cloud, которое предоставляет интеграцию именно с _решениями от Netflix OSS_.
# 🧩 **Spring Cloud vs Spring Cloud Netflix**
## ✅ **Spring Cloud — “зонтичный” проект**
Spring Cloud объединяет множество технологий для микросервисов:
- сервис-дискавери (Eureka, Consul, Zookeeper)
- централизованная конфигурация (Spring Cloud Config)
- распределённые шины событий (Spring Cloud Bus)
- балансировка нагрузки (Spring Cloud LoadBalancer)
- API Gateway (Spring Cloud Gateway)
- распределённый трейсинг (Sleuth + Zipkin, Micrometer Tracing)
- очереди событий (Stream + Kafka/Rabbit)
- circuit breakers (Resilience4j)
- и др.
Spring Cloud — не завязан на одного вендора, он агностичен.
## 🎬 **Spring Cloud Netflix — адаптация под Netflix OSS**
Этот проект даёт интеграцию конкретно с библиотеками Netflix:
- **Eureka** — сервис-дискавери
- **Ribbon** — клиентский балансировщик
- **Hystrix** — circuit breaker
- **Zuul** — API Gateway
- **Archaius** — конфигурация
Но важно:  
📌 _Многие проекты Netflix OSS больше не поддерживаются Netflix и официально перешли в maintenance mode в Spring Cloud._
### Что сейчас deprecated?
- Ribbon ❌
- Hystrix ❌
- Zuul 1 ❌
- Archaius ❌
### Чем их заменили в современном Spring Cloud?

|Netflix OSS|Статус|Замена|
|---|---|---|
|Ribbon|deprecated|**Spring Cloud LoadBalancer**|
|Hystrix|deprecated|**Resilience4j**|
|Zuul 1|deprecated|**Spring Cloud Gateway**|
|Archaius|deprecated|**Spring Cloud Config**|
|Eureka|поддерживается|Eureka остаётся рабочим|
# 🆚 Ключевое отличие

|Характеристика|Spring Cloud|Spring Cloud Netflix|
|---|---|---|
|Назначение|Общая экосистема для микросервисов|Адаптация Netflix OSS инструментов|
|Поддержка|Активно развивается|Частично закрыта/в maintenance|
|Гибкость|Может использовать разные реализации (Eureka, Consul, Gateway…)|Жёстко привязан к Eureka/Hystrix/Ribbon/Zuul|
|Современные рекомендации|Использовать|Использовать ограниченно (только Eureka актуальна)|
# 📝 Вывод
**Spring Cloud Netflix — лишь один набор модулей внутри большого Spring Cloud**, сфокусированный на Netflix OSS.  
В современной архитектуре его используют редко, в основном _только Eureka_ остаётся актуальной.