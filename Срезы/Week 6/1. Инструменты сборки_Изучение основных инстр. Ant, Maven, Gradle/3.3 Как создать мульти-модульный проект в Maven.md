Мульти-модульные проекты в Maven очень популярны для крупных приложений, где есть несколько независимых, но связанных модулей.
## 🧩 Основная идея
Мульти-модульный проект состоит из:
1. **Parent POM** – корневой `pom.xml`, который управляет общими зависимостями, версиями, плагинами.
2. **Модули** – подпроекты, каждый со своим `pom.xml`.
Maven позволяет:
- строить все модули одной командой (`mvn install` на уровне parent)
- управлять версиями и зависимостями централизованно
## ⚙️ Структура проекта
```
my-project/           <-- parent
├── pom.xml           <-- parent POM
├── module-a/         <-- первый модуль
│   └── pom.xml
├── module-b/         <-- второй модуль
│   └── pom.xml
└── module-c/
    └── pom.xml
```
## 🔹 1. Parent POM
```xml
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.example</groupId>
    <artifactId>my-project</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>  <!-- очень важно для parent -->

    <modules>
        <module>module-a</module>
        <module>module-b</module>
        <module>module-c</module>
    </modules>

    <properties>
        <java.version>17</java.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Общие зависимости всех модулей -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-starter-web</artifactId>
                <version>3.3.2</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.11.0</version>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
```
- `<packaging>pom</packaging>` – ключевое, чтобы Maven понимал, что это **parent**, а не обычный JAR.
- `<modules>` – список подпроектов.
## 🔹 2. Модульный POM
Пример для `module-a/pom.xml`:
```xml
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.example</groupId>
        <artifactId>my-project</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>module-a</artifactId>
    <packaging>jar</packaging>

    <dependencies>
        <!-- Модули могут использовать зависимости parent -->
    </dependencies>
</project>
```
- `<parent>` связывает модуль с parent POM.
- Модули могут наследовать версии и плагины из parent.
## 🔹 3. Сборка
В корне проекта:
```bash
mvn clean install
```
- Maven построит все модули в порядке, указанном в `<modules>`.
- Модули могут ссылаться друг на друга через `<dependency>`:
```xml
<dependency>
    <groupId>com.example</groupId>
    <artifactId>module-a</artifactId>
    <version>1.0.0</version>
</dependency>
```
## 🔹 4. Рекомендации
1. **Использовать dependencyManagement в parent** — чтобы все версии зависимостей были согласованы.
2. **Стараться минимизировать количество cross-module зависимостей** — чтобы сборка была быстрой и предсказуемой.
3. **Разделять модули логически**: например, `core`, `service`, `web`, `api`.
4. **Сохранять структуру `src/main/java` и `src/test/java`** во всех модулях — Maven ожидает эту стандартную структуру.