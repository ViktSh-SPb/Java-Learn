# 🧩 Основные компоненты Spring Boot
## 1. **Spring Boot Starters**
Это наборы зависимостей (ready-to-use пакеты), которые включают всё необходимое для конкретной задачи.
Например:
- `spring-boot-starter-web` → MVC, встроенный сервер, JSON (Jackson)
- `spring-boot-starter-data-jpa` → работа с БД через JPA/Hibernate
- `spring-boot-starter-security` → аутентификация и авторизация
▶ **Зачем нужны:**  
Чтобы не подбирать десятки зависимостей вручную.
## 2. **Автоконфигурация (Spring Boot AutoConfiguration)**
Spring Boot анализирует:
- классы на classpath,
- настройки в application.yml/properties,
- аннотации в коде
…и автоматически создает стандартные бины: DataSource, EntityManagerFactory, Jackson ObjectMapper, MVC-конфиг и т. д.
▶ **Зачем нужна:**  
Чтобы не писать огромные XML или JavaConfig.
## 3. **Spring Application (SpringApplication.run)**
Это “запускатор” приложения:
- создаёт Spring Context (контейнер зависимостей),
- активирует автоконфигурацию,
- сканирует компоненты,
- запускает встроенный сервер (если есть Web Starter).
▶ **Зачем нужен:**  
Чтобы стартовать приложение одной командой.
## 4. **Встроенный сервер (Embedded Server)**
Если вы используете Web Starter, Boot поднимает:
- Tomcat (по умолчанию),
- либо Jetty, Undertow — если добавлены зависимости.
▶ **Зачем нужен:**  
Чтобы веб-приложение запускалось как обычный jar.
## 5. **Actuator**
Модуль для мониторинга приложения:
- `/actuator/health`
- `/actuator/info`
- `/actuator/metrics`
- `/actuator/loggers`
▶ **Зачем нужен:**  
DevOps / мониторинг / диагностика.
## 6. **Файлы конфигурации**
- `application.properties`
- `application.yml`
В них задаётся:
- порт сервера,
- параметры БД,
- профили,
- переменные окружения.
▶ **Зачем нужны:**  
Чтобы отделять конфигурацию от кода.
## 7. **Spring Beans & IoC Container**
Все ваши сервисы, репозитории, контроллеры — это **бины**, которыми управляет Spring.
▶ **Зачем нужны:**  
Поддержка DI (Dependency Injection).
# 🔗 Как эти компоненты взаимодействуют
Ниже — цепочка запуска приложения **в реальной жизни**:
### 1️⃣ Вы запускаете класс с `@SpringBootApplication`
SpringApplication.run → создаёт IoC-контейнер.
### 2️⃣ Boot включает **автоконфигурацию**
Находит класс `spring.factories` → подключает десятки AutoConfig модулей.
### 3️⃣ Boot сканирует классы
- контроллеры → в MVC
- сервисы → в контейнер
- репозитории → создаёт proxy для JPA
### 4️⃣ Создаются бины
- DataSource
- ObjectMapper
- WebMvcConfigurer
- RestTemplate/WebClient
- SecurityFilterChain (если есть security)
### 5️⃣ Если приложение веб-типа → запускается встроенный сервер
Tomcat/Jetty/Undertow создаёт HTTP-коннекторы.
### 6️⃣ Подключаются настройки из application.yml
Автоконфигурация использует их для:
- БД
- кешей
- security
- очередей (Rabbit, Kafka)
- mail server
- и т.д.
### 7️⃣ Приложение полностью готово
Контроллеры принимают запросы → сервисы → репозитории → БД → обратно.
# 🖼 Если очень коротко
Spring Boot состоит из:

|Компонент|Для чего|
|---|---|
|**Starters**|Подключают нужные библиотеки|
|**AutoConfiguration**|Настраивает приложение автоматически|
|**SpringApplication**|Запускает приложение|
|**Embedded Server**|Поднимает HTTP-сервер|
|**Actuator**|Мониторинг|
|**Config files**|Настройки|
|**IoC контейнер**|Управляет всеми компонентами|
Все они работают вместе, чтобы разработчик **писал только бизнес-логику**, а инфраструктурную часть Spring Boot делал сам.