Аннотации **`@Cacheable`**, **`@CachePut`** и **`@CacheEvict`** выполняют _разные роли_ в механизме Spring Cache.  
# 🔄 **1. @Cacheable — получить из кэша или положить в кэш**
### ❗ Метод _может не выполниться_, если данные уже есть в кэше.
**Логика работы:**
1. Проверяет кэш по ключу.
2. Если значение найдено → возвращает его и НЕ вызывает метод.
3. Если нет → вызывает метод и кладёт результат в кэш.
### Пример:
```java
@Cacheable(value = "products", key = "#id")
public Product getProduct(Long id) {
    System.out.println("Метод вызван!");
    return repo.findById(id).orElseThrow();
}
```
При повторных вызовах метод не будет выполняться.
# ♻️ **2. @CachePut — всегда вызывает метод, но обновляет кэш**
### ❗ Метод ВСЕГДА выполняется, кэш обновляется **после выполнения**.
Используется, когда:
- нужно синхронизировать данные в кэше после обновления,
- но нельзя пропустить выполнение метода (например, запись в БД).
### Пример:
```java
@CachePut(value = "products", key = "#product.id")
public Product update(Product product) {
    return repo.save(product); // метод всегда выполняется
}
```
Метод вызывается каждый раз и результат ИДЁТ в кэш.
# 🗑️ **3. @CacheEvict — удалить данные из кэша**
### Либо:
- удалить запись по ключу,
- либо очистить весь кэш.
Используется при:
- удалении данных,
- изменении состояния,
- сбросе целого кэша после массовых изменений.
### Пример — удалить запись:
```java
@CacheEvict(value = "products", key = "#id")
public void deleteProduct(Long id) {
    repo.deleteById(id); 
}
```
### Пример — очистить ВСЕ:
```java
@CacheEvict(value = "products", allEntries = true)
public void clearCache() { }
```
# 🧠 Краткое сравнение

|Аннотация|Метод вызывается?|Что делает с кэшем?|Тип использования|
|---|---|---|---|
|**@Cacheable**|❌ может НЕ вызываться|кладёт в кэш, если нет|читать данные|
|**@CachePut**|✅ всегда|обновляет кэш|обновлять данные|
|**@CacheEvict**|🟡 зависит от условий|удаляет из кэша|удалять / очищать|
# 🧩 Когда использовать какую?
## ✔ `@Cacheable`
Когда метод **чтения** и повторные вызовы дают одинаковый результат.
Например:
- получение тарифов
- получение карточного продукта
- получение профиля клиента
## ✔ `@CachePut`
Когда вы **обновляете данные**, и они должны обновиться в кэше.
Например:
- изменение карточного тарифа
- изменение имени пользователя
- обновление баланса кэшированных лимитов
## ✔ `@CacheEvict`
Когда **удаляете данные** или они **устарели**.
Например:
- удаление объекта
- пересчёт тарифов
- смена статуса продукта
- очистка кэша по расписанию