В HTTP **идемпотентность** (от лат. _idem potere_ — “давать тот же результат”) означает, что **повторное выполнение одного и того же запроса** не изменяет состояние сервера — результат остаётся тем же, что и при первом вызове.
## 🔹 Идемпотентные методы HTTP

| Метод       | Идемпотентен? | Объяснение                                                                           |
| ----------- | ------------- | ------------------------------------------------------------------------------------ |
| **GET**     | ✅ Да          | Только запрашивает данные, не изменяет их. Повторные запросы не влияют на состояние. |
| **HEAD**    | ✅ Да          | Как `GET`, но возвращает только заголовки. Не изменяет ресурсы.                      |
| **PUT**     | ✅ Да          | Каждый запрос устанавливает ресурс в **одно и то же состояние**, даже при повторе.   |
| **DELETE**  | ✅ Да          | Повторное удаление ресурса не изменяет результат — ресурс просто не существует.      |
| **OPTIONS** | ✅ Да          | Только сообщает, какие методы поддерживаются. Не изменяет ресурс.                    |
| **TRACE**   | ✅ Да          | Возвращает запрос обратно для диагностики. Не влияет на сервер.                      |
## 🔸 Неидемпотентные методы

|Метод|Идемпотентен?|Почему нет|
|---|---|---|
|**POST**|❌ Нет|Каждый запрос обычно **создаёт новый ресурс** или **изменяет состояние** — повторение даёт разный результат.|
|**PATCH**|⚠️ Не всегда|Может быть идемпотентен, если частичное обновление задаёт конкретные значения, но обычно нет (зависит от реализации).|
## 🔹 Пример различий
### `PUT` (идемпотентный):
```http
PUT /users/1
{
  "name": "Alice"
}
```
— при любом количестве повторов пользователь останется **Alice**.
### `POST` (неидемпотентный):
```http
POST /users
{
  "name": "Alice"
}
```
— при каждом повторе создаст **нового пользователя**.
## 🧠 Запомнить просто:
> **GET, PUT, DELETE, HEAD, OPTIONS, TRACE — идемпотентные.**  
> **POST и (чаще всего) PATCH — неидемпотентные.**