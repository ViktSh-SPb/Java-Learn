# 🔹 Основные проблемы, которые решает 2PC
## 1️⃣ **Атомарность операций на нескольких узлах**
- Проблема: при распределённых изменениях данных каждая система может завершить операцию независимо, что ведёт к **несогласованному состоянию**.
- Решение 2PC: гарантирует, что **все операции либо фиксируются, либо откатываются** одновременно.
- Пример: списание денег с одного счёта в одном банке и зачисление на счёт в другом банке.
## 2️⃣ **Согласованность данных**
- Проблема: без координации узлы могут оказаться с разными состояниями данных после выполнения транзакции.
- Решение 2PC: **координатор транзакции** собирает подтверждения от всех участников и только после этого инициирует commit.
- Все узлы переходят в **консистентное состояние** одновременно.
## 3️⃣ **Управление отказами участников**
- Проблема: один из узлов может упасть или не успеть выполнить транзакцию.
- Решение 2PC:
    1. **Фаза подготовки (prepare)** — узлы сообщают о готовности commit.
    2. **Фаза фиксации (commit/abort)** — координатор решает, выполнять commit или откат.
- Таким образом, даже при сбое узлов данные не оказываются в **частично выполненном состоянии**.
## 4️⃣ **Синхронизация между разными системами**
- Проблема: разные СУБД или сервисы могут иметь свои локальные механизмы транзакций, но глобальной координации нет.
- Решение 2PC: создаёт **единую точку согласования** для всех участников.
# 🔹 Ограничения 2PC
Хотя 2PC решает основные проблемы распределённых транзакций, он имеет недостатки:

|Ограничение|Причина|
|---|---|
|Потеря доступности при сбое координатора|Система ждёт решения всех участников|
|Высокие задержки|Две фазы и синхронизация сетевых узлов|
|Риск блокировок|Участники блокируют ресурсы до завершения commit|
# 🔹 Итог
**Two-Phase Commit решает:**
1. **Атомарность** распределённых операций
2. **Согласованность данных** на всех узлах
3. **Контроль над отказами участников**
4. **Синхронизацию транзакций между разными системами**
💡 Однако для высоконагруженных микросервисных систем чаще применяют **Saga Pattern**, чтобы повысить доступность и масштабируемость, жертвуя строгой ACID-согласованностью.