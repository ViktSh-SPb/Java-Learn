**HTTP (HyperText Transfer Protocol)** — это протокол прикладного уровня для передачи данных в сети Интернет. Он определяет **правила обмена сообщениями между клиентом (например, браузером) и сервером**.
### 🔹 Основные характеристики HTTP
1. **Клиент-серверная модель**
    - Клиент делает запрос (request) к серверу.
    - Сервер обрабатывает запрос и возвращает ответ (response).
2. **Без состояния (stateless)**
    - Каждый запрос рассматривается отдельно.
    - Сервер не хранит информацию о предыдущих запросах (для сохранения состояния используют cookies, сессии или токены).
3. **Текстовый протокол**
    - Сообщения (запросы и ответы) передаются в виде читаемого текста.
4. **Используемые методы**
    - `GET` — получить ресурс.
    - `POST` — создать ресурс.
    - `PUT` — обновить ресурс полностью.
    - `PATCH` — обновить ресурс частично.
    - `DELETE` — удалить ресурс.
    - `OPTIONS`, `HEAD` и другие — служебные методы.
5. **Коды состояния (HTTP status codes)**
    - `200 OK` — успешный запрос.
    - `201 Created` — ресурс создан.
    - `204 No Content` — успешно, но тело ответа пустое.
    - `400 Bad Request` — ошибка клиента.
    - `404 Not Found` — ресурс не найден.
    - `500 Internal Server Error` — ошибка сервера.
6. **Использование URI (Uniform Resource Identifier)**
    - Определяет, к какому ресурсу обращается клиент.
    - Пример: `https://example.com/users/1`.
### 🔹 Резюме
HTTP — это стандарт для обмена данными между клиентом и сервером в интернете, который обеспечивает **универсальный и простой способ запроса и передачи информации**, включая веб-страницы, API, файлы и другие ресурсы.