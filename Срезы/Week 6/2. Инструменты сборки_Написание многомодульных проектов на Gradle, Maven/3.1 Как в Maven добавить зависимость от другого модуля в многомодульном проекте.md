## ⚙️ 1. Структура многомодульного проекта
Допустим, у нас есть проект с тремя модулями:
```
my-app/
├── pom.xml                ← родительский (parent)
├── core/
│   └── pom.xml
└── web/
    └── pom.xml
```
- `core` содержит общую бизнес-логику, DTO и утилиты.
- `web` — это веб-приложение, которое использует `core`.
## 🧩 2. Родительский `pom.xml`
Файл `my-app/pom.xml` служит **агрегатором** и **родителем** для всех модулей.
```xml
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.example</groupId>
    <artifactId>my-app</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>

    <modules>
        <module>core</module>
        <module>web</module>
    </modules>
</project>
```
## 🧠 3. POM модуля `core`
Файл `core/pom.xml` описывает библиотеку, которую другие модули смогут использовать как зависимость.
```xml
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.example</groupId>
        <artifactId>my-app</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>core</artifactId>
    <packaging>jar</packaging>

    <dependencies>
        <!-- при необходимости можно указать зависимости -->
    </dependencies>
</project>
```
## 🔗 4. Добавляем зависимость от `core` в модуль `web`
Теперь в `web/pom.xml` мы можем сослаться на `core` **так же, как на внешнюю библиотеку**, только с тем же `groupId` и `artifactId`.
```xml
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.example</groupId>
        <artifactId>my-app</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>web</artifactId>
    <packaging>jar</packaging>

    <dependencies>
        <dependency>
            <groupId>com.example</groupId>
            <artifactId>core</artifactId>
            <version>1.0.0</version>
        </dependency>
    </dependencies>
</project>
```
## 🧩 5. Как это работает
- Когда вы выполняете:
    ```bash
    mvn clean install
    ```
    Maven **сначала соберёт `core`**, затем **`web`**, потому что `web` от него зависит.
- Maven автоматически понимает, что `core` — это **внутренний модуль того же проекта**, и **не ищет его в репозиториях** (`.m2/repository`).
## ✅ 6. Альтернатива (Dependency Management)
Если у вас несколько зависимостей между модулями и вы хотите **управлять версиями централизованно**, можно вынести их в `<dependencyManagement>` родительского POM:
```xml
<dependencyManagement>
    <dependencies>
        <dependency>
            <groupId>com.example</groupId>
            <artifactId>core</artifactId>
            <version>1.0.0</version>
        </dependency>
    </dependencies>
</dependencyManagement>
```
Тогда в дочернем модуле (`web`) версию можно **не указывать**:
```xml
<dependency>
    <groupId>com.example</groupId>
    <artifactId>core</artifactId>
</dependency>
```
## 🧾 Итого

|Шаг|Действие|Файл|
|---|---|---|
|1|Создать корневой `pom.xml` с `<modules>`|`my-app/pom.xml`|
|2|Настроить дочерний `core`|`core/pom.xml`|
|3|Настроить дочерний `web` и добавить зависимость|`web/pom.xml`|
|4|Выполнить `mvn install` или `mvn package`|CLI|
