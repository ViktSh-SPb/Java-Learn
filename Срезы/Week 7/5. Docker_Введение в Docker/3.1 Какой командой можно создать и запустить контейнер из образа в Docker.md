Для создания **и** запуска контейнера из существующего Docker-образа используется команда:
# ✅ **docker run**
Это универсальная команда, которая **создаёт новый контейнер** и **сразу запускает его**.
## 📌 Базовый пример

```bash
docker run image_name
```
## 📌 Наиболее распространённый вариант
```bash
docker run -d --name my-container image_name
```
Где:
- `-d` — запускает в фоновом режиме (detached mode)
- `--name my-container` — задаёт имя контейнера
- `image_name` — имя образа (например, `nginx`, `redis`, `myapp:1.0`)
## 📌 Пример с пробросом порта
```bash
docker run -d -p 8080:80 nginx
```
## 📌 Пример с томом
```bash
docker run -d -v mydata:/data redis
```
## 📌 Если образа нет
`docker run` автоматически **скачает образ** (`docker pull`) из Docker Hub и затем запустит контейнер.