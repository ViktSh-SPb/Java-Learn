В Spring Cloud балансировка нагрузки реализована **клиентской (client-side)** моделью: каждый сервис-клиент сам выбирает, к какому экземпляру другого сервиса отправить запрос.  
Это отличается от классической схемы “кластер + внешний балансировщик (Nginx, HAProxy)”.
# ⚙️ **1. Архитектура клиентской балансировки в Spring Cloud**
## 🔍 **Шаг 1: сервис-дискавери (обычно Eureka)**
Сервис-клиент запрашивает у Eureka список доступных экземпляров сервиса:
```
payment-service → [10.10.0.5, 10.10.0.6, 10.10.0.7]
```
## 🔀 **Шаг 2: LoadBalancer выбирает один из экземпляров**
Далее Spring Cloud применяет стратегию балансировки:
- round-robin (по умолчанию)
- random
- weighted (через кастомную стратегию)
- любая кастомная логика через Bean
## 📤 **Шаг 3: RestTemplate/WebClient делает запрос уже на выбранный инстанс**
Так работает механизм:
```
paymentServiceClient.getPayments()
   ↓ Eureka
   ↓ LoadBalancer (round-robin)
   ↳ отправляет запрос на конкретный instance
```
# 🧰 **2. Технологии, которые используются сегодня**
В **современных версиях Spring Cloud** применяется:
## ⭐ **Spring Cloud LoadBalancer**
Это замена старому Netflix Ribbon.
Преимущества:
- нативная интеграция с WebClient и RestTemplate
- поддерживает Reactor Netty
- кастомизация через beans
- не требует Netflix OSS
### Пример конфигурации:
```java
@Bean
@LoadBalanced
public RestTemplate restTemplate() {
    return new RestTemplate();
}
```
# 🏛️ **3. Как это выглядело раньше (Ribbon – deprecated)**
Раньше балансировка была через **Ribbon**.  
Он:
- получал список сервисов из Eureka
- хранил локальный кэш инстансов
- делал клиентскую балансировку
Но Ribbon больше не поддерживается, его заменили:
- **Spring Cloud LoadBalancer**
- **Resilience4j** вместо Hystrix
- **Spring Cloud Gateway** вместо Zuul
# 🧩 **4. Как работает балансировка для разных типов клиентов**
## 🔵 **RestTemplate**
Аннотация `@LoadBalanced` подключает LoadBalancerInterceptor:
```java
@LoadBalanced
@Bean
public RestTemplate restTemplate() {
    return new RestTemplate();
}
```
## 🟣 **WebClient (Reactor)**
Работает через фильтр:
```java
@Bean
@LoadBalanced
public WebClient.Builder webClientBuilder() {
    return WebClient.builder();
}
```
## 💛 **Feign (OpenFeign)**
Feign автоматически использует Spring Cloud LoadBalancer, если тот есть в classpath.
# 🧮 **5. Какие алгоритмы балансировки поддерживаются?**
### По умолчанию:
- **Round Robin** – циклический выбор
### Через кастомизацию:
- Random
- Weighted Round Robin
- Латентность-ориентированная балансировка
- Минимальное число активных соединений
- Любая кастомная стратегия (через Bean):
```java
@Bean
ReactorLoadBalancer<ServiceInstance> myLoadBalancer(Environment env) {
    return new CustomLoadBalancer(...);
}
```
# 🧱 **6. Server-side балансировка**
Spring Cloud поддерживает и эту модель:
### Через:
- Spring Cloud Gateway (может балансировать сервер-сайд)
- Kubernetes Service (kube-proxy/Envoy)
- Istio/Service Mesh
Но это уже **не Spring Cloud LoadBalancer**, а балансировка на уровне инфраструктуры.
# 📝 Итог
В **современном Spring Cloud** балансировка нагрузки реализована через:
### ✔ клиентскую балансировку (Spring Cloud LoadBalancer)
### ✔ интеграцию с Discovery (Eureka/Consul)
### ✔ поддержку RestTemplate, WebClient, Feign
### ✔ круглое распределение (Round Robin) или кастомные стратегии
### ✔ лёгкую заменяемость и расширяемость
Spring Cloud Netflix Ribbon **устарел** и больше не используется.