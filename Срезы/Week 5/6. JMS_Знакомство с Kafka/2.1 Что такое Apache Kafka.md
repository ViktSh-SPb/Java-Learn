**Apache Kafka** — это **распределённая платформа потоковой передачи данных (streaming platform)** с открытым исходным кодом, разработанная для **обработки, хранения и передачи больших объёмов сообщений в реальном времени**.
Её часто описывают как **«распределённый commit log»** — потому что Kafka хранит события в виде неизменяемого журнала записей, к которому можно многократно обращаться.
## 🧩 Основные концепции

| Термин                   | Описание                                                                                         |
| ------------------------ | ------------------------------------------------------------------------------------------------ |
| **Producer (продюсер)**  | Приложение, которое **отправляет** сообщения (события) в Kafka.                                  |
| **Consumer (консумер)**  | Приложение, которое **читает** сообщения из Kafka.                                               |
| **Topic (топик)**        | Категория или поток, в который продюсеры отправляют сообщения и из которого консумеры их читают. |
| **Partition (партиция)** | Раздел топика — Kafka делит данные на партиции для масштабирования и параллелизма.               |
| **Broker**               | Сервер Kafka, который хранит данные и обслуживает запросы клиентов.                              |
| **Cluster (кластер)**    | Набор брокеров, работающих как единая система.                                                   |
| **Offset (смещение)**    | Уникальный идентификатор позиции сообщения внутри партиции.                                      |
## ⚙️ Как работает Kafka
1. **Продюсер** публикует сообщение в **топик**.
2. Kafka сохраняет сообщение в одну из **партиций** топика (определяется по ключу).
3. **Сообщения хранятся на диске** и могут быть реплицированы на другие брокеры для отказоустойчивости.
4. **Консумеры** читают сообщения, отслеживая свою позицию по **offset**.
5. Kafka не удаляет сообщения сразу — они хранятся заданное время (например, 7 дней), что позволяет повторно их читать.
## ⚡ Основные особенности
- **Высокая производительность**: миллионы сообщений в секунду.
- **Горизонтальное масштабирование**: добавление брокеров и партиций без остановки системы.
- **Отказоустойчивость**: данные реплицируются между брокерами.
- **Хранение и повторное чтение сообщений**: Kafka — это не просто «очередь», а **журнал событий**, который можно перечитывать.
- **Потоковая обработка**: интеграция с **Kafka Streams** и **ksqlDB** позволяет обрабатывать данные в реальном времени.
## 🧠 Пример применения
Kafka часто используется для:
- **Интеграции микросервисов** (event-driven архитектура),
- **Аналитики в реальном времени** (логирование, мониторинг, телеметрия),
- **ETL-процессов** (перенос данных между системами),
- **Event sourcing и CQRS-паттернов**,
- **Обработки транзакций** и данных IoT.
## 💡 Пример
**Приложение логирования:**
- Web-серверы → отправляют логи в Kafka (продюсеры),
- Kafka → сохраняет события в топик `server-logs`,
- Система аналитики (консумер) → читает эти логи в реальном времени и строит дашборды.