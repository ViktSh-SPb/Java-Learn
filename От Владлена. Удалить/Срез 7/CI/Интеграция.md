### Что такое непрерывная интеграция?

Непрерывная интеграция (Continuous Integration, CI) — это практика разработки программного обеспечения, при которой изменения в код регулярно и автоматически встраиваются в общую кодовую базу, после чего запускаются тесты и проверки для выявления ошибок на ранних этапах.

### Какие преимущества предоставляет использование систем непрерывной интеграции, таких как TeamCity или Jenkins?

1. Автоматизация сборки и тестирования  
 - Устранение рутины – CI-системы автоматически запускают сборку и тесты при каждом коммите, экономя время разработчиков.  
 - Раннее обнаружение багов – ошибки выявляются сразу, а не на этапе релиза.  
 - Стабильность кодовой базы – код всегда проходит проверку перед слиянием.  
 2. Быстрая обратная связь  
 - Разработчики мгновенно получают уведомления о проваленных тестах или ошибках сборки.  
 - Можно быстро исправить проблему, пока она свежа в памяти. 
4. Прозрачность и контроль  
 - Журналирование изменений – история сборок, тестов и деплоев всегда доступна.  
 - Метрики качества – отчеты о покрытии кода (code coverage), статическом анализе (SonarQube) и производительности.  

| Критерий          | TeamCity (JetBrains)                     | Jenkins                          |
|:------------------|:----------------------------------------|:---------------------------------|
| Простота настройки | Готовые шаблоны, удобный UI             | Гибкость, но требует ручной настройки |
| Поддержка         | Коммерческая (есть бесплатная версия)   | Бесплатный, но требует self-hosting |
| Производительность | Оптимизирован для больших проектов      | Зависит от конфигурации          |
| Интеграции        | Хорошая поддержка .NET, Kotlin          | Огромное количество плагинов     |
| Масштабируемость  | Встроенная поддержка распределённых сборок | Требует дополнительных плагинов |
| Цена             | Платная (бесплатно до 100 конфигураций) | Полностью бесплатный (Open Source) |
| UI/UX            | Современный и удобный интерфейс         | Устаревший UI, но настраиваемый  |
| Подходит для      | Корпоративных и коммерческих проектов   | Open Source, DevOps-команд       |
### Каковы основные компоненты системы непрерывной интеграции?

 1. Система контроля версий (VCS)  
 Примеры: Git (GitHub, GitLab, Bitbucket), SVN, Mercurial.  
 Роль:  
 - Хранит исходный код и историю изменений.  
 - Обеспечивает механизм ветвления и слияния (например, Git Flow, GitHub Flow).  
2. CI-сервер (Orchestrator)  
 Примеры: Jenkins, TeamCity, GitHub Actions, GitLab CI/CD, CircleCI.  
 Роль:  
 - Запускает автоматические сборки при каждом коммите или по расписанию.  
 - Координирует выполнение пайплайнов (например, сборка → тесты → деплой).  
 3. Система сборки (Build Tools)  
 Примеры:  
 - Для Java: Maven, Gradle.  
 - Для .NET: MSBuild, dotnet CLI.  
 - Для JavaScript: npm, yarn, Webpack.  
 Роль:  
 - Компиляция кода.  
 - Управление зависимостями (библиотеками, пакетами).  
4. Автоматизированные тесты  
 Типы тестов:  
 - Юнит-тесты (JUnit, pytest, NUnit) – проверка отдельных модулей.  
 - Интеграционные тесты – тестирование взаимодействия компонентов.  
 - E2E-тесты (Selenium, Cypress) – проверка работы всего приложения.  
 Роль:  
 - Обнаружение ошибок до попадания кода в основную ветку.  
5. Артефакты и репозитории  
 Примеры:  
 - Хранение билдов: Nexus, Artifactory, Docker Hub.  
 - Контейнеры: Docker, Podman.  
 Роль:  
 - Сохранение собранных версий ПО (JAR, DLL, Docker-образы).  
 - Возможность отката к предыдущим версиям.  
 6. Система мониторинга и уведомлений  
 Примеры: Slack, Email, Telegram-боты, Grafana.  
 Роль:  
 - Оповещение команды о проваленных сборках или тестах.  
 - Визуализация метрик (время сборки, покрытие кода).  
7. Инфраструктура и окружения  
 Примеры:  
 - Виртуальные машины (VM), контейнеры (Docker, Kubernetes).  
 - Облачные платформы (AWS, Azure, GCP).  
 Роль:  
 - Запуск тестов в изолированных средах (например, staging).  
 - Подготовка к Continuous Delivery (CD).  
 8. Дополнительные инструменты  
 - Статический анализ кода: SonarQube, ESLint, Checkstyle.  
 - Проверка безопасности: OWASP ZAP, Snyk.  
 - Управление конфигурацией: Ansible, Terraform.  
 
 Как это работает вместе?  
 1. Разработчик делает `git push` в репозиторий.  
 2. CI-сервер (например, Jenkins) детектирует изменение и запускает пайплайн.  
 3. Сборка: код компилируется (например, через Maven).  
 4. Тесты: запускаются автоматические тесты.  
 5. Если всё успешно – артефакт сохраняется в Nexus/Docker Hub.  
 6. Уведомление отправляется в Slack.  
 7. При необходимости – развертывание в тестовое окружение.  

### Как настроить автоматическое сборку и тестирование проекта на Jenkins?

1. Подготовка   Требования:  
 - Установленный Jenkins
 - Доступ к репозиторию (GitHub, GitLab, Bitbucket).  
 - JDK (для Java) 
 - Maven (если Java) / pytest (если Python).
2. Создание нового задания (Job) в Jenkins  
3. Настройка подключения к репозиторию  
 4. В разделе ""Управление исходным кодом"" выберите Git.  
 5. Укажите URL репозитория (например, `https://github.com/ваш-юзер/ваш-репозиторий.git`).  
 6. Добавьте учетные данные (Credentials), если репозиторий приватный. 
7. Настройка триггеров сборки  
 Выберите, когда Jenkins будет запускать сборку:  
 - GitHub Webhook (при пуше в репозиторий).  
 - Poll SCM (проверка изменений по расписанию, например, `* * * * *` — каждую минуту).  
 - Ручной запуск.  
5. Настройка шагов сборки (Build Steps)  
 Для Java/Maven проекта:  
 6. Добавьте шаг ""Invoke top-level Maven targets"".  
7. Настройка тестирования и отчетов  
 Публикация результатов тестов (JUnit, pytest)  
 8. Добавьте Post-build Action → ""Publish JUnit test result report"".  
 9. Укажите путь к отчетам:  
    - Для Maven: `/target/surefire-reports/*.xml`  
    - Для pytest: `/test-results/*.xml` (нужен `pytest --junitxml=test-results/results.xml`)  
Отправка уведомлений  
 - Slack/Email: Установите плагины (например, ""Slack Notification"") и настройте оповещения.  
7. Запуск и проверка  
 8. Нажмите ""Собрать сейчас"" (Build Now).  
 9. Проверьте Console Output на ошибки.  
 10. Убедитесь, что тесты проходят и артефакты создаются.   

Вместо Freestyle можно использовать Jenkinsfile (более гибко):  
 1. Создайте в корне репозитория файл `Jenkinsfile`:  
    ```groovy
    pipeline {
        agent any
        stages {
            stage('Build') {
                steps {
                    sh 'mvn clean install'  // или 'pytest'
                }
            }
            stage('Test') {
                steps {
                    junit '/target/surefire-reports/*.xml'
                }
            }
        }
    }
    ```
 2. В Jenkins выберите Pipeline и укажите путь к `Jenkinsfile`.  

### В чем различие между TeamCity и Jenkins с точки зрения управления конфигурацией?

Оба инструмента поддерживают CI/CD, но подходы к настройке и управлению конфигурацией у них принципиально разные.
1. Хранение конфигурации  
 Jenkins  
 - Файлы конфигурации:  
   - Основные настройки хранятся в `config.xml` (на сервере Jenkins).  
   - Jenkinsfile (для Pipeline) — декларативный или скриптовый Groovy-код, хранится в репозитории.  
 - Плюсы:  
   - Полная гибкость (можно писать сложные скрипты).  
   - Конфигурация как код (IaC) — легко версионировать.  
 - Минусы:  
   - Нет встроенного GUI для редактирования сложных пайплайнов.  
   - Ошибки в скриптах могут ломать сборки.  

 TeamCity  
 - Файлы конфигурации:  
   - Основные настройки хранятся в XML-файлах на сервере (но не редактируются вручную).  
   - Kotlin DSL (альтернатива Jenkinsfile) — конфигурация в коде, но с GUI-поддержкой.  
 - Плюсы:  
   - Удобный веб-интерфейс для настройки без кода.  
   - Встроенный Versioned Settings (синхронизация с Git).  
 - Минусы:  
   - Меньшая гибкость для нестандартных сценариев.  

 2. Подход к настройке пайплайнов  
 Jenkins  
 - Declarative Pipeline (простой YAML-подобный синтаксис) или Scripted Pipeline (полноценный Groovy).  
 - Пример Declarative Pipeline:  
   ```groovy
   pipeline {
       agent any
       stages {
           stage('Build') {
               steps {
                   sh 'mvn clean package'
               }
           }
       }
   }
 ```  
 - Нужно писать код даже для простых задач.  

 TeamCity  
 - GUI + Kotlin DSL (можно настраивать через интерфейс, а потом экспортировать в код).  
 - Build Steps добавляются через визуальный редактор:  
   - Нет необходимости писать код для базовых сценариев.  
   - Kotlin DSL генерируется автоматически.  
 - Пример Kotlin DSL в TeamCity:  
   ```kotlin
   version = ""2023.05""
   project {
       buildType {
           id(""MyBuild"")
           name(""My Build"")
           steps {
               maven {
                   goals = ""clean package""
               }
           }
       }
   }
```


 3. Ведение истории изменений  
 Jenkins  
 - Нет встроенного механизма для отслеживания изменений конфигурации.  
 - Рекомендуется хранить `Jenkinsfile` в Git и использовать Jenkins Configuration as Code (JCasC).  

 TeamCity  
 - Versioned Settings — автоматически коммитит изменения конфигурации в Git.  
 - Полная аудит-логи всех изменений (кто и когда менял настройки).  

4. Масштабируемость и шаблоны  
 Jenkins  
 - Shared Libraries — общие скрипты для нескольких проектов.  
 - Job DSL — генерация джобов кодом (но сложно поддерживать).  

 TeamCity  
 - Meta-Runners — переиспользуемые шаги сборки.  
 - Project Templates — готовые шаблоны конфигураций.  

5. Безопасность и роли  
 Jenkins  
 - Гибкая, но сложная система ролей (плагин Role-Based Strategy).  
 - Нужно настраивать вручную.  

 TeamCity  
 - Встроенные роли и пермишены (более удобно).  
 - Интеграция с LDAP/Active Directory.  

Итог: что выбрать?  

| Критерий         | Jenkins                                   | TeamCity                              |
|:-----------------|:-----------------------------------------|:--------------------------------------|
| Конфигурация     | Код (Groovy/Jenkinsfile)                 | GUI + Kotlin DSL                      |
| Гибкость        | Максимальная (можно писать скрипты)      | Ограничена интерфейсом                |
| Удобство        | Сложнее для новичков                     | Проще (меньше кода)                   |
| Версионирование | Требует ручной настройки (Git + JCasC)   | Встроенное (Versioned Settings)       |
| Масштабируемость| Shared Libraries, Job DSL                | Meta-Runners, Templates               |
| Подход          | Программируемый (как код)                | Декларативный (GUI-ориентированный)   |
| Интеграция с IDE| Плагины для IDEA                         | Нативная поддержка JetBrains IDE      |
| Отладка         | Через логи                               | Визуальный дебаггер сборок            |
| Обучение        | Документация + сообщество                | Официальная документация + туториалы  |
| Поддержка Docker| Через плагины                            | Встроенная поддержка                  

 Когда выбрать Jenkins?  
 - Нужна полная кастомизация через код.  
 - Бюджет ограничен (Jenkins бесплатный).  

 Когда выбрать TeamCity?  
 - Хочется удобный GUI + код (Kotlin DSL).  
 - Нужна встроенная история изменений.  
 - Готовы платить за Enterprise-функции. 
### Как можно интегрировать систему контроля версий с TeamCity или Jenkins для автоматического запуска сборок?

Для автоматического запуска сборок при изменениях в репозитории можно использовать вебхуки (Webhooks) или опрос SCM (Poll SCM). Рассмотрим оба подхода для TeamCity и Jenkins.  
1. Интеграция с Jenkins  
  Способ 1: Вебхуки (GitHub/GitLab/Bitbucket)  
 Как работает:  
 При пуше в репозиторий VCS отправляет HTTP-запрос в Jenkins, который запускает сборку.  
 Способ 2: Poll SCM (опрос репозитория)  
 Если вебхуки недоступны, Jenkins может периодически проверять репозиторий.  
 - В настройках джоба → «Триггеры сборки» → «Poll SCM».  
 - Укажите расписание (например, `*/5 * * * *` — проверка каждые 5 минут). 

| Критерий            | Вебхуки                            | Poll SCM                            |
| :------------------ | :--------------------------------- | :---------------------------------- |
| Скорость            | Мгновенный запуск                  | Задержка (проверка по расписанию)   |
| Нагрузка            | Меньше запросов к серверу          | Постоянные запросы к VCS            |
| Надежность          | Зависит от доступности вебхука     | Работает при недоступности вебхуков |
| Сложность настройки | Требует конфигурации в VCS         | Проще в реализации                  |
| Требования к сети   | Нужен публичный URL                | Работает в изолированных сетях      |
| Механизм работы     | Событийный (push-модель)           | Периодический опрос (pull-модель)   |
| Ресурсоемкость      | Эффективное использование ресурсов | Постоянное потребление ресурсов     |
| Точность            | Точно определяет изменения         | Возможны пропуски изменений         |

### Как настроить параметризованные сборки в Jenkins для поддержки различных сред выполнения?

Параметризованные сборки позволяют запускать один и тот же пайплайн с разными настройками
2. Добавление параметров  
 Choice Parameter (выбор среды)  
 - Позволяет выбрать среду из списка.  
 ```plaintext
 Name: ENVIRONMENT  
 Choices:  
 dev  
 stage  
 prod  
 Description: Выберите среду для деплоя  
3. Использование параметров в Pipeline  
 Declarative Pipeline (Jenkinsfile)  
 ```groovy
 pipeline {
     agent any
     parameters {
         choice(
             name: 'ENVIRONMENT',
             choices: ['dev', 'stage', 'prod'],
             description: 'Выберите среду'
         )
         string(
             name: 'VERSION',
             defaultValue: '1.0.0',
             description: 'Версия приложения'
         )
         booleanParam(
             name: 'RUN_TESTS',
             defaultValue: true,
             description: 'Запускать тесты?'
         )
     }
     stages {
         stage('Build') {
             steps {
                 echo ""Сборка версии ${params.VERSION} для среды ${params.ENVIRONMENT}""
                 sh 'mvn clean package'
             }
         }
         stage('Test') {
             when { expression { params.RUN_TESTS == true } }
             steps {
                 echo ""Запуск тестов в ${params.ENVIRONMENT}""
                 sh 'mvn test'
             }
         }
         stage('Deploy') {
             steps {
                 script {
                     if (params.ENVIRONMENT == 'prod') {
                         input message: ""Подтвердите деплой в prod"", ok: ""Развернуть""
                     }
                     echo ""Деплой в ${params.ENVIRONMENT}""
                     sh ""./deploy.sh --env ${params.ENVIRONMENT}""
                 }
             }
         }
     }
 }
 ```

 🔸 Freestyle Project (использование в скриптах)  
 - В Build Step → Execute shell:  
 ```bash
 if [ ""$ENVIRONMENT"" = ""prod"" ]; then
     echo ""Деплой в prod с версией $VERSION""
     ./deploy-prod.sh
 else
     echo ""Деплой в $ENVIRONMENT""
     ./deploy.sh --env $ENVIRONMENT
 fi
```

4. Запуск сборки с параметрами  
 - Вручную:  
   - Нажмите 
 5. Динамические параметры (Active Choices)  
 Плагин Active Choices позволяет создавать зависимые параметры.  
 6. Установите плагин ""Active Choices"".  
 7. Добавьте параметр типа ""Active Choices Parameter"":  
 ```groovy
 name: DEPLOY_REGION  
 script: groovyScript {
     if (ENVIRONMENT == ""dev"") {
         return [""us-east-1"", ""eu-west-1""]
     } else {
         return [""us-east-1""]
     }
 }
 ```

 🔹 6. Безопасность и валидация  
 - Параметры для prod:  
   - Добавьте approval step (ручное подтверждение).  
   ```groovy
   stage('Prod Approval') {
       when { branch 'main' }
       steps {
           input message: ""Подтвердите деплой в prod"", ok: ""Развернуть""
       }
   }
   ```
 - Запрет прямого деплоя в prod из feature-веток:  
 ```groovy
   when {
       allOf {
           expression { params.ENVIRONMENT == 'prod' }
           branch 'main'
       }
   }""Build with Parameters"" → укажите нужные значения → ""Build"".  
 - Через API:
```


 Итог:
- Гибкость: Один пайплайн для всех сред.  
-  Безопасность: Ручное подтверждение для prod.  
- Автоматизация: Запуск через API или вебхуки. 

### Как использовать Docker в связке с TeamCity или Jenkins для создания изолированных сред для сборки и тестирования?

Интеграция Docker с CI-системами (TeamCity и Jenkins) позволяет создавать изолированные, воспроизводимые среды для сборки и тестирования, избегая проблем с зависимостями и конфликтами версий. 
 1. Сборка в контейнере:
    - Запуск каждого этапа (build, test, deploy) в отдельном Docker-контейнере.
    - Использование официальных образов (например, `openjdk:11` для Java, `python:3.9` для Python).
 2. Тестирование в изолированной среде:
    - Запуск тестов в контейнере с предустановленными зависимостями.
 3. Деплой артефактов:
    - Создание Docker-образов и их отправка в Docker Registry (Docker Hub, GitLab Container Registry, AWS ECR).


Настройка Docker в Jenkins
  Шаг 1: Установка плагинов
 - Docker Plugin – управление Docker из Jenkins.
 - Docker Pipeline – поддержка Docker в Jenkinsfile.
 - Docker Build and Publish – сборка и публикация образов.

  Шаг 2: Настройка Docker-агента
 1. Вариант A: Docker as a build agent  
    ```groovy
    pipeline {
        agent {
            docker {
                image 'maven:3.8.4-openjdk-11'  // Образ для сборки
                args '-v $HOME/.m2:/root/.m2'   // Кэширование Maven
            }
        }
        stages {
            stage('Build') {
                steps {
                    sh 'mvn clean package'
                }
            }
        }
    }
    ```
    - Плюсы: Легко настроить, нет зависимости от хостовой системы.
    - Минусы: Нет доступа к Docker внутри контейнера (нельзя запускать `docker build`).

 2. Вариант B: Docker-in-Docker (DinD)  
    ```groovy
    agent {
        docker {
            image 'docker:20.10'
            args '-v /var/run/docker.sock:/var/run/docker.sock'
        }
    }
    ```
    - Плюсы: Можно запускать `docker build` внутри контейнера.
    - Минусы: Менее безопасно (не рекомендуется для production).

 3. Вариант C: Docker на отдельном сервере (Jenkins + Docker Host)  
    - Настройте Docker Daemon на удаленном хосте.
    - В Jenkins укажите:
      ```groovy
      environment {
          DOCKER_HOST = 'tcp://docker-host:2375'
      }
      ```

  Шаг 3: Пример Jenkinsfile с Docker
 ```groovy
 pipeline {
     agent any
     stages {
         stage('Build') {
             agent {
                 docker {
                     image 'gradle:7.4-jdk11'
                     args '-v /tmp/gradle-cache:/home/gradle/.gradle'
                 }
             }
             steps {
                 sh 'gradle build'
             }
         }
         stage('Test') {
             agent {
                 docker {
                     image 'python:3.9'
                     args '-v $PWD:/app'
                 }
             }
             steps {
                 sh 'pip install pytest && pytest /app/tests'
             }
         }
         stage('Docker Build') {
             agent any
             steps {
                 script {
                     docker.build(""my-app:${env.BUILD_NUMBER}"", ""--build-arg VERSION=${env.BUILD_NUMBER} ."")
                     docker.withRegistry('https://registry.example.com', 'docker-creds') {
                         docker.image(""my-app:${env.BUILD_NUMBER}"").push()
                     }
                 }
             }
         }
     }
 }
 ```


  3. Настройка Docker в TeamCity
  Шаг 1: Установка Docker-поддержки
 - Включите Docker в настройках TeamCity (нужен Docker-демон на сервере).

  Шаг 2: Использование Docker в Build Steps
 1. Сборка внутри контейнера:
    - В Build Configuration → Runner Type выберите `Docker`.
    - Укажите образ (например, `maven:3.8.4`).
    - Добавьте команды:
      ```bash
      mvn clean package
      ```

 2. Сборка Docker-образов:
    - Добавьте шаг Command Line:
      ```bash
      docker build -t my-app:${build.number} .
      docker push my-registry.com/my-app:${build.number}
      ```

 3. Использование Docker Compose:
    - Создайте `docker-compose.test.yml`:
      ```yaml
      version: '3'
      services:
        app:
          build: .
          ports: [""8080:8080""]
        db:
          image: postgres:13
      ```
    - Запустите тесты:
      ```bash
      docker-compose -f docker-compose.test.yml up --build --abort-on-container-exit
      ```

  Шаг 3: Пример конфигурации TeamCity
 ```kotlin
 // Kotlin DSL для TeamCity
 version = ""2023.05""
 project {
     buildType {
         id(""DockerBuild"")
         name(""Docker Build and Test"")
         steps {
             dockerCommand {
                 commandType = build {
                     source = path {
                         path = ""Dockerfile""
                     }
                     namesAndTags = ""my-app:%build.number%""
                 }
             }
             script {
                 scriptContent = """"""
                     docker run my-app:%build.number% npm test
                 """"""
             }
         }
     }
 }
 ```


  4. Лучшие практики
  Для Jenkins:
 - Кэширование зависимостей:  
   ```groovy
   args '-v $HOME/.m2:/root/.m2'  // Для Maven
   args '-v /tmp/gradle-cache:/home/gradle/.gradle'  // Для Gradle
   ```
 - Очистка контейнеров после сборки:
   ```groovy
   post {
       always {
           script {
               docker.image('maven:3.8.4').stop()
           }
       }
   }
   ```

  Для TeamCity:
 - Используйте Docker-агенты для масштабирования.
 - Храните Dockerfile в репозитории для воспроизводимости.


  5. Проблемы и решения

| Проблема                          | Решение                                                                 | Дополнительные рекомендации                     |
|:-----------------------------------|:-----------------------------------------------------------------------|:-----------------------------------------------|
| Нет доступа к Docker Daemon        | Настройте `DOCKER_HOST` или пробросьте `/var/run/docker.sock`          | Проверьте права на сокет (chmod 666)           |
| Ошибки при сборке образов          | Используйте `docker build --no-cache`                                  | Анализируйте логи через `docker build --progress=plain` |
| Контейнеры не останавливаются      | Добавьте `--rm` в `docker run` или явный `docker stop` в Jenkinsfile   | Используйте `docker-compose down` для композов |
| Медленные сборки                   | Настройте локальный Registry или кэш зависимостей                      | Рассмотрите BuildKit (`DOCKER_BUILDKIT=1`)     |
| Нехватка дискового пространства    | Регулярно выполняйте `docker system prune`                             | Настройте автоматическую очистку через cron     |
| Проблемы с сетью между контейнерами| Создайте пользовательскую bridge-сеть (`docker network create`)        | Проверьте firewall правила                     |
| Утечки памяти                      | Установите лимиты через `-m` и `--memory-swap`                        | Мониторьте через `docker stats`                |
| Проблемы с правами                 | Используйте `--user` или настройте правильные UID/GID в образе         | Избегайте root в контейнерах                   |

  Итог
 - Jenkins: Гибкость через `Jenkinsfile`, но требует ручной настройки Docker.  
 - TeamCity: Встроенная поддержка Docker, но менее гибкая, чем Jenkins.  

 Что выбрать?  
 - Если нужен полный контроль → Jenkins + Docker Pipeline.  
 - Если важна простота → TeamCity с Docker-раннерами.  

### Как настроить мониторинг и уведомления о состоянии сборки в системах непрерывной интеграции, чтобы оперативно реагировать на ошибки?

Чтобы оперативно реагировать на сбои в сборках, нужно настроить:  
 1) Мониторинг статуса сборок,  
 2) Автоматические уведомления (Slack, Email, Telegram),  
 3) Визуализацию метрик (графики, дашборды).  

 Рассмотрим решения для Jenkins и TeamCity.

1. Мониторинг сборок  
  В Jenkins  
 1.1. Плагины для мониторинга  
 - Build Monitor View – визуализация статуса сборок на dashboard.  
 - Blue Ocean – улучшенный интерфейс с графическим представлением пайплайнов.  
 - Prometheus Metrics – экспорт метрик для Grafana.  

 1.2. Настройка Prometheus + Grafana  
 1. Установите плагин Prometheus Metrics.  
 2. Добавьте в Jenkins конфигурацию Prometheus:  
    ```plaintext
    Управление Jenkins → Система → Prometheus → /prometheus
    ```  
 3. Настройте Grafana для отображения данных:  
    - Импортируйте дашборд ""Jenkins Performance and Health"" (ID: 9964).  


  В TeamCity  
 1.1. Встроенные инструменты  
 - Projects Overview – общий статус всех сборок.  
 - Custom Charts – графики времени сборки, успешности и т.д.  

 1.2. Интеграция с Grafana  
 1. Используйте REST API TeamCity для сбора метрик.  
 2. Настройте Grafana с плагином TeamCity DataSource.  

 3. Уведомления о сборках  
  Общие каналы оповещений  

| Канал уведомлений | Jenkins                                                                 | TeamCity                                                                 |
|:------------------|:------------------------------------------------------------------------|:-------------------------------------------------------------------------|
| Email             | Встроенная поддержка SMTP                                               | Встроенная поддержка SMTP                                                |
| Slack             | Официальный плагин `Slack Notification`                                 | Официальный плагин `Slack Notifications`                                 |
| Telegram          | Плагин `Telegram Notifications`                                         | Через Webhooks + кастомный бот                                           |
| MS Teams          | Плагин `Office 365 Connector`                                           | Через Incoming Webhook                                                   |
| Webhooks          | Плагин `Generic Webhook Trigger`                                        | Встроенная поддержка Webhooks                                            |
| Mobile Push       | Через плагины (например, `Pushover Notifications`)                      | Ограниченная поддержка через Email-to-SMS                                |
| SMS               | Через плагины (например, `Twilio Notifications`)                        | Через интеграцию со сторонними сервисами                                 |
| Консольные уведомления | Встроенные (лог сборки)                                            | Встроенные (лог сборки + системные нотификации)                          |
| Кастомные интеграции | Через Groovy-скрипты и REST API                                     | Через Kotlin DSL и REST API                                              |

Ключевые особенности:
- **Jenkins**: Более гибкая система уведомлений за счет огромного количества плагинов
- **TeamCity**: Более стабильная встроенная реализация популярных каналов
- Обе системы поддерживают REST API для кастомных интеграций
- Для корпоративных решений TeamCity предлагает более централизованное управление уведомлениями


  Настройка Slack-уведомлений  
 В Jenkins  
 4. Установите Slack Notification Plugin.  
 5. В Manage Jenkins → Configure System найдите Slack:  
    - Workspace → `your-workspace.slack.com`  
    - Credential → Добавьте Slack Token (`xoxb-...`).  
 6. В пайплайне добавьте:  
    ```groovy
    post {
        success {
            slackSend channel: '#ci-alerts', message: ""✅ Сборка ${env.JOB_NAME} успешна! (<${env.BUILD_URL}|Подробнее>)"" 
        }
        failure {
            slackSend channel: '#ci-alerts', message: ""❌ Сборка ${env.JOB_NAME} упала! (<${env.BUILD_URL}|Исправить>)"" 
        }
    }
    ```

 В TeamCity  
 7. Установите Slack Notifications Plugin.  
 8. В Project Settings → Notifications добавьте правило:  
    - Event: Build Failed / Build Success.  
    - Message Template:  
      ```plaintext
      {build.status} в {project.name}: {build.name} ({build.link})
      ```


Настройка Telegram-уведомлений  
 В Jenkins  
 9. Установите Telegram Notifications Plugin.  
 10. Создайте бота через @BotFather и получите `API Token`.  
 11. В пайплайне:  
 ```groovy
    post {
        failure {
            telegramSend message: ""🔴 Сборка ${env.JOB_NAME} упала!"", 
                         chatId: ""@your_channel"", 
                         token: ""123456:ABC-DEF1234""
        }
    }
 ```

 В TeamCity  
 Используйте Webhooks:  
 13. Создайте скрипт (Python/PHP), который отправляет запросы в Telegram API.  
 14. В Build Failure Conditions → Trigger Webhook:  
  ```plaintext
    URL: https://api.telegram.org/bot<TOKEN>/sendMessage?chat_id=@channel&text=Build failed
   ```

 15. Расширенные сценарии  
  Оповещения только при критических ошибках  
 В Jenkins (Groovy):  
 ```groovy
 post {
     failure {
         script {
             if (currentBuild.duration > 10*60*1000) {  // Если сборка длилась >10 мин
                 slackSend channel: '#critical', message: ""🚨 Долгая сборка: ${env.JOB_NAME}""
             }
         }
     }
 }
 ```

 В TeamCity (Kotlin DSL):  
 ```kotlin
 failureConditions {
     executionTimeoutMin = 10  // Оповещать, если сборка длится >10 мин
 }
 ```

 📌 Автоматическое создание Issue в Jira  
 В Jenkins:  
 16. Установите Jira Plugin.  
 17. В пайплайне:  
 ```groovy
    post {
        failure {
            jiraIssue projectKey: ""PROJ"", 
                     issueType: ""Bug"", 
                     summary: ""Сборка ${env.JOB_NAME} упала""
        }
    }
  ```

  18. Дашборды и логирование  
  ELK Stack (Elasticsearch + Kibana)  
 - Настройте Filebeat для сбора логов Jenkins/TeamCity.  
 - Визуализируйте ошибки в Kibana.  

  Встроенные отчеты  
 - Jenkins: View → Test Results Analyzer.  
 - TeamCity: Build Results → Tests.  


 🔹 Итог  

| Инструмент | Плюсы                                                                 | Минусы                                                                 |
|:-----------|:---------------------------------------------------------------------|:----------------------------------------------------------------------|
| **Jenkins**  | • Открытый исходный код (бесплатный)<br>• Огромное сообщество и документация<br>• Тысячи плагинов для любых задач<br>• Полная кастомизация через Groovy | • Сложная первоначальная настройка<br>• Требует обслуживания<br>• Интерфейс устарел<br>• Производительность зависит от конфигурации |
| **TeamCity** | • Простая установка и настройка<br>• Современный интерфейс<br>• Встроенные функции без плагинов<br>• Отличная поддержка .NET и Java-проектов<br>• Нативная интеграция с IDE JetBrains | • Ограниченная бесплатная версия (до 100 конфигураций)<br>• Меньше плагинов<br>• Менее гибкий для нестандартных задач<br>• Коммерческая лицензия для больших проектов |

 Рекомендации по выбору:
- **Выберите Jenkins**, если нужна максимальная гибкость и есть время на настройку
- **Выберите TeamCity**, если важна простота и готовые решения "из коробки"
- Для enterprise-решений TeamCity часто оказывается выгоднее из-за экономии времени
- Для open-source проектов Jenkins предпочтительнее из-за нулевой стоимости

 Рекомендации:  
 1. Для Slack/Email → используйте встроенные решения.  
 2. Для продвинутого мониторинга → Prometheus + Grafana.  
 3. Для автоматических действий → Webhooks + скрипты.  

 Настроили уведомления, но что-то не работает? Проверьте:  
 - Логи Jenkins (`/var/log/jenkins`).  
 - Консоль TeamCity (`TeamCity Server Logs`).  
 - Доступность API (Slack/Telegram).  
