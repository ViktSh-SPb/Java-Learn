## 🧾 **Код 204 — No Content**
**Значение:**
> Сервер успешно обработал запрос, **но не возвращает тело ответа (body)**.
Это значит, что:
- операция **выполнена корректно** (без ошибок);
- **возвращать данные не требуется** (например, ресурс не изменился или тело не нужно клиенту).
## ⚙️ **Когда используется 204**

|Сценарий|Пример|Пояснение|
|---|---|---|
|✅ **Успешное удаление**|`DELETE /users/123` → `204 No Content`|Пользователь удалён, тело ответа не нужно.|
|✅ **Успешное обновление без результата**|`PUT /users/123 { "status": "active" }` → `204 No Content`|Изменения применены, но клиент не запрашивал обновлённые данные.|
|✅ **Успешный запрос без тела**|`POST /session/logout` → `204 No Content`|Выход выполнен, тело ответа не требуется.|
## ⚠️ **Важно помнить**
- Ответ **не должен содержать тело (body)**.  
    Если сервер добавит тело — оно **будет проигнорировано** клиентами.
- Можно вернуть **заголовки**, например `Cache-Control`, `ETag` и т.п.
- После 204 клиент **не должен ожидать редиректов или нового контента**.
- Часто используется для оптимизации — **экономия трафика** и **ускорение отклика**.
## 🧩 **Пример**
### Запрос:
```http
DELETE /api/v1/tasks/10 HTTP/1.1
Authorization: Bearer eyJhbGciOiJIUzI1Ni...
```
### Ответ:
```http
HTTP/1.1 204 No Content
```
— означает: задача успешно удалена, возвращать данные не нужно.
## 💬 **Отличие от других похожих кодов**

|Код|Название|Отличие|
|---|---|---|
|**200 OK**|Возвращает данные|Сервер обработал запрос и отдал тело (например, JSON).|
|**202 Accepted**|Асинхронная обработка|Запрос принят, но ещё не завершён.|
|**204 No Content**|Нет данных в ответе|Запрос выполнен полностью, тело не нужно.|
## 🧠 Итого:
> **204 No Content** — это сигнал клиенту:  
> _«Операция прошла успешно, но тебе нечего отображать»._