# Stash
**git stash** - позволяет отложить текущие незавершенные изменения (включая изменения в отслеживаемых файлах и staged-файлах), чтобы переключиться на другую задачу или ветку без создания коммита. Это "черновик" для кода.
## Основные команды
### 1. Сохранить изменения в stash
```bash
git stash
# или с комментарием:
git stash push -m "Описание изменений"
```
### 2. Просмотреть список сохраненных stash'ей
```bash
git stash list
```
Вывод:
```bash
stash@{0}: On main: Название stash\'a
stash@{1}: WIP on feature: a1b2c3d...
```
### 3. Вернуть последние отложенные изменения
```bash
git stash pop     # изменения из stash появляются как незакомиченные, из stash удаляются
```
или:
```bash
git stash apply   # применить, но ОСТАВИТЬ в списке
```
### 4. Вернуть конкретный stash

```bash
git stash apply stash@{1}   # где 1 - номер stash'a
```
### 5. Удалить stash
```bash
git stash drop stash@{0}    # удалить конкретный stash
git stash clear             # удалить ВСЕ stash'ы
```
## Ключи

| Опция                        | Описание                                               |
| ---------------------------- | ------------------------------------------------------ |
| `-u` / `--include-untracked` | Сохраняет и новые (неотслеживаемые) файлы              |
| `-a` / `--all`               | Сохраняет всё, включая игнорируемые файлы (.gitignore) |
| `--keep-index`               | Не сохраняет staged-изменения (только unstaged)        |
| `-p` / `--patch`             | Интерактивный режим (выбор изменений для stash)        |
## Примеры использования
### 1. Экстренное переключение на другую ветку
```bash
git stash                # сохранили текущие изменения
git checkout main        # переключились на main
# что-то сделали
git checkout feature
git stash pop            # восстановили сохраненные изменения
```
### 2. Сохранение новых файлов
```bash
git stash -u
```
### 3. Создание ветки из stash
```bash
git stash branch new-branch stash@{1}  # создаст ветку new-branch и применит stash
```
## Чем полезен stash?
1. **Временное переключение контекста** - например, срочно починить баг в main, не коммитя текущую работу.
2. **Сохранение экспериментов** - если изменения пока не готовы для коммита, но и терять их не хочется.
3. **Работа с грязной рабочей директорией** - когда *git pull* или *git merge* требует чистого состояния.
## Ограничения
1. Не сохраняет:
	- Игнорируемые файлы (без `-а`).
	- Неотслеживаемые файлы (без `-u`).
2. **Stash**'и не синхронизируются между разными клонами репозитория.
## Советы
📌Всегда добавляйте описание к **stash**, чтобы не запутаться.
```bash
git stash push -m "Работа над модулями оплаты"
```
📌Проверяйте список перед удалением.
```bash
git stash show stash@{1}   # Посмотреть изменения в stash
```
📌Используйте `git stash -p` если нужно сохранить только часть изменений.