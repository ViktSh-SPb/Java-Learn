В RabbitMQ **обмен сообщениями** осуществляется через компонент **Exchange**, который принимает сообщения от продюсеров и направляет их в очереди по определённым правилам. RabbitMQ поддерживает несколько типов обменов, каждый из которых реализует свою логику маршрутизации.
## 1️⃣ **Direct Exchange**
- **Принцип работы**: сообщение отправляется в **очередь с точно соответствующим routing key**.
- Используется, когда **нужно доставить сообщение конкретной очереди**.
**Пример:**
```text
Exchange "direct_orders" 
Routing key = "order_created" 
→ очередь "order_processing"
```
## 2️⃣ **Fanout Exchange**
- **Принцип работы**: сообщение отправляется **во все очереди**, привязанные к exchange, **игнорируя routing key**.
- Используется для **broadcast-подобной доставки** сообщений нескольким подписчикам.
**Пример:**  
Все очереди, подписанные на exchange `notifications`, получат одно и то же сообщение о новом пользователе.
## 3️⃣ **Topic Exchange**
- **Принцип работы**: сообщение доставляется в очереди, **routing key которых соответствует шаблону (pattern)**.
- Шаблоны используют `*` (один сегмент) и `#` (любое количество сегментов).
**Пример:**
- Сообщение с ключом `user.signup.email` → попадёт в очередь с шаблоном `user.*.email`
- Сообщение с ключом `user.signup.sms` → попадёт в очередь с шаблоном `user.#`
## 4️⃣ **Headers Exchange**
- **Принцип работы**: маршрутизация не по routing key, а по **хедерам сообщения**.
- Сообщение попадает в очередь, если заголовки соответствуют заданным условиям.
- Гибкий способ фильтрации сообщений по различным атрибутам.
**Пример:**
- Заголовки: `{"type": "order", "priority": "high"}`
- Очередь принимает только сообщения с `type=order` и `priority=high`.
## 5️⃣ Краткое сравнение типов обменов

|Тип Exchange|Routing Key|Применение|
|---|---|---|
|**Direct**|точное соответствие|Доставка в конкретную очередь|
|**Fanout**|игнорируется|Broadcast сообщений всем очередям|
|**Topic**|шаблон (`*`, `#`)|Гибкая маршрутизация по категориям|
|**Headers**|заголовки|Маршрутизация по метаданным, не по ключу|
💡 **Итого**:  
Тип обмена в RabbitMQ определяет **как и куда будут доставлены сообщения**, что позволяет реализовать разные сценарии:
- Очереди задач (direct)
- Рассылка уведомлений (fanout)
- Сложная фильтрация событий (topic, headers)
