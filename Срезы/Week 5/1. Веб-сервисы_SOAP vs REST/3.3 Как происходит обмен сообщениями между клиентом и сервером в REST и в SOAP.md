Обмен сообщениями в **REST** и **SOAP** принципиально отличается, потому что один — это **архитектурный стиль**, а другой — **строгий протокол** с фиксированной структурой сообщений.
## 🔹 1. REST
**REST** использует стандартные возможности **HTTP** для обмена данными.
### Основные моменты:
1. **Методы HTTP**
    - `GET` — получение данных
    - `POST` — создание ресурса
    - `PUT` — обновление ресурса
    - `DELETE` — удаление ресурса
    - `PATCH` — частичное обновление
2. **Обмен данными**
    - Запрос и ответ обычно передаются в **JSON** (реже XML или другой формат).
    - Каждый ресурс имеет свой **URI**, например:
        ```
        GET /users/123
        POST /orders
        ```
3. **Структура обмена**
    - **Клиент → Сервер:** HTTP-запрос с методом, URI, заголовками и телом (для POST/PUT).
    - **Сервер → Клиент:** HTTP-ответ с кодом состояния (`200 OK`, `404 Not Found`) и телом ответа с данными.
**Пример GET-запроса:**
```http
GET /users/123 HTTP/1.1
Host: api.example.com
Accept: application/json
```
**Пример ответа:**
```json
{
  "id": 123,
  "name": "Alice",
  "email": "alice@example.com"
}
```
**Особенности REST:**
- Сообщения **легковесные**, структура гибкая.
- Не требует специальных библиотек — достаточно HTTP-клиента.
- Статус ответа и заголовки передают метаданные о выполнении запроса.
## 🔹 2. SOAP
**SOAP** — это строгий протокол обмена сообщениями через XML.
### Основные моменты:
1. **Метод передачи**
    - Чаще всего используется **HTTP POST**, но может работать и через SMTP, TCP и др.
2. **Структура сообщения**
    - **Envelope** — обёртка всего сообщения
    - **Header** — опционально, для аутентификации, подписей, маршрутизации
    - **Body** — тело запроса или ответа, содержит данные и метод
**Пример запроса SOAP:**
```xml
<soapenv:Envelope xmlns:soapenv="http://schemas.xmlsoap.org/soap/envelope/"
                  xmlns:ser="http://example.com/service">
   <soapenv:Header/>
   <soapenv:Body>
      <ser:getUser>
         <ser:id>123</ser:id>
      </ser:getUser>
   </soapenv:Body>
</soapenv:Envelope>
```
**Пример ответа SOAP:**
```xml
<soapenv:Envelope xmlns:soapenv="http://schemas.xmlsoap.org/soap/envelope/"
                  xmlns:ser="http://example.com/service">
   <soapenv:Body>
      <ser:getUserResponse>
         <ser:id>123</ser:id>
         <ser:name>Alice</ser:name>
         <ser:email>alice@example.com</ser:email>
      </ser:getUserResponse>
   </soapenv:Body>
</soapenv:Envelope>
```
**Особенности SOAP:**
- Сообщения **тяжёлые**, с строгой структурой и обязательным XML.
- Могут содержать **подписи, шифрование, токены** прямо в сообщении.
- Используется **WSDL** для описания всех методов сервиса и структуры сообщений.
## 🔹 Сравнительное резюме

|Критерий|REST|SOAP|
|---|---|---|
|Протокол|HTTP (GET, POST, PUT, DELETE)|HTTP POST (обычно), SMTP, TCP|
|Формат данных|JSON, XML, YAML|XML только|
|Структура сообщения|Лёгкая, гибкая|Жёсткая: Envelope → Header → Body|
|Аутентификация|HTTPS, OAuth, JWT|WS-Security, сертификаты, SAML|
|Масштабируемость|Легко кешируется, быстрый|Тяжеловесный, сложнее кэшировать|
|Документация|Часто Swagger/OpenAPI|WSDL обязательно|
💡 **Вывод:**
- **REST** — лёгкий и простой обмен через HTTP с гибкой структурой данных.
- **SOAP** — строгий и формальный обмен XML-сообщениями с поддержкой встроенной безопасности и контрактов.