Речь идёт о **контекстном пути (context path)** или **контекстном меню / контексте веб-приложения** в **Tomcat**, то есть о том, **под каким URL** ваше приложение будет доступно, например:

```
http://localhost:8080/myapp
```
Контекст в Tomcat определяет:
- где физически расположено приложение;
- под каким путём (context path) оно доступно в браузере;
- какие настройки (ресурсы, параметры, логирование и т. д.) применяются к нему.
## 🧩 1. **Что такое контекст Tomcat**
Контекст (Context) — это конфигурационный элемент `<Context>`, который описывает **одно веб-приложение**.  
Он определяет:
- имя контекста (например, `/shop`);
- путь к каталогу приложения или `.war` файлу;
- ресурсы (например, базы данных через JNDI);
- настройки перезапуска и кэширования.
## ⚙️ 2. **Способы настройки контекста**
### ✅ **Вариант 1: через `server.xml`**
Можно явно указать контекст в файле  
`<TOMCAT_HOME>/conf/server.xml`.
Пример:
```xml
<Host name="localhost" appBase="webapps">
    <Context path="/shop" docBase="C:/projects/myshop" reloadable="true" />
</Host>
```
🔹 Здесь:
- `path="/shop"` — URL-префикс приложения (`http://localhost:8080/shop`);
- `docBase` — путь к каталогу приложения или `.war` файлу;
- `reloadable="true"` — автоматическая перезагрузка при изменении классов.
> ⚠️ Рекомендуется **не изменять напрямую `server.xml`**,  
> лучше использовать отдельные файлы контекста (см. ниже).
### ✅ **Вариант 2: через файл контекста (`context.xml`)**
Создай файл:
```
<TOMCAT_HOME>/conf/Catalina/localhost/myapp.xml
```
С содержимым:
```xml
<Context docBase="C:/projects/myapp" reloadable="true" />
```
После этого приложение будет доступно по адресу:
```
http://localhost:8080/myapp
```
> 💡 Имя файла (`myapp.xml`) = имя контекста (URL-путь).
### ✅ **Вариант 3: внутри самого приложения (`META-INF/context.xml`)**
Внутри структуры проекта:
```
src/main/webapp/META-INF/context.xml
```
Пример:
```xml
<Context reloadable="true">
    <Resource name="jdbc/MyDB"
              auth="Container"
              type="javax.sql.DataSource"
              username="user"
              password="pass"
              driverClassName="org.postgresql.Driver"
              url="jdbc:postgresql://localhost:5432/mydb"
              maxTotal="20"
              maxIdle="10"/>
</Context>
```
При деплое WAR Tomcat автоматически подхватит этот файл.
## 🔧 3. **Изменение контекстного пути при деплое WAR**
Если ты деплоишь `.war` в папку:
```
<TOMCAT_HOME>/webapps/
```
То контекстный путь **берётся из имени файла**:

|Имя WAR|URL приложения|
|---|---|
|`myapp.war`|`http://localhost:8080/myapp`|
|`ROOT.war`|`http://localhost:8080/` (главное приложение)|
Хочешь изменить URL — просто переименуй `.war`.
## 🧠 4. **Пример с контекстными параметрами**
Можно задать параметры для всего приложения:
```xml
<Context path="/shop" docBase="C:/projects/shop">
    <Parameter name="theme" value="dark" override="false" />
</Context>
```
В приложении ты можешь получить параметр так:
```java
String theme = getServletContext().getInitParameter("theme");
```
## 🚀 5. **Проверка результата**
После настройки:
1. Запусти Tomcat (`bin/startup.bat` на Windows).
2. Открой в браузере:
    ```
    http://localhost:8080/myapp
    ```
3. Если всё верно — отобразится твоя главная страница.
## ⚠️ 6. **Типичные ошибки**

|Ошибка|Причина|Решение|
|---|---|---|
|`HTTP 404`|Неправильный `path` или `docBase`|Проверь пути|
|Контекст не подхватывается|Файл `.xml` не в `Catalina/localhost`|Помести туда|
|Изменения не применяются|Кеш Tomcat|Очисти `work/` и перезапусти сервер|
