## 1️⃣ **Unit-тестирование сервисов**
- **Цель:** проверить отдельные классы и методы внутри сервиса в изоляции.
- **Особенности:**
    - Использование моков для зависимостей (`Mockito`, `MockK`).
    - Быстрое выполнение, выявляет ошибки на раннем этапе разработки.
- **Пример:** тестирование бизнес-логики сервиса `OrderService`, не обращаясь к базе данных или другим сервисам.
## 2️⃣ **Integration-тестирование отдельных микросервисов**
- **Цель:** проверить взаимодействие микросервиса с его зависимостями (БД, очереди, API других сервисов).
- **Особенности:**
    - Использование **тестовых контейнеров** (`Testcontainers`) для базы данных или очередей.
    - Проверка API сервиса через HTTP (`MockMvc`, `RestAssured`).
- **Пример:** проверка создания заказа в `OrderService` с записью в тестовую базу данных PostgreSQL.
## 3️⃣ **Consumer-driven контрактное тестирование (CDC)**
- **Цель:** убедиться, что сервисы корректно взаимодействуют друг с другом через API или события.
- **Особенности:**
    - Потребитель (consumer) определяет контракт, который поставщик (provider) должен соблюдать.
    - Используются инструменты: **Pact**, **Spring Cloud Contract**.
- **Пример:** `PaymentService` должен принимать события от `OrderService` в формате X → контрактное тестирование гарантирует совместимость.
## 4️⃣ **End-to-End (E2E) тестирование**
- **Цель:** проверить работу всей системы микросервисов как единого целого.
- **Особенности:**
    - Тесты запускаются в **тестовом окружении с реальными сервисами**.
    - Часто используют автоматизированное тестирование UI (`Selenium`) или API (`RestAssured`).
- **Пример:** размещение заказа через фронтенд → проверка событий во всех микросервисах → проверка уведомлений пользователю.
## 5️⃣ **Contract / API Testing**
- **Цель:** проверить публичные API микросервисов на соответствие спецификациям (OpenAPI/Swagger).
- **Особенности:**
    - Проверка формата запросов/ответов, статусов HTTP, схем данных.
    - Используются Postman, REST Assured, Karate.
## 6️⃣ **End-to-End тестирование через события (Event-driven E2E)**
- **Цель:** проверить обработку событий между микросервисами, если используется **асинхронная коммуникация** (Kafka, RabbitMQ).
- **Особенности:**
    - Тестируются топики, очереди, обработчики событий.
    - Можно использовать тестовые брокеры или embedded Kafka.
- **Пример:** `OrderService` публикует событие `OrderCreated` → `PaymentService` его обрабатывает → `NotificationService` отправляет уведомление.
## 7️⃣ **Performance / Load Testing**
- **Цель:** проверить производительность микросервисов под нагрузкой.
- **Особенности:**
    - Микросервисы могут масштабироваться горизонтально → тестирование через JMeter, Gatling, Locust.
- **Пример:** проверка обработки 1000 заказов в секунду системой микросервисов.
## 8️⃣ **Chaos Testing / Resilience Testing**
- **Цель:** проверить устойчивость микросервисов к сбоям.
- **Особенности:**
    - Искусственно вызываются сбои, задержки или недоступность сервисов.
    - Инструменты: **Chaos Monkey**, **Simian Army**, **Gremlin**.
- **Пример:** отключение одного инстанса `PaymentService` → проверка, что система продолжает обработку заказов.
## 🔹 Рекомендации по стратегии тестирования микросервисов
1. **Тестируйте на нескольких уровнях:**
    - Unit → Integration → Contract → End-to-End
2. **Используйте изоляцию для unit и integration тестов** (моки, тестовые контейнеры).
3. **Контрактное тестирование** важно для независимого развития сервисов.
4. **E2E тесты** делают реальную проверку, но они медленные — запускать реже.
5. **Нагрузочные и хаос-тесты** помогают выявить проблемы масштабируемости и отказоустойчивости.