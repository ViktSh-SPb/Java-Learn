В Kubernetes **Операторы (Operators)** — это способ автоматизации управления сложными приложениями, расширяющий возможности обычных контроллеров. Они позволяют **интеллектуально управлять жизненным циклом приложения**, учитывая его бизнес-логику.
## 1️⃣ Принцип работы операторов
1. **Custom Resource Definition (CRD)**
    - Оператор добавляет в Kubernetes **свой собственный тип ресурса** (Custom Resource, CR).
    - Пример: `MySQLCluster`, `RedisCluster`, `KafkaTopic`.
    - CRD описывает **состояние и конфигурацию приложения**, которое оператор будет контролировать.
2. **Контроллер (Controller)**
    - Основная логика оператора — это контроллер, который наблюдает за объектами CRD.
    - Контроллер реализует **“Desired State → Current State”**:
        - Desired State — то, как должно выглядеть приложение (например, 3 реплики MySQL).
        - Current State — текущее состояние в кластере.
        - Если Current State не совпадает с Desired State, оператор предпринимает действия для синхронизации.
3. **Реакция на события**
    - Когда создаётся или изменяется объект CR, оператор получает событие.
    - Оператор анализирует, что нужно сделать (например, добавить новые поды, обновить конфигурацию, создать секреты).
    - Все изменения происходят **автоматически**, без ручного вмешательства.
## 2️⃣ Пример использования
### Пример: MySQL Operator
1. Создаём объект CR `MySQLCluster`:
```yaml
apiVersion: mysql.presslabs.org/v1alpha1
kind: MysqlCluster
metadata:
  name: mydb
spec:
  replicas: 3
  version: "8.0"
```
2. Оператор следит за этим объектом:
    - Проверяет, что есть 3 пода MySQL нужной версии.
    - Если один Pod упал — автоматически создаёт новый.
    - Обновление версии MySQL — оператор делает поэтапный rolling update, сохраняя данные.
## 3️⃣ Применение операторов
- **Базы данных и stateful-приложения**: PostgreSQL, MySQL, MongoDB, Cassandra.
- **Сложные middleware**: Kafka, Elasticsearch, Redis Cluster.
- **Автоматизация DevOps**: резервное копирование, обновления, мониторинг.
## 4️⃣ Преимущества
1. **Автоматизация сложных операций**
    - Создание, обновление, масштабирование, резервное копирование.
2. **Сохранение согласованного состояния (desired state)**
    - Даже при сбое узлов или подов оператор восстанавливает приложение.
3. **Интеграция бизнес-логики**
    - Оператор знает специфику приложения: например, как безопасно обновлять кластеры баз данных.
### 🔹 Итог
**Оператор — это расширенный контроллер Kubernetes**, который:
1. Использует **Custom Resource (CR)** для описания состояния приложения.
2. Следит за текущим состоянием приложения.
3. Автоматически приводит его в соответствие с желаемым состоянием, включая сложные операции.