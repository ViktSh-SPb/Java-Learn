HATEOAS (Hypermedia As The Engine Of Application State) — это один из ключевых принципов REST, который определяет, что **клиент взаимодействует с сервером исключительно через гипермедиа (ссылки и действия), предоставленные самим сервером**, а не через заранее жестко запрограммированные URL и действия.
### 🔹 Суть
- Клиент получает не только данные ресурса, но и информацию о **доступных действиях и связанных ресурсах**.
- Сервер «говорит» клиенту, что можно делать дальше, через ссылки и методы (GET, POST, PUT, DELETE и т.д.).
- Это делает API **самодокументируемым и более гибким**, поскольку новые операции могут добавляться на сервере без изменения клиентской логики.
### 🔹 Пример JSON с HATEOAS
```json
{
  "id": 1,
  "name": "Alice",
  "_links": {
    "self": { "href": "/users/1" },
    "update": { "href": "/users/1", "method": "PUT" },
    "delete": { "href": "/users/1", "method": "DELETE" },
    "friends": { "href": "/users/1/friends", "method": "GET" }
  }
}
```
- Клиент видит **данные пользователя** и сразу **доступные действия**.
- Не нужно заранее знать маршруты для обновления, удаления или получения друзей — сервер предоставляет их динамически.
### 🔹 Преимущества HATEOAS
1. **Гибкость** — клиент не зависит от документации API, а использует гипермедиа.
2. **Эволюция API** — новые действия можно добавлять без ломки клиентов.
3. **Упрощение навигации** — клиент узнаёт доступные ресурсы и действия прямо из ответа сервера.