В **Kubernetes** объект **Service** отвечает за доступ к подам и балансировку нагрузки. Существуют несколько типов сервисов, которые различаются по способу доступа и сфере действия.
## 1️⃣ **ClusterIP (по умолчанию)**
- **Что делает:** Создаёт внутренний IP в кластере.
- **Доступность:** Только внутри кластера.
- **Когда использовать:** Внутренние сервисы, микросервисы общаются друг с другом.
- **Пример:**

```yaml
apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  type: ClusterIP
  selector:
    app: myapp
  ports:
    - port: 80
      targetPort: 8080
```
## 2️⃣ **NodePort**
- **Что делает:** Открывает порт на каждом узле кластера.
- **Доступность:** Вне кластера через `<NodeIP>:<NodePort>`.
- **Когда использовать:** Простые тестовые приложения, доступ снаружи без LoadBalancer.
- **Пример:**
```yaml
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080
```
- Kubernetes выберет порт в диапазоне 30000–32767, если не указан.
## 3️⃣ **LoadBalancer**
- **Что делает:** Интегрируется с облачным провайдером, создаёт внешний балансировщик нагрузки.
- **Доступность:** Снаружи кластера по внешнему IP.
- **Когда использовать:** Приложения, которые должны быть доступны извне в продакшне.
- **Пример:**
```yaml
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8080
```
- В облаках (AWS, GCP, Azure) автоматически создаётся LB.
## 4️⃣ **ExternalName**
- **Что делает:** Создаёт сервис, который ссылается на внешний DNS-ресурс.
- **Доступность:** Подам внутри кластера через внутренний DNS.
- **Когда использовать:** Чтобы поды обращались к внешнему сервису как к внутреннему.
- **Пример:**
```yaml
apiVersion: v1
kind: Service
metadata:
  name: external-db
spec:
  type: ExternalName
  externalName: db.example.com
```
- Не создаёт ClusterIP или NodePort, просто перенаправляет DNS-запросы.
## 🔹 Сравнительная таблица

|Тип Service|Доступность|Применение|
|---|---|---|
|ClusterIP|Внутри кластера|Микросервисы, внутренние сервисы|
|NodePort|Вне кластера через узел|Тестовые приложения|
|LoadBalancer|Вне кластера через LB|Продакшн-приложения, публичный доступ|
|ExternalName|Через DNS внутри кластера|Доступ к внешним сервисам|
