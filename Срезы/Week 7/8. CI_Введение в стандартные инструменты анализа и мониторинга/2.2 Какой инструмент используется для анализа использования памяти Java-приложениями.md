Для **анализа использования памяти Java-приложениями** применяются специальные инструменты — **memory profilers**. Они позволяют увидеть:
- сколько памяти потребляют объекты,
- какие классы создают больше всего аллокаций,
- где возникают утечки памяти (memory leaks),
- состояние кучи (heap dump) и распределение объектов в ней.
Самые популярные инструменты:
## 🔍 Основные инструменты для анализа памяти Java
### **1. VisualVM (бесплатный)**
- Идет вместе с JDK или ставится отдельно.
- Позволяет:
    - смотреть heap usage в реальном времени,
    - снимать heap dumps,
    - искать утечки памяти,
    - анализировать коллектор мусора (GC).
- Отличный инструмент для повседневной диагностики.
### **2. Java Flight Recorder (JFR) + JDK Mission Control (JMC)**
- Встроено в современные версии JVM.
- Очень низкая нагрузка → можно использовать на проде.
- Позволяет:
    - собирать информацию о аллокациях,
    - смотреть, какие объекты создаются слишком часто,
    - анализировать задержки GC,
    - изучать причины высокой памяти.
### **3. JProfiler (платный)**
- Один из самых мощных инструментов.
- Умеет:
    - анализ утечек,
    - allocation hotspots,
    - live memory view,
    - анализировать JDBC, HTTP, потоки и многое другое.
- Подходит для глубокой диагностики сложных проблем.
### **4. IntelliJ IDEA Profiler**
- Встроен в Ultimate версию IntelliJ.
- Позволяет запускать memory profiler прямо из IDE.
- Умеет снимать и анализировать heap dumps.
### **5. Eclipse Memory Analyzer (MAT)**
- Бесплатный, очень мощный инструмент для анализа **heap dump**.
- Лучший выбор, если нужно:
    - анализировать большой heap dump (гигабайты),
    - искать циклические зависимости,
    - определять источники утечек через _Leak Suspects Report_.
## 🏆 Что использовать?

|Задача|Инструмент|
|---|---|
|Базовый мониторинг|**VisualVM**|
|Продакшн-диагностика|**JFR + JMC**|
|Глубокое расследование|**JProfiler**|
|Анализ больших heap dump|**MAT**|
