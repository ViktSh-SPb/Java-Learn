## 🧱 1. Безопасность в REST
REST-сервисы работают поверх **HTTP**, поэтому принципы безопасности реализуются **на уровне протокола** и архитектуры API.
### 🔒 Основные механизмы безопасности:

|Подход|Суть|
|---|---|
|**HTTPS (TLS)**|Шифрует трафик между клиентом и сервером, предотвращая перехват данных.|
|**Аутентификация (Authentication)**|Подтверждает личность пользователя (Basic Auth, OAuth2, JWT, API Keys и др.).|
|**Авторизация (Authorization)**|Проверяет права пользователя на выполнение операции. Например, доступ к `/admin` только для администратора.|
|**Валидация данных**|Проверяет корректность входящих данных (тип, формат, длина) — защита от инъекций.|
|**Rate limiting**|Ограничивает частоту запросов (предотвращает DDoS).|
|**CORS (Cross-Origin Resource Sharing)**|Контролирует, с каких доменов разрешены запросы.|
|**Content-Type и Accept заголовки**|Позволяют контролировать тип данных (JSON, XML и т.п.), избегая XSS/инъекций.|

📘 Пример:
```http
GET /users
Authorization: Bearer eyJhbGciOiJIUzI1Ni...
```
— использование **JWT** позволяет аутентифицировать и авторизовать пользователя по токену.
## 🔁 2. Идемпотентность в REST
REST строго придерживается идемпотентности HTTP-методов (в отличие от SOAP).  
Это означает, что **повторение одного и того же запроса не должно изменять состояние сервера**.
### 🔹 Применение:

|Метод|Пример|Поведение|
|---|---|---|
|**GET**|`GET /users/1`|Только читает данные, безопасен и идемпотентен.|
|**PUT**|`PUT /users/1 { "name": "Alice" }`|Заменяет ресурс целиком. Повтор даёт тот же результат.|
|**DELETE**|`DELETE /users/1`|Удаляет. Повторное удаление не меняет результат (ресурса нет).|
|**POST**|`POST /users { "name": "Alice" }`|Создаёт новый ресурс. Повтор вызова → новый объект.|
|**PATCH**|`PATCH /users/1 { "status": "active" }`|Частичное обновление, может быть идемпотентным, если реализовано корректно.|
## 🧩 3. Связь безопасности и идемпотентности

|Принцип|Как связан с безопасностью|
|---|---|
|**Безопасные методы** (`GET`, `HEAD`) не изменяют данные|Можно кэшировать, логировать и повторять без риска.|
|**Идемпотентность** (`PUT`, `DELETE`) предотвращает повторное изменение данных при сетевых сбоях|Уменьшает вероятность непредсказуемых изменений при повторной отправке.|
|**POST (неидемпотентный)** требует особой защиты|Например, CSRF-токены, чтобы злоумышленник не мог повторно отправить создание ресурса.|
## 🛡️ Итого:

|Принцип|Применение в REST|
|---|---|
|**Безопасность**|Реализуется через HTTPS, токены, авторизацию, фильтрацию данных.|
|**Идемпотентность**|Гарантирует предсказуемое поведение при повторных запросах.|
|**Комбинация**|Позволяет строить устойчивые и защищённые REST API без побочных эффектов.|
