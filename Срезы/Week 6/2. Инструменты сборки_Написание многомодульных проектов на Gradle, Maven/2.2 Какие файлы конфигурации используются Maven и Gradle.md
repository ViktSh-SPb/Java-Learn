## 🧩 **Maven**
### Основной файл конфигурации:
**`pom.xml` (Project Object Model)**
📘 Это XML-файл, который определяет:
- **основную информацию о проекте** — имя, версия, группа (`groupId`, `artifactId`, `version`);
- **зависимости** проекта (dependencies);
- **плагины** и их конфигурацию;
- **настройки сборки** (build, profiles, properties);
- **наследование** от родительских POM’ов и **агрегацию** модулей (в multi-module проектах).
🔹 Пример:
```xml
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.example</groupId>
    <artifactId>my-app</artifactId>
    <version>1.0.0</version>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
            <version>3.3.0</version>
        </dependency>
    </dependencies>
</project>
```
### Дополнительные файлы Maven:
- **`settings.xml`** — глобальные или пользовательские настройки Maven (репозитории, прокси, профили).  
    📍Находится:
    - глобальный: `${MAVEN_HOME}/conf/settings.xml`
    - пользовательский: `~/.m2/settings.xml`
- **`profiles.xml`** — (редко используется) отдельное хранение профилей.
- **`pom.properties`** — генерируется автоматически при сборке (в `target/classes/META-INF/maven/...`).
## ⚙️ **Gradle**
Gradle более гибок, и его конфигурации пишутся на **Groovy** или **Kotlin DSL**.
### Основные файлы:
- **`build.gradle`** _(или `build.gradle.kts` для Kotlin DSL)_  
    Основной файл сборки проекта.
    В нём указываются:
    - зависимости (`dependencies`);
    - плагины (`plugins`);
    - задачи (`tasks`);
    - конфигурации сборки.
    🔹 Пример (`build.gradle`):
    ```groovy
    plugins {
        id 'java'
        id 'application'
    }
    
    group = 'com.example'
    version = '1.0.0'
    
    repositories {
        mavenCentral()
    }
    
    dependencies {
        implementation 'org.springframework.boot:spring-boot-starter-web:3.3.0'
        testImplementation 'org.junit.jupiter:junit-jupiter:5.10.0'
    }
    
    application {
        mainClass = 'com.example.Main'
    }
    ```
### Дополнительные файлы Gradle:
- **`settings.gradle` / `settings.gradle.kts`**  
    Определяет _структуру проекта_ — имя и состав модулей (в multi-module проектах).
    ```groovy
    rootProject.name = 'my-app'
    include 'core', 'web'
    ```
- **`gradle.properties`**  
    Содержит параметры, которые можно переопределять для сборки:
    ```
    org.gradle.jvmargs=-Xmx2g
    version=1.0.0
    ```
- **`local.properties`** _(чаще в Android-проектах)_ — хранит локальные пути (например, к SDK), не коммитится в Git.
## 🧾 Итого — сравнение

|Назначение|Maven|Gradle|
|---|---|---|
|Основной файл проекта|`pom.xml`|`build.gradle` / `build.gradle.kts`|
|Файл с глобальными настройками|`settings.xml`|`gradle.properties` / `local.properties`|
|Структура проекта|через `pom.xml` (modules)|`settings.gradle`|
|Формат|XML|Groovy / Kotlin DSL|
|Расширение|через плагины Maven|через плагины и кастомные задачи|
