## 🧩 **Сервлеты vs JSP: ключевые отличия**

| №     | Критерий                       | **Сервлет (Servlet)**                                                     | **JSP (JavaServer Pages)**                                                            |
| ----- | ------------------------------ | ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| **1** | **Назначение**                 | Используется для обработки HTTP-запросов, бизнес-логики, работы с данными | Используется для генерации HTML (представления), т.е. отображения данных пользователю |
| **2** | **Тип кода**                   | Чистый Java-код (класс, наследующий `HttpServlet`)                        | HTML-страница с вкраплениями Java-кода (`<% ... %>`)                                  |
| **3** | **Фокус**                      | Контроллер (логика)                                                       | View (представление)                                                                  |
| **4** | **Кто создает HTML?**          | HTML формируется в Java-коде (через `out.println("<html>...</html>")`)    | HTML — основа, а Java-код встраивается в него                                         |
| **5** | **Компиляция и исполнение**    | Компилируется заранее как `.class`                                        | При первом вызове автоматически компилируется в сервлет                               |
| **6** | **Удобство для верстальщиков** | Низкое — тяжело писать HTML в Java                                        | Высокое — удобно писать HTML, добавляя немного логики                                 |
| **7** | **Поддержка MVC**              | Используется как **Controller** в MVC                                     | Используется как **View** в MVC                                                       |
| **8** | **Файл**                       | `.java` (в пакете `javax.servlet.*`)                                      | `.jsp` (в корне webapp или подкаталоге)                                               |
| **9** | **Производительность**         | Быстрее при многократных вызовах (компилируется один раз)                 | На первом вызове немного медленнее (из-за авто-компиляции в сервлет)                  |
## ⚙️ **Как JSP работает "под капотом"**
> JSP — это просто **синтаксический сахар** над сервлетами.
1. При первом обращении Tomcat **транслирует JSP в Java-класс сервлета**.
2. Затем **компилирует его в `.class`**.
3. Дальше страница работает как обычный сервлет, обрабатывая HTTP-запросы.
📄 Пример:  
`index.jsp` → `org.apache.jsp.index_jsp.java` → `index_jsp.class`
## 🎯 **Когда использовать что**

|Ситуация|Что выбрать|
|---|---|
|Нужна обработка данных, логика, редиректы, работа с БД|**Сервлет**|
|Нужно просто отобразить данные пользователю|**JSP**|
|Архитектура MVC (Model–View–Controller)|Сервлет = Controller, JSP = View|
|Работа с фреймворками (Spring MVC, JSF и др.)|JSP (или шаблонизатор — Thymeleaf, Freemarker)|
## 🧠 **Современная практика**
Сегодня **JSP и чистые сервлеты** применяются редко — в основном для обучения.  
В реальных проектах им на смену пришли фреймворки, которые **автоматизируют их взаимодействие**:
- **Spring MVC** → Сервлеты остаются под капотом, а представления делаются через **Thymeleaf / Freemarker**.
- **Jakarta Faces (JSF)** → Более декларативный подход к построению UI.