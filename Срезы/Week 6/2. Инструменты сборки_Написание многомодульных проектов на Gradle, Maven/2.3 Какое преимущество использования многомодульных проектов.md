## ⚙️ Что такое многомодульный проект
Многомодульный проект — это структура, где один **корневой проект (root)** объединяет несколько **подпроектов (modules)**, каждый из которых можно собирать и тестировать отдельно.
Пример структуры:
```
my-app/
├── pom.xml (или settings.gradle)
├── core/
│   └── pom.xml
├── service/
│   └── pom.xml
└── web/
    └── pom.xml
```
## 🚀 Основные **преимущества**
### 1. **Повторное использование кода (реюзабилити)**
- Общие библиотеки (например, `core` с утилитами, DTO, логикой) можно подключать в другие модули как зависимость:
    ```xml
    <dependency>
        <groupId>com.example</groupId>
        <artifactId>core</artifactId>
        <version>1.0.0</version>
    </dependency>
    ```
- Это предотвращает дублирование кода между сервисами (DRY — _Don’t Repeat Yourself_).
### 2. **Быстрая и централизованная сборка**
- Один вызов команды:
    ```
    mvn clean install
    ```
    или
    ```
    gradle build
    ```
    собирает **все модули** в правильном порядке.
- Maven/Gradle автоматически понимает зависимости между модулями и строит их в нужной последовательности.
### 3. **Изоляция и независимость модулей**
- Каждый модуль можно **собирать, тестировать и версионировать отдельно**.
- Ошибки в одном модуле не ломают весь проект.
- Упрощается CI/CD — можно пересобирать только изменённые части.
### 4. **Разделение ответственности**
- Разные команды могут работать над разными модулями независимо (например, одна — над `api`, другая — над `service`).
- У каждого модуля может быть свой набор зависимостей, тестов и настроек.
### 5. **Чистая архитектура и масштабируемость**
- Структура становится ближе к реальной архитектуре приложения (core, service, web, persistence, etc.).
- Проект легче **масштабировать**, добавляя новые модули вместо разрастания одного гигантского `src`.
### 6. **Единая точка управления зависимостями и версиями**
- В корневом `pom.xml` (или `build.gradle`) можно задать общие зависимости и их версии:
    ```xml
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-starter</artifactId>
                <version>3.3.0</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
    ```
- Все модули будут использовать эти версии — меньше конфликтов и несогласованностей.
### 7. **Упрощённое тестирование и интеграция**
- Можно тестировать каждый модуль отдельно, а потом интеграционно — все вместе.
- Gradle и Maven умеют выполнять сборку инкрементально: если модуль не изменился, он не пересобирается.
## 🧩 Пример из практики
Например, проект интернет-магазина можно разделить так:
```
shop/
├── pom.xml
├── core/           # Бизнес-логика, общие сущности
├── data/           # Работа с базой данных
├── service/        # Взаимодействие с внешними API
├── web/            # REST-контроллеры
└── api/            # DTO и клиенты для других сервисов
```
Это позволяет:
- обновлять `core` без затрагивания `web`;
- переиспользовать `api` в других сервисах;
- тестировать `data` независимо от UI.
## ⚖️ Недостатки (для баланса)
Чтобы понимать картину полностью:
- сложнее настроить сборку при первых шагах;
- возможны зависимости между модулями, которые нужно тщательно управлять;
- если модулей **слишком много**, навигация может стать громоздкой.
✅ **Итого:**  
Многомодульный проект — это способ **организовать код логично, масштабируемо и переиспользуемо**, при этом сохраняя **централизованное управление** зависимостями и сборкой.