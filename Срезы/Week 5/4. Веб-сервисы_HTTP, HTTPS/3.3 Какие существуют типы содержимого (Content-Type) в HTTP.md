В HTTP заголовок **`Content-Type`** указывает, какой тип данных передаётся в теле запроса или ответа. Это помогает клиенту или серверу правильно обработать данные.
### 🔹 Основные категории Content-Type

|Категория|Примеры|Описание|
|---|---|---|
|**Текстовые**|`text/plain`, `text/html`, `text/css`, `text/javascript`|Простой текст, HTML-страницы, CSS, JS|
|**JSON / XML**|`application/json`, `application/xml`, `text/xml`|Структурированные данные, часто используется в API|
|**Формы**|`application/x-www-form-urlencoded`, `multipart/form-data`|Данные HTML-форм; `multipart/form-data` для файлов|
|**Изображения**|`image/png`, `image/jpeg`, `image/gif`, `image/webp`|Картинки разных форматов|
|**Аудио / Видео**|`audio/mpeg`, `video/mp4`|Аудио и видео-файлы|
|**Бинарные данные**|`application/octet-stream`|Общий тип для любых бинарных данных, например, при скачивании файлов|
|**PDF / Документы**|`application/pdf`, `application/msword`, `application/vnd.openxmlformats-officedocument.wordprocessingml.document`|Документы разных форматов|
### 🔹 Примеры использования
1. **JSON API**
```http
POST /api/users HTTP/1.1
Content-Type: application/json

{
  "name": "Alice",
  "email": "alice@mail.com"
}
```
2. **HTML-страница**
```http
HTTP/1.1 200 OK
Content-Type: text/html

<html>...</html>
```
3. **Форма с файлом**
```http
POST /upload HTTP/1.1
Content-Type: multipart/form-data; boundary=----WebKitFormBoundary
```
✅ **Итог:** `Content-Type` позволяет серверу и клиенту **понимать, что за данные передаются**, и корректно их обрабатывать.