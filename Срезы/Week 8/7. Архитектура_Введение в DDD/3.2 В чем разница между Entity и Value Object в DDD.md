В DDD **Entity** и **Value Object** — это два фундаментальных строительных блока предметной области, и они решают разные задачи. Вот ключевые отличия:
# ✅ **Entity (Сущность)**
### **Определение**
Сущность — это объект, который определяется **уникальной идентичностью (ID)**, а не набором полей.
### **Признаки**
- Имеет **уникальный идентификатор**, который сохраняется во времени.
- Дважды созданные объекты с одинаковыми данными — **разные сущности**, если у них разные ID.
- Может изменять своё состояние (мутабельный объект).
- Представляет объект реального мира: пользователь, заказ, транзакция.
### **Пример**
```java
class User {
    private UUID id;          // уникальная идентичность
    private String name;
    private String email;
}
```
# ✅ **Value Object (Значимый объект)**
### **Определение**
Value Object — объект, который определяется **только своим значением**, а не идентичностью.
### **Признаки**
- **Не имеет ID**.
- Является **иммутабельным**.
- Два Value Object’а с одинаковыми значениями считаются **одинаковыми**.
- Может использоваться как часть Entity или как отдельная сущность предметной логики.
- Хорошо подходит для инкапсуляции бизнес-правил.
### **Примеры Value Object'ов**
- Email
- Money
- Address
- Coordinates
- FullName
### **Пример на Java**
```java
public record Money(BigDecimal amount, String currency) {
    public Money {
        if (amount.compareTo(BigDecimal.ZERO) < 0) {
            throw new IllegalArgumentException("Amount must be >= 0");
        }
    }
}
```
# 🆚 Краткая разница Entity vs Value Object

|Характеристика|Entity|Value Object|
|---|---|---|
|**Идентичность (ID)**|Есть|Нет|
|**Сравнение**|По ID|По значению|
|**Изменяемость**|Может быть изменяемой|Иммутабельный|
|**Жизненный цикл**|Отслеживается|Неважен, можно пересоздать|
|**Использование**|Пользователь, заказ, товар|Email, Money, Address|
# 👍 Как это применяется в архитектуре
- Entity обеспечивает **уникальность и долгоживущие объекты**.
- Value Object обеспечивает **чистую логику**, безопасность типов и снижение ошибок.
Например:
```java
class Order {
    private OrderId id;
    private List<OrderItem> items;
    private Money totalPrice; // Value Object
}
```