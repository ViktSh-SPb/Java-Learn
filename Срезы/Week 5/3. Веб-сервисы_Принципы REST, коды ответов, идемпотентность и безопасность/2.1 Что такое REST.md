**REST (Representational State Transfer)** — это архитектурный стиль взаимодействия между клиентом и сервером, основанный на **использовании стандартных протоколов HTTP** и принципах простоты, масштабируемости и независимости компонентов.
### 🔹 Основная идея
В REST **все ресурсы системы (например, пользователи, заказы, товары)** имеют **уникальные URI (адреса)**, а взаимодействие с ними осуществляется с помощью стандартных HTTP-методов:
- `GET` — получение ресурса;
- `POST` — создание ресурса;
- `PUT` / `PATCH` — обновление ресурса;
- `DELETE` — удаление ресурса.
### 🔹 Пример REST-запроса
```http
GET /api/users/5 HTTP/1.1
Host: example.com
Accept: application/json
```
Ответ:
```json
{
  "id": 5,
  "name": "Alice",
  "email": "alice@example.com"
}
```
### 🔹 Основные принципы REST
1. **Клиент–сервер** — клиент и сервер независимы, могут развиваться отдельно.
2. **Отсутствие состояния (stateless)** — каждый запрос самодостаточен и не зависит от предыдущих.
3. **Кэширование** — ответы сервера можно кэшировать для повышения производительности.
4. **Единообразие интерфейса** — одинаковые принципы доступа ко всем ресурсам.
5. **Многоуровневая система** — REST-система может состоять из нескольких уровней (прокси, балансировщики и т.д.).
6. **Передача представлений** — ресурс может быть представлен в разных форматах (JSON, XML, HTML и др.).
### 🔹 Форматы данных
REST не ограничен форматом, но чаще всего используется:
- **JSON** (наиболее популярный);
- **XML**;
- **YAML**, **CSV** — реже.
### 🔹 Преимущества REST
- Простота и читаемость API;
- Высокая производительность;
- Лёгкая интеграция с фронтендом и мобильными приложениями;
- Независимость клиентской и серверной части;
- Масштабируемость.
### 🔹 Недостатки REST
- Нет строгого стандарта безопасности, как в SOAP (нужно реализовывать отдельно, например, с помощью OAuth2 или JWT).
- Не подходит для сложных транзакций и асинхронных операций.
- Иногда избыточен — приходится делать несколько запросов для получения связанных данных (в отличие от GraphQL).