**JConsole** — это графический инструмент, входящий в состав JDK, который помогает **мониторить работу Java-приложений в реальном времени**. Он взаимодействует с JVM через **JMX (Java Management Extensions)**.
# 📊 Возможности JConsole для мониторинга Java-приложений
### 1. **Мониторинг использования памяти**
JConsole позволяет наблюдать:
- Heap Memory (Young / Old Generation)
- Non-Heap Memory (Metaspace)
- Количество сборок мусора
- Время, потраченное GC
Это помогает понять:
- есть ли утечки памяти,
- достаточен ли хип,
- как часто запускается GC.
### 2. **Мониторинг потоков (Threads)**
В реальном времени можно смотреть:
- количество активных потоков,
- состояние каждого потока (RUNNABLE, WAITING, BLOCKED),
- стек вызовов потоков.
Это полезно для:
- диагностики deadlock’ов,
- поиска "подвисших" потоков,
- анализа чрезмерного использования потоков.
### 3. **Работа с MBeans**
Это один из самых полезных разделов JConsole.
Позволяет:
- Читать значения всех JMX-метрик,
- Управлять компонентами приложения (если имеются exposed MBeans),
- Настраивать параметры в реальном времени.
Например, если в приложении есть кастомный MBean, вы можете менять конфигурацию "на лету".
### 4. **Мониторинг CPU и нагрузки на систему**
JConsole показывает:
- загрузку процессора,
- количество операций GC,
- статистику по потокам исполнения.
### 5. **Обнаружение Deadlock’ов**
Вкладка _Threads_ позволяет автоматически обнаруживать взаимные блокировки потоков.
### 6. **Мониторинг Class Loading**
На вкладке _Classes_ видно:
- сколько классов загружено,
- сколько выгружено,
- скорость загрузки классов.
Это помогает выявлять:
- memory leak в ClassLoader,
- проблемы с динамическими загрузками.
# 🚀 Как запустить JConsole
Если приложение запущено локально:
```
jconsole
```
Выбираете свой Java-процесс из списка.
Если подключение удалённое — приложение должно быть запущено с JMX-параметрами:
```
-Dcom.sun.management.jmxremote 
-Dcom.sun.management.jmxremote.port=9010 
-Dcom.sun.management.jmxremote.authenticate=false 
-Dcom.sun.management.jmxremote.ssl=false
```
# 📌 Когда использовать JConsole?
- Нужно быстро посмотреть метрики без тяжелых инструментов.
- Надо проверить GC и память в реальном времени.
- Нужно диагностировать проблемы с потоками (deadlock, high contention).
- Требуется работа с JMX-модулями приложения.