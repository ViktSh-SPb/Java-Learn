В DDD **агрегаты (Aggregates)** — это один из ключевых механизмов управления сложностью доменной модели. Они помогают структурировать предметную область так, чтобы обеспечить **целостность данных**, **предсказуемость поведения** и **ограничение связей между объектами**.
# ✅ Что такое агрегат?
**Агрегат — это группа связанных Entities и Value Objects, которые образуют логическую единицу изменения.**
У агрегата есть:
- **корень агрегата (Aggregate Root)** — главная сущность,
- **внутренние сущности и value objects** — которые изменяются _только через корень_,
- **граница агрегата (Aggregate Boundary)** — определяет, что входит внутрь и что является внешним.
Агрегат — это _консистентная транзакционная единица_.
# 🔑 Основная идея
> **Все инварианты (бизнес-правила о целостности данных) должны быть консистентными внутри одного агрегата.**
Примеры инвариантов:
- сумма заказа не может быть отрицательной,
- остаток товара нельзя превысить,
- бронирование не может пересекаться по времени.
# 🧩 Зачем нужны агрегаты?
## 1. Управление сложностью модели
Без агрегатов модель становится "спагетти": сущности ссылаются друг на друга, циклы, запутанные зависимости, хрупкость.
Агрегаты **разбивают модель на независимые куски**.
## 2. Обеспечение целостности (Invariant Protection)
Агрегат гарантирует, что все бизнес-правила соблюдаются **внутри одной транзакции**.
👉 Пример:
```
Order
 ├─ OrderItem
 ├─ DeliveryAddress
 └─ Price
```
Ты не можешь изменить OrderItem напрямую — только через Order.
## 3. Контроль доступа к данным
Агрегат **инкапсулирует бизнес-логику**:
- внешние сервисы работают **только с корнем агрегата**,
- никто не может модифицировать внутренние сущности напрямую.
Это защищает систему от неконтролируемых изменений.
## 4. Масштабирование и микросервисы
Границы агрегатов помогают провести **границы сервисов**.
Плохой признак:
> Один сервис должен открывать транзакции на сущности другого сервиса.
Хороший:
> Один агрегат = одна транзакция = один сервис.
## 5. Уменьшение количества транзакций
Все консистентные изменения выполняются **атомарно**.
Если требуется распределённая транзакция → это сигнал, что агрегаты определены неправильно.
# 🎯 Что делает Aggregate Root?
Корень агрегата:
- является **единственной точкой входа** для изменений,
- несёт ответственность за соблюдение инвариантов,
- управляет внутренними объектами,
- хранится в репозитории.
Пример:
```java
public class Order {
    private OrderId id;
    private List<OrderItem> items = new ArrayList<>();

    public void addItem(ProductId productId, int quantity) {
        if (quantity <= 0) throw new IllegalArgumentException();
        items.add(new OrderItem(productId, quantity));
        recalculateTotal();
    }
}
```
Внешний мир делает только:
```java
order.addItem(...);
```
Но не:
```java
order.getItems().add(...); // НЕЛЬЗЯ
```
# 🧠 Как агрегаты уменьшают сложность системы?

|Проблема без агрегатов|Решение с агрегатами|
|---|---|
|Объекты связаны ко всем|Чёткие границы|
|Трудно понять, что где изменяется|Только через root|
|Много транзакций|Одна транзакция = один агрегат|
|Инварианты разорваны по коду|Все правила внутри агрегата|
|Легко нарушить данные|Инкапсуляция|
# 📝 Итог
**Роль агрегатов в DDD:**
- Упрощают систему через группировку сущностей.
- Обеспечивают транзакционную целостность.
- Инкапсулируют бизнес-логику.
- Уменьшают зависимость между частями модели.
- Помогают определять границы модулей и микросервисов.