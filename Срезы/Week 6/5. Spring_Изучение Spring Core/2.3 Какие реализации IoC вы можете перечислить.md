В контексте **Spring и Dependency Injection**, **IoC (Inversion of Control)** — это принцип, при котором **контейнер управляет созданием и связыванием объектов**, а не приложение самостоятельно.
Существует несколько **реализаций IoC**, которые отличаются по способу внедрения зависимостей:
## 1️⃣ **Dependency Injection (DI) — внедрение зависимостей**
Самая популярная реализация IoC в современных фреймворках.  
Spring активно использует DI.
### Способы DI:

|Тип внедрения|Описание|Пример в Spring|
|---|---|---|
|**Constructor Injection**|Зависимости передаются через конструктор|`@Autowired` на конструкторе|
|**Setter Injection**|Зависимости передаются через сеттер|`@Autowired` на методе setX()|
|**Field Injection**|Зависимость внедряется прямо в поле|`@Autowired` на поле|

> Constructor Injection рекомендуется как **наиболее безопасный и тестируемый способ**.
## 2️⃣ **Dependency Lookup (DL) — поиск зависимостей**
IoC реализуется через **ручной запрос к контейнеру**, вместо автоматического внедрения.  
Пример в Spring:
```java
@Autowired
private ApplicationContext context;

PaymentService service = context.getBean(PaymentService.class);
```
> В отличие от DI, объект **сам ищет свои зависимости** через контейнер. Используется редко — нарушает принцип слабой связанности.
## 3️⃣ **Service Locator (шаблон «Сервис-локатор»)**
Похож на Dependency Lookup: объект получает зависимость через **централизованный реестр сервисов**.
```java
PaymentService service = ServiceLocator.getService(PaymentService.class);
```
- Преимущество: легко динамически менять реализации.
- Недостаток: нарушает IoC и усложняет тестирование.
> Spring **не рекомендует** активно использовать этот подход, но он возможен.
## 4️⃣ **Event-driven IoC**
IoC через **события**: объекты взаимодействуют через события, а не напрямую.  
Пример в Spring:
```java
@Component
public class EventPublisher {
    @Autowired
    private ApplicationEventPublisher publisher;

    public void publish() {
        publisher.publishEvent(new CustomEvent(this, "data"));
    }
}

@Component
public class EventListener {
    @EventListener
    public void handle(CustomEvent event) {
        System.out.println("Received: " + event.getMessage());
    }
}
```
> Контейнер управляет вызовом обработчиков — класс сам **не вызывает обработчик напрямую**.
## 5️⃣ **Factory/Provider-based IoC**
Контейнер создает объекты через **фабрики** или провайдеры.  
Пример в Spring:
```java
@Bean
public PaymentService paymentService() {
    return new CreditCardPaymentService();
}
```
- Spring сам управляет созданием объекта и его внедрением.
- Позволяет динамически конфигурировать зависимости через Java-код.
## 6️⃣ **Aspect-Oriented IoC (через AOP)**

Хотя это скорее расширение DI, Spring использует AOP, чтобы **контейнер внедрял аспекты в бины**.  
Пример: логирование, транзакции, безопасность.

```java
@Service
public class OrderService {
    @Transactional
    public void placeOrder() { ... }
}
```

- Аннотация `@Transactional` — Spring внедряет **прокси**, который управляет транзакциями.
### 🔑 Итог
Основные реализации IoC:
1. **Dependency Injection (constructor, setter, field)** ✅
2. **Dependency Lookup / Service Locator**
3. **Event-driven IoC**
4. **Factory/Provider-based IoC**
5. **AOP-based IoC**
> Spring использует **DI как основной способ**, но сочетает его с **Factory/Provider** и **AOP**, что делает систему гибкой и расширяемой.