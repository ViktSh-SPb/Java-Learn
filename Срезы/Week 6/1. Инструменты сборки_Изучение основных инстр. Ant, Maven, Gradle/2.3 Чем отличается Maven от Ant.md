## 🧩 Коротко:
> 🔹 **Ant** — это старый инструмент, выполняющий сборку на основе _наборов инструкций (скриптов)_.  
> 🔹 **Maven** — это более современная система, основанная на _концепции проекта и зависимостей_.
## ⚙️ 1. **Подход к сборке**
### 🔸 Ant (Apache Ant)
Ant работает по принципу:
> “Что делать” → ты сам пишешь пошагово **все действия сборки**.
Например, в Ant ты должен явно указать:
- где лежат исходники,
- как их компилировать,
- куда класть .class-файлы,
- как упаковать в .jar.
📄 Пример `build.xml`:
```xml
<project name="MyProject" default="jar">
    <target name="compile">
        <javac srcdir="src" destdir="build/classes"/>
    </target>

    <target name="jar" depends="compile">
        <jar destfile="build/MyProject.jar" basedir="build/classes"/>
    </target>
</project>
```
➡️ Всё прописывается вручную, даже самые базовые шаги.
### 🔸 Maven
Maven работает по принципу:
> “Что собрать” → а _как_ — он уже знает.
Он использует **стандартизированный жизненный цикл** и **структуру проекта**, тебе нужно только описать зависимости и немного метаданных.
📄 Пример `pom.xml`:
```xml
<project>
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.example</groupId>
  <artifactId>myproject</artifactId>
  <version>1.0.0</version>

  <dependencies>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.13.2</version>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
```
➡️ Maven сам знает, где искать код (`src/main/java`), куда компилировать и как упаковывать `.jar`.
## 🧩 2. **Управление зависимостями**
### 🧱 Ant
- Зависимости нужно **скачивать и подключать вручную** (копировать `.jar` в `/lib`).
- Нет встроенного репозитория.
### 🚀 Maven
- Использует **централизованную систему репозиториев** (`mavenCentral` и локальный `.m2`).
- Автоматически загружает и кэширует зависимости по координатам:
    ```
    groupId:artifactId:version
    ```
📦 Пример:
```xml
<dependency>
  <groupId>org.springframework.boot</groupId>
  <artifactId>spring-boot-starter-web</artifactId>
  <version>3.2.0</version>
</dependency>
```
## 🧩 3. **Структура проекта**
### Ant
Ты сам решаешь, как организовать файлы — нет стандартов.  
Можно сделать как угодно, но это затрудняет поддержку и интеграцию.
### Maven
Навязывает стандартную структуру:
```
src/
  main/
    java/
    resources/
  test/
    java/
pom.xml
```
Благодаря этому IDE и CI/CD-системы знают, где искать код, тесты, ресурсы и артефакты.
## 🧩 4. **Жизненный цикл сборки**

|Этап|Ant|Maven|
|---|---|---|
|Определение шагов|Ручное, через `<target>`|Автоматическое (фазы: compile, test, package, install, deploy)|
|Зависимости между шагами|Задаются вручную|Заданы фреймворком|
|Конфигурация|Полностью вручную|Через плагины|
## 🧩 5. **Повторное использование и стандарты**
- Ant → нет встроенного понимания «проекта» или «модуля».  
    Каждый build.xml уникален.
- Maven → всё стандартизировано, можно легко делиться настройками между проектами (через **архетипы**, **parent POM**, **плагины**).
## 📊 Сравнение в таблице

|Критерий|**Ant**|**Maven**|
|---|---|---|
|Подход|Скриптовый|Декларативный|
|Конфигурация|Пошаговые инструкции|Описание проекта|
|Управление зависимостями|Ручное|Автоматическое|
|Структура проекта|Произвольная|Стандартизированная|
|Повторное использование|Сложно|Просто|
|Репозитории|Нет|Есть (Maven Central)|
|Расширяемость|Плагины, но неструктурированные|Плагины, управляемые фазы|
|Обучение|Просто начать, но сложно масштабировать|Сложнее начать, но легче поддерживать|
## ✅ Ключевое отличие
> **Ant — инструмент сборки (build tool).**  
> **Maven — инструмент управления проектом (project management tool).**
Maven не просто компилирует код — он знает всё о **жизненном цикле проекта**, зависимостях, версиях, плагинах и публикации артефактов.