# ✅ Что такое Bounded Context?
**Bounded Context — это чётко ограниченная область модели**, внутри которой:
- используется своё Ubiquitous Language (единый язык),
- своя доменная модель,
- свои правила, инварианты и логика,
- своё понимание терминов.
Главное: **внутри контекста модели точны, за его пределами — недействительны**.
❗ Один и тот же термин может иметь разный смысл в разных контекстах.
# 🔥 Почему Bounded Context — важнейший элемент DDD?
### 1. **Снижает сложность большой модели**
Если попытаться создать одну общую модель для всей бизнес-логики, она станет:
- перегруженной,
- противоречивой,
- трудной в поддержке.
Bounded Context разделяет сложную предметную область на логические части.
### 2. **Устраняет двусмысленность терминов (Ubiquitous Language)**
Один термин в разных частях бизнеса — разные вещи.
**Пример: "Order"**
- в eCommerce — покупка товара,
- в логистике — заказ на доставку,
- в бухгалтерии — документ для списания.
Каждый контекст может иметь своё определение и структуру "Order".  
Без Bounded Context возникает хаос в терминах.
### 3. **Обеспечивает независимость моделей**
Если контексты независимы:
- изменения в одном не ломают другой,
- разные команды могут работать параллельно,
- каждый контекст развивается в своём темпе.
Это критично для масштабируемой архитектуры.
### 4. **Формирует естественные границы микросервисов**
В современных системах чаще всего:
> 1 Bounded Context = 1 микросервис
Контекст задаёт:
- отдельный код,
- отдельную базу данных,
- собственную модель,
- собственные API и события.
Если строить микросервисы “по слоям” или “по таблицам”, вы гарантированно получите tightly coupled архитектуру.
### 5. **Облегчает управление зависимостями**
Модели разных контекстов связаны не напрямую, а через:
- контекстные карты (Context Map),
- события (Domain Events),
- ACL (Anti-Corruption Layer),
- соглашения об интеграции.
Это делает взаимодействие предсказуемым и контролируемым.
# 🎯 Практическое применение Bounded Contexts
## 1. Анализ домена с экспертами
Определяются части бизнеса, в которых:
- другое понимание терминов,
- разные правила,
- разные процессы.
Это основа для разбиения.
## 2. Построение Context Map
Создаётся карта взаимодействия контекстов:
- Upstream / Downstream
- Partnership
- Conformist
- Anti-Corruption Layer
- Published Language
- Shared Kernel
Контекстная карта показывает связи доменных моделей.
## 3. Организация команд
В DDD-компаниях:
> One team — One Bounded Context
Это снижает коммуникационные издержки (закон Конвея).
## 4. Проектирование архитектуры
Bounded Context определяет:
- границы микросервиса,
- отдельную базу данных,
- отдельный жизненный цикл.
Это предотвращает создание "микромонолита" — микросервисов, сильно зависящих друг от друга.
# 🧠 Пример
Допустим, есть система интернет-магазина. Возможные Bounded Context:
- **Catalog** — товары, категории, атрибуты.
- **Ordering** — создание заказов.
- **Inventory** — остатки на складе.
- **Billing** — платежи.
- **Delivery** — доставка.
Термин “Product” в Catalog и “Item” в Inventory — разные концепты.  
DDD фиксирует это разделением контекстов.
# 🔥 Итог
### Контекстные границы (Bounded Contexts) важны, потому что:
- уменьшают сложность системы,
- устраняют двусмысленность терминов,
- создают независимые модели,
- формируют правильные границы микросервисов,
- улучшают масштабируемость,
- позволяют командам работать автономно,
- снижают риски при изменениях,
- создают ясную архитектуру предметной области.