RabbitMQ используется для **асинхронного обмена сообщениями между приложениями или микросервисами**. Проще говоря, это система, которая позволяет отправлять и получать данные без прямой блокирующей связи между сервисами.
## 1️⃣ **Очереди задач (Task Queues / Work Queues)**
- Распределение фоновых задач между несколькими рабочими процессами.
- Позволяет **масштабировать обработку задач** и не перегружать основной сервис.
**Пример:**
- Веб-сервер получает заказ → помещает задачу в очередь `process_order` → несколько воркеров забирают и обрабатывают заказы параллельно.
## 2️⃣ **Интеграция микросервисов**
- RabbitMQ позволяет микросервисам **обмениваться событиями асинхронно**.
- Сервисы не зависят напрямую друг от друга и могут работать независимо.
**Пример:**
- Сервис заказов публикует событие `OrderCreated` → сервис уведомлений подписан на очередь и отправляет email клиенту.
## 3️⃣ **Асинхронная обработка запросов**
- Продюсер не ждёт, пока задача будет обработана — ускоряется отклик пользователя.
- Потребитель обрабатывает задачу в фоне.
**Пример:**
- Загрузка большого файла → задача по конвертации отправляется в очередь → пользователь получает ответ мгновенно.
## 4️⃣ **Событийная обработка (Event-driven architecture)**
- RabbitMQ может рассылать события нескольким подписчикам одновременно.
- Используются **fanout** и **topic exchanges** для маршрутизации сообщений.
**Пример:**
- Событие `UserRegistered` → отправляется в очереди для сервиса email, аналитики и уведомлений одновременно.
## 5️⃣ **Приоритетные и отложенные сообщения**
- Очереди могут поддерживать **приоритет сообщений** или **задержку доставки**.
- Важные сообщения обрабатываются быстрее, а менее важные — позже.
## 6️⃣ **Сценарии с высокой надежностью**
- **Подтверждение получения (ack)** и повторная отправка при сбое позволяют **не терять сообщения**.
- **Mirrored queues** обеспечивают отказоустойчивость на уровне кластера.
### 🔹 Итог
RabbitMQ используется там, где важно:
1. Асинхронное взаимодействие сервисов.
2. Масштабируемая обработка задач.
3. Надёжная доставка сообщений.
4. Возможность маршрутизации и обработки сообщений с различными приоритетами.