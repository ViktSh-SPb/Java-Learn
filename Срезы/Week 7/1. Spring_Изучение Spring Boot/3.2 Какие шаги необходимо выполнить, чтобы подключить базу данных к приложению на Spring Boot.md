# ✅ 1. Добавить необходимый Starter в зависимости
### Для PostgreSQL:
```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-data-jpa</artifactId>
</dependency>

<dependency>
    <groupId>org.postgresql</groupId>
    <artifactId>postgresql</artifactId>
</dependency>
```
### Для MySQL:
```xml
<dependency>
    <groupId>mysql</groupId>
    <artifactId>mysql-connector-j</artifactId>
</dependency>
```
### Для тестов — H2:
```xml
<dependency>
    <groupId>com.h2database</groupId>
    <artifactId>h2</artifactId>
</dependency>
```
# ✅ 2. Настроить параметры соединения в `application.yml` или `application.properties`
### Пример для PostgreSQL (`application.yml`):
```yaml
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mydb
    username: user
    password: pass
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
```
### Популярные значения `ddl-auto`:
- `none` — ничего не меняет в схеме
- `validate` — проверяет схему
- `update` — обновляет таблицы при необходимости
- `create` — создает схему при каждом запуске
- `create-drop` — создает и удаляет при остановке
# ✅ 3. Создать сущности (Entity)
```java
@Entity
public class User {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    private String name;
}
```
# ✅ 4. Создать репозиторий
Spring Data JPA автоматически создаёт реализацию.
```java
public interface UserRepository extends JpaRepository<User, Long> {
}
```
# ✅ 5. Использовать репозиторий в сервисах или контроллерах
```java
@Service
public class UserService {
    private final UserRepository repo;

    public UserService(UserRepository repo) {
        this.repo = repo;
    }

    public User save(User user) {
        return repo.save(user);
    }
}
```
# ✅ 6. Проверить соединение
При запуске приложения Spring Boot:
- создаёт DataSource,
- устанавливает соединение,
- настраивает Hibernate,
- создаёт/обновляет таблицы (если задано ddl-auto),
- кидает ошибку, если невозможно подключиться к БД.
Если всё настроено корректно — увидите логи Hibernate и SQL-запросы.
# 📌 Что делает Spring Boot за вас?
Spring Boot **автоматически**:
- создаёт `DataSource`,
- поднимает EntityManagerFactory,
- подключает Hibernate,
- обрабатывает транзакции,
- ищет Repository-интерфейсы и создаёт реализации,
- управляет соединениями через HikariCP.
