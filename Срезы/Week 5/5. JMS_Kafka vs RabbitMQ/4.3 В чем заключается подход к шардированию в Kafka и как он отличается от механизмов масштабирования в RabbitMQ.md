## 🟠 **Kafka: подход к шардированию**
В Kafka **шардирование — это встроенная часть архитектуры**, реализованная через механизм **партиционирования (partitioning)**.
### 🔹 Что такое партиция (partition)
- **Топик** в Kafka делится на **несколько партиций** — это и есть шардирование данных.
- Каждая партиция — это **упорядоченная, неизменяемая последовательность сообщений**, хранящаяся на диске.
- Сообщения в партиции идентифицируются смещением (`offset`), а не временем или ID.
### 🔹 Как работает шардирование
1. **Продюсер выбирает партицию**
    - При отправке сообщения продюсер указывает ключ (`key`).
    - Kafka использует **хеш-функцию от ключа**, чтобы определить, в какую партицию поместить сообщение.
    - Сообщения с одинаковым ключом → всегда попадают в одну и ту же партицию (гарантия порядка для ключа).
2. **Параллельное потребление**
    - Разные **консумеры** одной группы получают **разные партиции**, что позволяет масштабировать обработку.
    - Kafka **автоматически балансирует** распределение партиций между консумерами (rebalance).
3. **Хранение и отказоустойчивость**
    - Каждая партиция реплицируется на несколько брокеров: есть **лидер** и **реплики (followers)**.
    - Если лидер упал, одна из реплик становится новым лидером.
### 📈 Преимущества такого шардирования
- **Масштабирование по горизонтали** — можно добавлять брокеры и партиции без простоя.
- **Порядок сообщений** сохраняется **в пределах ключа**.
- **Производительность** растёт линейно при увеличении числа партиций и консумеров.
📘 _Итог:_ Kafka изначально спроектирована как **распределённая шардированная лог-система**, где шардирование = партиционирование.
## 🟢 **RabbitMQ: подход к масштабированию (кластеризация и очереди)**
RabbitMQ не использует классическое шардирование, как Kafka, но имеет **механизмы масштабирования через кластеры и очереди**.
### 🔹 Как масштабируется RabbitMQ
1. **Кластеризация (Clustering)**
    - Несколько узлов RabbitMQ объединяются в кластер.
    - Метаданные (об очередях, обменниках, биндингах) синхронизируются между всеми узлами.
    - Однако **сообщения в очередях хранятся только на одном узле** (до версии с queue mirroring или quorum queues).
2. **Queue Mirroring (зеркальные очереди)**
    - Позволяет хранить копии очередей на нескольких узлах для отказоустойчивости.
    - Но: это не масштабирование производительности, а **репликация**.
3. **Shovel и Federation**
    - Для распределения нагрузки можно подключать **несколько брокеров через Federation или Shovel** — по сути, вручную реализуя «шардирование очередей».
    - Это даёт горизонтальное масштабирование, но **требует ручной конфигурации и маршрутизации**.
4. **Multiple Queues**
    - Масштабирование часто достигается **разделением очередей**: например, `queue-1`, `queue-2`, `queue-3`, куда сообщения направляются через routing key.
    - Это аналог шардирования, но **на уровне приложения** или **exchange routing**, а не встроенный механизм, как в Kafka.
### 📉 Ограничения
- RabbitMQ не гарантирует **строгого порядка сообщений** при распределении по очередям.
- Добавление узлов не увеличивает пропускную способность автоматически — нужно **перенастраивать маршрутизацию и обменники**.
## ⚖️ **Сравнение: Kafka vs RabbitMQ**

|Характеристика|**Kafka**|**RabbitMQ**|
|---|---|---|
|Подход к масштабированию|Автоматическое шардирование (partitioning)|Кластеры, federation, несколько очередей|
|Единица масштабирования|Партиция топика|Очередь|
|Механизм распределения|Ключ → партиция (hash-based)|Routing key / exchange rules|
|Гарантия порядка|В пределах партиции|Только в пределах одной очереди|
|Балансировка нагрузки|Автоматическая (rebalance консумеров)|Настраивается вручную (через routing)|
|Репликация|Встроенная (ISR, лидеры/фолловеры)|Mirrored или quorum queues|
|Отказоустойчивость|Автоматическое переключение лидеров|Репликация, но менее прозрачная|
|Replay сообщений|Да (offset control)|Обычно нет|
|Масштабирование чтения|Через consumer groups|Через отдельные очереди и воркеры|
## 🧭 Итого
- **Kafka** — изначально распределённая система, где **шардирование (partitioning)** встроено в архитектуру: автоматическое, управляемое ключами, с упором на **высокую производительность и масштабирование по горизонтали**.
- **RabbitMQ** — классическая брокерная система, где масштабирование реализуется через **множество очередей**, **кластеризацию** или **federation**, и требует **явного проектирования** маршрутизации и балансировки.