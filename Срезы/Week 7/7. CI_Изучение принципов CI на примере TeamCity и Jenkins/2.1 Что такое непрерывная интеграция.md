**Непрерывная интеграция (Continuous Integration, CI)** — это практика в разработке программного обеспечения, при которой **разработчики регулярно объединяют (интегрируют) свои изменения в общий репозиторий**, а каждая интеграция автоматически проверяется с помощью сборки и тестов.
Цель CI — **рано обнаруживать ошибки**, снижать риски интеграции и ускорять выпуск качественного ПО.
## 🔹 Основные принципы CI
1. **Частые интеграции**
    - Разработчики часто (несколько раз в день) сливают код в основную ветку (`main`/`master`).
2. **Автоматическая сборка**
    - После каждого коммита система CI запускает сборку проекта, чтобы проверить, что он компилируется.
3. **Автоматическое тестирование**
    - Запуск unit-тестов, интеграционных тестов, статического анализа кода.
    - Цель — поймать ошибки как можно раньше.
4. **Быстрая обратная связь**
    - Разработчики сразу узнают, если интеграция сломалась, и могут быстро исправить проблему.
5. **Единая кодовая база**
    - Все изменения объединяются в одном репозитории, что снижает конфликты при слиянии.
## 🔹 Примеры инструментов CI
- **Jenkins**
- **GitHub Actions**
- **GitLab CI/CD**
- **CircleCI**
- **Travis CI**
## 🔹 Преимущества CI
1. Быстрое обнаружение ошибок и конфликтов при слиянии.
2. Более стабильная основная ветка (`main`).
3. Ускорение разработки за счёт автоматических сборок и тестов.
4. Основа для **непрерывной доставки (Continuous Delivery)** и **непрерывного деплоя (Continuous Deployment)**.