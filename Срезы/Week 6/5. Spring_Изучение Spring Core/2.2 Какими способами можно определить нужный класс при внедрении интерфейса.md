## 🔍 Основные способы указать нужную реализацию
### ✅ **1. Использовать `@Primary`**
Если вы хотите, чтобы одна реализация использовалась **по умолчанию**,  
пометьте её аннотацией **`@Primary`**.
```java
public interface PaymentService {
    void pay();
}

@Service
@Primary
public class CreditCardPaymentService implements PaymentService {
    public void pay() {
        System.out.println("Оплата картой");
    }
}

@Service
public class PayPalPaymentService implements PaymentService {
    public void pay() {
        System.out.println("Оплата через PayPal");
    }
}

@Service
public class OrderService {

    @Autowired
    private PaymentService paymentService; // внедрится CreditCardPaymentService
}
```
📘 **Spring** выберет `CreditCardPaymentService`, потому что он помечен `@Primary`.
### ✅ **2. Использовать `@Qualifier`**
Если вы хотите **явно указать**, какой bean использовать — примените аннотацию **`@Qualifier`**.
```java
@Service("paypalService")
public class PayPalPaymentService implements PaymentService {
    public void pay() {
        System.out.println("PayPal оплата");
    }
}

@Service("cardService")
public class CreditCardPaymentService implements PaymentService {
    public void pay() {
        System.out.println("Оплата картой");
    }
}

@Service
public class OrderService {

    @Autowired
    @Qualifier("paypalService")
    private PaymentService paymentService; // внедрится PayPalPaymentService
}
```
📘 **`@Qualifier("beanName")`** указывает Spring, какой bean нужно выбрать, если их несколько.
### ✅ **3. Использовать имя параметра конструктора или поля**
Если у вас **несколько реализаций**, но вы внедряете через **конструктор** или **параметр**,  
Spring может использовать **имя параметра** для выбора bean’а.
```java
@Service
public class OrderService {

    private final PaymentService paypalService;

    public OrderService(PaymentService paypalService) {
        this.paypalService = paypalService; // внедрится bean с именем "paypalService"
    }
}
```
📘 Работает, если имя параметра совпадает с именем bean'а в контексте (`@Service("paypalService")`).
### ✅ **4. Внедрение списка или карты (если нужно несколько реализаций)**
Если нужно использовать **все реализации интерфейса**, можно внедрить их **в коллекцию**:
```java
@Service
public class PaymentProcessor {

    @Autowired
    private List<PaymentService> paymentServices;

    public void processAll() {
        paymentServices.forEach(PaymentService::pay);
    }
}
```
📘 Spring соберёт **все** бины, реализующие `PaymentService`, в список.  
Порядок можно управлять аннотацией `@Order`.
### ✅ **5. Конфигурация через Java-класс (`@Configuration`)**
Можно зарегистрировать нужную реализацию вручную через **`@Bean`**:
```java
@Configuration
public class PaymentConfig {

    @Bean
    public PaymentService paymentService() {
        return new PayPalPaymentService();
    }
}
```
📘 При этом имя bean'а будет `paymentService`, и Spring будет использовать его при внедрении.
### ✅ **6. Профили (`@Profile`)**
Если разные реализации нужны **в зависимости от окружения** (dev, test, prod):
```java
public interface DataService { ... }

@Service
@Profile("dev")
public class InMemoryDataService implements DataService { ... }

@Service
@Profile("prod")
public class DatabaseDataService implements DataService { ... }
```
📘 Активный профиль выбирается через:
```properties
spring.profiles.active=prod
```
### ✅ **7. Ручное внедрение через ApplicationContext**
(редко используется, но возможно)
```java
@Autowired
private ApplicationContext context;

public void process() {
    PaymentService service = context.getBean("paypalService", PaymentService.class);
    service.pay();
}
```
📘 Не рекомендуется — нарушает принципы DI (создаёт зависимость от контейнера).
## ⚙️ Резюме

|Способ|Когда использовать|Пример|
|---|---|---|
|`@Primary`|Если есть реализация по умолчанию|Основная реализация|
|`@Qualifier`|Если нужно явно указать конкретный bean|Несколько реализаций|
|Имя параметра|При внедрении через конструктор|По совпадению имени|
|Коллекция (`List`, `Map`)|Если нужны все реализации|Пакетная обработка|
|`@Profile`|Разные окружения (dev/prod/test)|Разные БД, логика|
|Java-конфигурация (`@Bean`)|При явной регистрации бинов|Конфигурирование вручную|
