Кэширование имеет смысл применять, когда оно **существенно ускоряет работу приложения**, **уменьшает нагрузку на внешние ресурсы** и **не приводит к неконсистентности данных**, которую вы не готовы принять.
# ✅ **Когда стоит использовать кэширование**
## **1. Дорогие по времени операции**
Если метод выполняется долго (например, от 100–200 мс и выше), кэширование может дать огромный прирост.
Примеры:
- Сложные SQL-запросы (агрегации, несколько JOIN’ов)
- Вычислительные алгоритмы (расчёт стоимости, статистики)
- Обработка больших файлов или массивов данных
## **2. Часто повторяющиеся однотипные запросы**
Если пользователи/сервисы многократно вызывают один и тот же метод с одинаковыми параметрами — кэширование идеально подходит.
Примеры:
- Получение данных профиля пользователя
- Описание товаров
- Частые справочники (страны, тарифы, лимиты)
## **3. Нагрузка на базу данных слишком высока**
Кэширование уменьшает количество запросов к БД → снижает нагрузку → экономит ресурсы.
Применяется, когда:
- Есть пики нагрузки
- База работает на пределе
- Система масштабируется по чтению, а не по записи
## **4. Доступ к медленным внешним сервисам**
Когда данные берутся из:
- внешнего API,
- микросервиса,
- сторонней системы (CRM, billing),
- legacy-приложения,
— кэш снижает зависимость от задержек сети.
## **5. Данные редко меняются**
Статические или квазистатические данные — идеальные кандидаты для кэша.
Примеры:
- Конфигурации тарифов
- Справочники валют
- Курсы валют (которые обновляются раз в час)
- Списки категорий / ролей / разрешений
## **6. DTO-агрегация или сборка данных из разных источников**
Если метод собирает данные из нескольких сервисов и БД — он может быть дорогим.
Пример:
```text
REST endpoint "product details":
    - данные товара из БД
    - рейтинг из microservice 'rating'
    - остатки со склада
    - промо-акции из Redis
```
Такой запрос тяжёлый → кэширующий слой значительно ускорит.
# ⚠️ Когда кэширование НЕ следует использовать
## ❌ **1. Данные часто меняются и важна абсолютная консистентность**
Если данные меняются каждые секунды — использовать кэш опасно.
Например:
- Баланс денег
- Операции со счетами/транзакции
- Количество товаров на складе в реальном времени
## ❌ **2. Небольшие методы, которые и так работают быстро**
Если метод занимает 1–2 мс и делает простой SELECT — кэш может только ухудшить ситуацию.
## ❌ **3. Операции с большими объёмами данных**
Кэширование массивов по 10–100 МБ:
- перегружает память,
- увеличивает GC-паузы,
- создаёт дедупликацию.
# 🧠 Итог
Использовать кэширование стоит, если:
✔ метод дорогой по времени  
✔ результат часто повторяется  
✔ источник данных медленный  
✔ данные редко обновляются  
✔ критична скорость ответа  
✔ база перегружена
Не стоит использовать, если:
✘ важна абсолютная консистентность  
✘ операция слишком простая  
✘ объём данных слишком большой