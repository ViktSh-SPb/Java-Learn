## 1️⃣ Определение
**Система сборки (Build System)** — это инструмент или набор инструментов, который автоматизирует процесс превращения исходного кода в готовый продукт (приложение, библиотеку, артефакт).
**Основная цель:**
> Автоматизировать компиляцию, связывание, упаковку, тестирование и развертывание, чтобы сократить ошибки и ускорить разработку.
## 2️⃣ Основные функции систем сборки
1. **Компиляция кода**
    - Преобразование исходников (например, `.java`, `.cpp`, `.ts`) в исполняемые или промежуточные артефакты (`.class`, `.jar`, `.exe`).
2. **Управление зависимостями**
    - Автоматическая загрузка и подключение внешних библиотек (например, через Maven Central или npm Registry).
3. **Запуск тестов**
    - Unit, интеграционные или функциональные тесты, чтобы гарантировать корректность кода.
4. **Упаковка артефактов**
    - Создание архивов (`.jar`, `.war`, `.zip`) или установочных пакетов.
5. **Автоматизация повторяющихся задач**
    - Генерация документации, проверка стиля кода, копирование ресурсов.
6. **Инкрементальная сборка** (опционально)
    - Пересборка только изменённых частей проекта для ускорения.
## 3️⃣ Типы систем сборки
### 🔹 1. Системы на основе скриптов
- Простейшие: Make, Ant.
- Программист описывает **правила и зависимости вручную**.
- Пример Makefile:
```makefile
all: main.o utils.o
    gcc -o app main.o utils.o

main.o: main.c
    gcc -c main.c
```
### 🔹 2. Декларативные системы
- Gradle, Maven, Bazel.
- Вы описываешь **что и от чего зависит**, а система сама строит граф зависимостей и выполняет задачи.
- Пример Maven POM:
```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-web</artifactId>
    <version>3.3.0</version>
</dependency>
```
### 🔹 3. Языко-специфические менеджеры
- npm (JavaScript), pip (Python), Cargo (Rust) — объединяют сборку и управление зависимостями.
## 4️⃣ Примеры популярных систем сборки

|Язык / Экосистема|Система сборки|
|---|---|
|Java|Maven, Gradle, Ant|
|C/C++|Make, CMake, Ninja|
|Python|setuptools, poetry|
|JavaScript/TypeScript|npm, Yarn, webpack|
|Rust|Cargo|
|Go|go build / go mod|
## 5️⃣ Преимущества использования системы сборки
1. **Автоматизация** — меньше ручной работы, меньше ошибок.
2. **Повторяемость** — сборка всегда одинакова на разных машинах.
3. **Масштабируемость** — удобно управлять большими проектами с модулями.
4. **Интеграция с CI/CD** — автоматическая сборка и тестирование в пайплайнах.
5. **Управление зависимостями** — лёгкое подключение сторонних библиотек.
## 6️⃣ Ключевые концепции
- **Задача (Task)** — атомарная операция (компиляция, тест, упаковка).
- **Артефакт (Artifact)** — результат сборки (JAR, EXE, ZIP).
- **Граф зависимостей** — система понимает, какие задачи нужно выполнять в каком порядке.
- **Инкрементальная сборка** — пересобираются только изменённые части.
### 📘 Вывод
> Система сборки — это инструмент для **автоматизации всего процесса создания, тестирования и упаковки программного продукта**.  
> Она экономит время, снижает риск ошибок и позволяет эффективно работать в больших проектах с множеством зависимостей.