Docker предоставляет несколько удобных способов организовать коммуникацию между контейнерами на одном хосте. Вот самые важные механизмы.
# 🔹 1. **Docker network — основной способ связи контейнеров**
Это наиболее распространённый и рекомендуемый подход.
### 🟦 Создание собственной сети
```bash
docker network create my-net
```
### 🟦 Запуск контейнеров в одной сети
```bash
docker run -d --name app --network my-net my-app-image
docker run -d --name db  --network my-net postgres
```
### 🟦 Связь по имени контейнера
Контейнер **может обращаться к другому по его имени**, как по DNS:
```
jdbc:postgresql://db:5432/mydb
```
Потому что Docker встроенно поднимает **DNS-сервер** внутри одной overlay/bridge сети.
✔️ Работает "из коробки"  
✔️ Не нужны IP-адреса  
✔️ Изолировано от других контейнеров
# 🔹 2. **Использование Docker Compose (рекомендуется)**
Compose автоматически создаёт сеть и связывает контейнеры.
### 📌 Пример:
```yaml
services:
  app:
    image: my-app
  db:
    image: postgres
```
Все контейнеры в compose-файле автоматически попадают в сеть с DNS:
```
app → db
db → app (если нужно)
```
❗ Никаких дополнительных настроек не требуется.
# 🔹 3. **Legacy: флаг `--link` (устаревший)**
```bash
docker run --link db:db app
```
⚠️ Сейчас не рекомендуется.  
Не поддерживается в Docker Swarm.  
Заменён сетями.
# 🔹 4. **Общий volume (для обмена файлами, НЕ для общения по сети)**
```bash
docker run -v shared:/data app
docker run -v shared:/data worker
```
Но:  
❗ Тут **нет сетевой коммуникации**, только обмен файлами.
# 🔹 5. **Использование host network (опасно и редко нужно)**
Контейнеры используют сеть хоста напрямую.
```bash
docker run --network host my-app
```
Используется для:
- высокопроизводительных сетевых приложений,
- бенчмарков.
Минусы:
- Полное отсутствие сетевой изоляции.
- Порты могут конфликтовать.
# 🔹 6. **IP-адреса в одной сети (ручной способ)**
Каждый контейнер получает IP внутри своей docker-сети:
```bash
docker inspect container-name
```
Но обращаться по IP не рекомендуется:
- при перезапуске IP может измениться,
- имя контейнера надёжнее.
# 🧩 Итог

|Способ|Для чего подходит|Современность|
|---|---|---|
|**Docker network**|основной способ общения по сети|✔ современный|
|**Docker Compose**|микросервисы, локальная разработка|✔ современный|
|`--link`|старые проекты|❌ устарело|
|Volume|обмен файлами|✔ но не сеть|
|Host network|максимальная производительность|⚠️ опасно|
|IP напрямую|только когда очень надо|⚠️ нежелательно|
