**Механизм перенаправления (редиректа) в HTTP** позволяет серверу указать клиенту, что запрашиваемый ресурс **находится по другому адресу**, и клиент должен автоматически сделать новый запрос по этому адресу.
### 🔹 Как это работает
1. **Клиент делает запрос**  
    Например:
    ```
    GET /old-page HTTP/1.1
    Host: example.com
    ```
2. **Сервер отвечает статусом перенаправления**
    - Статус-код `3xx` (редко используется 300, чаще 301 или 302).
    - В заголовке `Location` указывается новый URL:
    ```
    HTTP/1.1 301 Moved Permanently
    Location: /new-page
    ```
3. **Клиент автоматически делает новый запрос**
    - Браузер или HTTP-клиент обращается к `Location`.
    - Если это браузер, пользователь видит новый URL в адресной строке.
### 🔹 Основные типы редиректов

|Код|Назначение|Особенности|
|---|---|---|
|301|Moved Permanently|Постоянное перенаправление, браузеры и поисковые системы обновляют ссылку|
|302|Found (или Temporary Redirect)|Временное перенаправление, оригинальный URL остаётся актуальным|
|303|See Other|Перенаправление после POST-запроса на GET-запрос|
|307|Temporary Redirect|Временный редирект, метод запроса сохраняется (POST → POST)|
|308|Permanent Redirect|Постоянный редирект, метод запроса сохраняется|
### 🔹 Пример последовательности
1. Клиент запрашивает `/old-page`.
2. Сервер возвращает `301 Location: /new-page`.
3. Клиент автоматически делает запрос `/new-page`.
4. Сервер возвращает `200 OK` и данные ресурса.
✅ **Итог:** редирект позволяет **сохранять работоспособность ссылок и корректно перенаправлять пользователей и клиентов** без ручного вмешательства.