## 1️⃣ Kafka: устойчивость к сбоям
Kafka — это распределённая система с **репликацией и партициями**.
### Основные механизмы:

| Механизм                    | Как работает                                                                                                                                                                                                                   |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Репликация партиций**     | Каждая партиция топика может иметь несколько реплик (`replication factor`). Одна реплика — **лидер (leader)**, остальные — **followers**. Лидер обслуживает все чтения и записи.                                               |
| **Лидеры и фолловеры**      | Если лидер падает, один из фолловеров автоматически становится лидером.                                                                                                                                                        |
| **Acknowledgements (acks)** | Продюсер может выбрать уровень подтверждения:<br>- `acks=0` — не ждёт подтверждения (неустойчиво)<br>- `acks=1` — ждёт подтверждения от лидера<br>- `acks=all` — ждёт подтверждения от всех реплик (максимальная устойчивость) |
| **Consumer offsets**        | Хранение смещений (offsets) позволяет потребителям читать с последней подтверждённой позиции после сбоя.                                                                                                                       |
| **Durable storage**         | Все сообщения записываются на диск (лог), можно настраивать retention policy.                                                                                                                                                  |
| **ISR (in-sync replicas)**  | Только реплики, синхронизированные с лидером, считаются безопасными; Kafka не подтверждает запись до репликации в ISR.                                                                                                         |
### 💡 Вывод по Kafka
- Устойчивость достигается через **репликацию, подтверждения записей и хранение логов на диске**.
- Сбои отдельных брокеров не приводят к потере данных, если есть достаточное количество реплик и корректные настройки acks.
## 2️⃣ RabbitMQ: устойчивость к сбоям
RabbitMQ ориентирован на **очереди и брокеров**, и устойчивость достигается другими механизмами.
### Основные механизмы:

|Механизм|Как работает|
|---|---|
|**Durable queues**|Очередь создаётся с флагом `durable=true`, чтобы survive перезапуск брокера.|
|**Persistent messages**|Сообщения можно помечать как `persistent`, чтобы они сохранялись на диск.|
|**Publisher confirms**|Продюсер может ждать подтверждения, что сообщение успешно записано в очередь.|
|**Mirrored queues / Classic queue replication**|Очередь может быть зеркалирована на несколько узлов кластера. Все операции синхронизируются между узлами.|
|**Quorum queues**|Современный механизм репликации очередей (оптимизирован для отказоустойчивости), работает как Raft-кластер.|
|**ACK от потребителя**|Consumer может подтверждать обработку сообщения (`ack`); непризнанные сообщения будут перенаправлены другим consumer-ам при сбое.|

### 💡 Вывод по RabbitMQ
- Устойчивость достигается через **долговечные очереди, зеркалирование и подтверждения сообщений**.
- В отличие от Kafka, **RabbitMQ ориентирован на delivery guarantee на уровне очереди**, а не на длительное хранение всех сообщений.
## 3️⃣ Сравнение Kafka vs RabbitMQ по устойчивости

|Особенность|Kafka|RabbitMQ|
|---|---|---|
|Хранение сообщений|Лог на диске, configurable retention|Очереди на диске (persistent)|
|Репликация|Репликация партиций (ISR)|Mirrored queues / Quorum queues|
|Потери сообщений|Минимальные при `acks=all` и репликах|Минимальные при durable queue + persistent messages + publisher confirm|
|Перезапуск брокера|Без потери данных|Без потери данных для durable queues|
|Отказ потребителя|Consumer может продолжить с последнего offset|Сообщения остаются в очереди до ack, перенаправляются другому consumer|
|Масштабирование|Партиции, кластеры|Очереди распределяются по нодам, кластеризация|
### 4️⃣ Основная разница
- **Kafka** — ориентирована на **хранение больших потоков данных** и масштабируемое распределённое хранение с репликацией.
- **RabbitMQ** — ориентирован на **доставку сообщений** и контроль подтверждений, устойчивость достигается через durable очереди и зеркалирование.