Kubernetes обеспечивает **высокую доступность (High Availability, HA)** и **устойчивость приложений (resiliency)** за счёт автоматизации управления контейнерами, распределения нагрузки и самовосстановления.
## 1️⃣ **Самовосстановление Pod’ов**
- Если Pod падает или контейнер внутри него завершается с ошибкой, **Kubernetes автоматически перезапускает Pod** через контроллеры (Deployment, ReplicaSet, StatefulSet).
- Контроллеры следят за количеством реплик и корректируют состояние:
```text
desired replicas = 3
current replicas = 2  → Kubernetes создаёт новый Pod
```
## 2️⃣ **ReplicaSet и Deployment**
- **ReplicaSet** поддерживает заданное количество идентичных Pod’ов.
- **Deployment** управляет обновлениями и откатами.
- Пример: Deployment с 5 репликами подов обеспечивает, что даже при падении двух подов, остальные работают и новые создаются автоматически.
## 3️⃣ **Масштабирование подов и узлов**
- **Horizontal Pod Autoscaler (HPA)** увеличивает количество подов при высокой нагрузке.
- **Cluster Autoscaler** добавляет новые узлы в кластер, если ресурсов на текущих узлах не хватает.
- Вместе это обеспечивает **устойчивость под нагрузкой** и предотвращает простои.
## 4️⃣ **Балансировка нагрузки (Service)**
- **Service** распределяет трафик между работающими Pod’ами.
- Если один Pod падает, трафик автоматически перенаправляется на доступные Pod’ы, что поддерживает доступность приложения.
## 5️⃣ **Сетевое разделение и Health Checks**
- **Liveness Probe**: проверяет, жив ли контейнер, и перезапускает его при необходимости.
- **Readiness Probe**: проверяет, готов ли контейнер обрабатывать запросы. Pod не будет включён в Service до прохождения проверки.
- Это предотвращает отправку трафика на “мертвые” или неготовые контейнеры.
## 6️⃣ **Резервное хранение данных**
- **PersistentVolume (PV) + PersistentVolumeClaim (PVC)** сохраняют данные вне жизненного цикла Pod’а.
- При перезапуске Pod’а или перемещении на другой узел данные остаются доступными.
## 7️⃣ **Разделение компонентов кластера (Control Plane HA)**
- Контролирующая плоскость (API Server, Scheduler, Controller Manager, etcd) может быть развернута с **несколькими репликами**, чтобы кластер продолжал работать даже при падении одного узла управления.
## 8️⃣ **Rolling Updates и откаты**
- Kubernetes обновляет приложения **без простоя** с помощью rolling updates.
- В случае проблем можно быстро сделать **rollback** на предыдущую стабильную версию.
## 🔹 Итог
Kubernetes обеспечивает высокую доступность и устойчивость через:
1. Автоматическое восстановление Pod’ов
2. Поддержку заданного количества реплик (ReplicaSet, Deployment)
3. Балансировку нагрузки (Service)
4. Масштабирование подов и узлов (HPA + Cluster Autoscaler)
5. Health checks (Liveness/Readiness Probes)
6. Постоянное хранилище данных (PV/PVC)
7. Отказоустойчивую Control Plane архитектуру
8. Rolling updates и откаты