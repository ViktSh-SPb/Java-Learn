# ✅ **Как настроить параметризованные сборки в Jenkins**
Параметризованная сборка позволяет запускать job с параметрами, например:
- **target environment**: `dev`, `test`, `stage`, `prod`
- **feature flags**
- **branch name**
- **версия артефакта**
- значения переменных для деплоя и т. д.
Это удобно, когда одна и та же job должна работать по-разному в разных средах.
# 🔧 **1. Включение параметризованной сборки**
1. Откройте нужный **Job (Pipeline/Freestyle)**.
2. Нажмите **Configure**.
3. Включите опцию:
```
This project is parameterized
```
4. Добавьте параметры, например:
### Типы параметров:
- **String Parameter** — текстовое значение
- **Choice Parameter** — список вариантов
- **Boolean Parameter** — true/false
- **Credentials Parameter** — учетные данные
- **File Parameter** — загрузка файла
# 🌍 **2. Пример параметра для выбора окружения**
Добавьте **Choice Parameter**:
- Name: `ENV`
- Choices:
    ```
    dev
    test
    stage
    prod
    ```
- Description: `Target environment for deployment`
Теперь при запуске сборки Jenkins предложит выбрать окружение.
# 🧪 **3. Использование параметров в Freestyle job**
Если у вас **Freestyle**, то параметры доступны как обычные переменные окружения.
Например, в Build Step (Execute shell):
```bash
echo "Deploying to environment: $ENV"

if [ "$ENV" = "prod" ]; then
  echo "Running production deployment"
  ./deploy.sh --prod
else
  echo "Running deployment to $ENV"
  ./deploy.sh --env=$ENV
fi
```
# 📜 **4. Использование параметров в Pipeline (Jenkinsfile)**
### Пример: Declarative Pipeline
```groovy
pipeline {
    agent any

    parameters {
        choice(name: 'ENV', choices: ['dev', 'test', 'stage', 'prod'], description: 'Target environment')
        string(name: 'VERSION', defaultValue: 'latest', description: 'Artifact version')
    }

    stages {
        stage('Build') {
            steps {
                echo "Building version ${params.VERSION} for ${params.ENV}"
                sh "mvn clean package"
            }
        }

        stage('Deploy') {
            steps {
                sh """
                    echo "Deploying version ${params.VERSION} to ${params.ENV}"
                    ./deploy.sh --env=${params.ENV} --version=${params.VERSION}
                """
            }
        }
    }
}
```
# 🧰 **5. Автозапуск параметризованной сборки**
Параметры сохраняются, и job может быть запущена:
- вручную через UI
- автоматом через Webhook
- через Jenkins API, например:
```bash
curl -X POST \
  "http://jenkins/job/my-job/buildWithParameters?ENV=test&VERSION=1.2.3"
```
# 🔒 **6. Использование параметров для секретов**
Добавляется параметр:
```
Credentials Parameter
```
В Pipeline:
```groovy
withCredentials([string(credentialsId: 'prod-token', variable: 'TOKEN')]) {
    sh "deploy --token=$TOKEN"
}
```
# 🧩 **7. Пример использования параметров для нескольких стадий**
```groovy
pipeline {
  agent any

  parameters {
    choice(name: 'ACTION', choices: ['build', 'test', 'deploy'], description: 'Action to perform')
    choice(name: 'ENV', choices: ['dev', 'test', 'prod'], description: 'Environment')
  }

  stages {
    stage('Execute Action') {
      steps {
        script {
          if (params.ACTION == 'build') {
            sh "mvn clean package"
          } else if (params.ACTION == 'test') {
            sh "mvn test"
          } else if (params.ACTION == 'deploy') {
            sh "./deploy.sh --env=${params.ENV}"
          }
        }
      }
    }
  }
}
```
# 🎯 Итог
**Параметризованные сборки Jenkins** позволяют:
- запускать одну и ту же job на разных окружениях
- хранить конфигурацию в Jenkinsfile
- передавать параметры в shell, PowerShell, Groovy
- интегрироваться с Git webhooks или API
- гибко управлять deploy-процессами