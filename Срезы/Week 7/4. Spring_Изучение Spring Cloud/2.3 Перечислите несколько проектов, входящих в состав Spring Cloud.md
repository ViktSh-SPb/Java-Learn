# 🧩 Основные проекты Spring Cloud
## 🔷 1. **Spring Cloud Netflix**
Набор интеграций с Netflix OSS (часть уже устарела):
- **Eureka** — Service Discovery
- **Ribbon** — клиентская балансировка (deprecated → заменён на Spring Cloud LoadBalancer)
- **Hystrix** — circuit breaker (deprecated → заменён на Resilience4j)
- **Zuul** — API Gateway (deprecated → заменён на Spring Cloud Gateway)
## 🔷 2. **Spring Cloud Config**
- Централизованное хранение конфигураций
- Config Server / Config Client
- Поддержка Git, Vault, файловой системы
## 🔷 3. **Spring Cloud Gateway**
Современный API Gateway:
- маршрутизация
- фильтры
- JWT
- rate limiting
- интеграция с discovery
## 🔷 4. **Spring Cloud LoadBalancer**
Замена Ribbon:
- балансировка запросов между экземплярами сервисов
- интеграция с discovery (Eureka, Consul, Kubernetes)
## 🔷 5. **Spring Cloud OpenFeign**
Декларативный HTTP-клиент:
```java
@FeignClient("user-service")
interface UserClient {
    @GetMapping("/users/{id}")
    UserDto get(@PathVariable Long id);
}
```
## 🔷 6. **Spring Cloud Sleuth (Micrometer Tracing)**
Сквозная трассировка запросов:
- traceId / spanId
- отправка трейсов в Zipkin или Jaeger
(Сейчас заменён на **Micrometer Tracing**)
## 🔷 7. **Spring Cloud Stream**
Универсальный API поверх брокеров:
- Kafka
- RabbitMQ
- AWS SNS/SQS
- Google Pub/Sub
Даёт единый programming model для event-driven систем.
## 🔷 8. **Spring Cloud Bus**
Рассылка событий между сервисами:
- обновление конфигурации
- инвалидация кэшей
- кластерные команды
Работает поверх Kafka или RabbitMQ.
## 🔷 9. **Spring Cloud Security**
Интеграция OAuth2 во всей микросервисной системе:
- token relay
- security autoconfig
- взаимодействие между сервисами
## 🔷 10. **Spring Cloud Consul / Zookeeper**
Интеграции с альтернативными сервис-реестрами:
- **Consul Discovery + Config**
- **Zookeeper Discovery**
## 🔷 11. **Spring Cloud Kubernetes**
Адаптация Spring Boot приложений к Kubernetes:
- discovery через kube-dns
- config через ConfigMap / Secret
- service account интеграция
- leader election
## 🔷 12. **Spring Cloud AWS / Azure / GCP**
Готовые интеграции с облаками:
- AWS S3, SNS, SQS, DynamoDB
- Azure Event Hub, CosmosDB
- GCP Pub/Sub, Storage
# 📌 Краткий список «топ» проектов
Если нужны только самые ключевые — вот они:
- Spring Cloud Config
- Spring Cloud Gateway
- Spring Cloud LoadBalancer
- Spring Cloud OpenFeign
- Spring Cloud Stream
- Spring Cloud Bus
- Spring Cloud Netflix (Eureka)
- Spring Cloud Consul
- Spring Cloud Kubernetes
- Micrometer Tracing (бывший Sleuth)