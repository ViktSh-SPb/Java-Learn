## ⚙️ Что такое _Incremental Build_?
**Incremental Build (инкрементальная сборка)** — это подход, при котором инструмент сборки **выполняет только те задачи, которые действительно требуют пересборки**, а не всё подряд.
То есть:
> «Если код, конфигурация или входные файлы не изменились — повторно их не компилируем, не тестируем и не пакуем».
## 🧩 Как работает Incremental Build в **Maven**
### 🔹 Основная идея Maven:
Maven — инструмент **на основе фаз и целей (goals)**, который исполняет их **в строгом порядке** (compile → test → package → install и т.д.).
### 🔹 Что делает Maven при сборке:
- Maven **не анализирует** изменения на уровне отдельных файлов или задач.
- При вызове `mvn clean install` он **всегда пересобирает всё**:
    - все исходники → пересобираются;
    - все тесты → перезапускаются;
    - все JAR’ы → пересоздаются.
Даже если ты изменил всего один `.java` файл, Maven пересоберёт **весь модуль**, потому что у него нет механизма кэширования промежуточных результатов.
### 🔹 Почему так:
- Maven работает по **декларативной модели (POM)** и не хранит зависимость между «входом» и «выходом» задач;
- Он не ведёт кэш вычислений (build cache) и не анализирует, что именно изменилось.

### 🔹 Итог:
📉 **Инкрементальность у Maven отсутствует по сути.**  
Если не использовать сторонние плагины (например, `maven-incremental-build-plugin` или IDE-кэш IntelliJ), сборка всегда полная.
## 🧩 Как работает Incremental Build в **Gradle**
### 🔹 Основная идея Gradle:
Gradle — это **ориентированный на задачи (task-based)** инструмент, у которого каждая задача описывает:
- свои **входные данные (inputs)**;
- свои **выходные результаты (outputs)**.
Например:
```groovy
tasks.register("compileJava") {
    inputs.files("src/main/java")
    outputs.dir("build/classes/java/main")
}
```
Gradle сохраняет в `.gradle` кэше хэш-сигнатуры файлов (inputs/outputs).  
И при следующем запуске он сравнивает:
- Если **входы и выходы не изменились**, задачу можно **пропустить**.
### 🔹 Принцип работы:
1. При первом запуске Gradle **строит "граф задач"** (Task Graph);
2. Для каждой задачи фиксирует **inputs/outputs**;
3. При повторной сборке сравнивает хэши:
    - Изменились исходники → задача выполняется;
    - Не изменились → задача **пропускается** («UP-TO-DATE»).
📄 Пример вывода Gradle:
```
> Task :compileJava UP-TO-DATE
> Task :processResources UP-TO-DATE
> Task :jar UP-TO-DATE
> Task :assemble UP-TO-DATE
```
### 🔹 Дополнительно: _Gradle Build Cache_ и _Configuration Cache_
Gradle идёт дальше и поддерживает:
1. **Local Build Cache** — хранит результаты задач локально (`~/.gradle/caches/build-cache`).
2. **Remote Build Cache** — общий кэш между машинами CI.
3. **Configuration Cache** — кэширует не только результаты задач, но и сам процесс конфигурации проекта (ускоряет первые секунды сборки).
Это делает возможной **практически мгновенную пересборку** при отсутствии изменений.
## 🚀 Сравнение Maven и Gradle по Incremental Build

|Характеристика|**Maven**|**Gradle**|
|---|---|---|
|Модель сборки|Фазовая (phase-goal)|Задачная (task graph)|
|Отслеживание изменений|Нет встроенного|Есть встроенное (inputs/outputs)|
|Кэширование результатов|Нет|Есть (local + remote build cache)|
|Конфигурация инкрементальности|Только через плагины или IDE|Встроено по умолчанию|
|Пропуск неизменённых задач|Нет|Да (UP-TO-DATE)|
|Конфигурационный кэш|Нет|Да (с версии 6.5+)|
|Производительность при частых пересборках|Средняя / низкая|Очень высокая|
## 💡 Пример реальной разницы
Предположим, у тебя проект из 50 модулей, и ты изменил **один файл** в одном модуле.
- **Maven**: пересоберёт весь модуль и, возможно, зависимые модули → 2–3 минуты.
- **Gradle**: пересоберёт только нужные `.class` и JAR → 5–10 секунд.
Разница становится колоссальной на CI/CD — Gradle может сократить общее время сборки **в 2–10 раз**.
## 📘 Краткий вывод

|Критерий|Maven|Gradle|
|---|---|---|
|Инкрементальная сборка|🚫 Нет (по умолчанию)|✅ Да (inputs/outputs)|
|Кэширование задач|🚫 Нет|✅ Local/Remote Build Cache|
|Анализ изменений|🚫 Нет|✅ По хэшам входных файлов|
|Производительность при повторных сборках|❌ Медленная|⚡ Очень быстрая|
|CI/CD интеграция с кэшами|Ограниченная|Отличная (Gradle Enterprise, Build Cache Server)|
✅ **Итог:**
> Gradle использует модель задач с описанными зависимостями и кэшированием результатов,  
> что делает его _по-настоящему инкрементальным_.  
> Maven же в большинстве случаев выполняет _полную пересборку_, что снижает производительность.