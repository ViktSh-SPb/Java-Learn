# 🎯 **Две части выбора в теореме PACELC**
## 🟡 **1. Часть “PA” — когда происходит разделение сети (Partition, P)**
Формулировка:
### **P → A или C**
Если в системе произошло **разделение сети**, то необходимо выбрать:
- **A (Availability)** — доступность  
    Система продолжает обслуживать запросы, даже если узлы не могут синхронизироваться.  
    → Это уменьшает согласованность.
- **C (Consistency)** — согласованность  
    Система блокирует часть запросов, чтобы сохранить единое состояние данных.  
    → Это снижает доступность.
Это **полностью совпадает с CAP**, и PACELC лишь уточняет и повторяет это правило.
## 🔵 **2. Часть “ELC” — когда разделения сети нет (Else)**
Формулировка:
### **Else → L или C**
Когда **разделения сети нет**, то есть система работает штатно, вам нужно выбрать между:
- **L (Latency)** — низкой задержкой  
    Система работает быстро, но может ослаблять строгую согласованность, чтобы ускорить ответы.
- **C (Consistency)** — строгой согласованностью  
    Все узлы синхронизированы перед ответом.  
    → Это увеличивает задержки.
Это — то, чего не хватает в теореме CAP.  
PACELC показывает, что **даже без сбоев** системы вынуждены балансировать между задержками и согласованностью.
# 🧠 Итог (коротко)
В PACELC два компромисса:
### **1. При Partition (P): выбор между A и C**
— как в CAP.
### **2. При отсутствии Partition (Else): выбор между L и C**
— низкая задержка или строгая согласованность.