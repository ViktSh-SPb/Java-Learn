**Spring Security** — это мощный и гибкий фреймворк для **защиты Java и Spring-приложений**.  
Он предоставляет механизмы **аутентификации**, **авторизации**, **управления сессиями**, **шифрования**, **фильтрации запросов** и многое другое.
# 🛡️ Зачем нужен Spring Security?
Spring Security используется, чтобы:
### ✔ 1. **Аутентифицировать пользователя**
Проверить _кто_ делает запрос.  
Например: логин/пароль, JWT-токен, OAuth2, SSO, LDAP и т.д.
### ✔ 2. **Авторизовать пользователя**
Проверить _что ему разрешено делать_.  
Например: роль ADMIN может удалять элементы, USER — нет.
### ✔ 3. **Защитить приложение от атак**, таких как:
- CSRF
- XSS
- Clickjacking
- Brute-force
- Session hijacking
### ✔ 4. **Защитить REST API через токены (JWT)**
# 🔧 Что делает Spring Security в приложении?
## 1. Перехватывает **каждый HTTP-запрос**
Все запросы проходят через цепочку фильтров (security filter chain).
## 2. Проводит аутентификацию
Проверяет токены, cookies, session, Basic/Digest auth и т.д.
## 3. Проверяет права пользователя
Если пользователь не имеет доступа — отдаёт 403 Forbidden.
## 4. Управляет сессиями и logout
Контролирует, сколько сессий открыто, когда их завершать и т.д.
## 5. Применяет политики безопасности
Например:
```java
http.csrf().disable();
http.authorizeHttpRequests(auth -> auth
        .requestMatchers("/admin/**").hasRole("ADMIN")
        .anyRequest().authenticated()
);
```
# 💡 Где используется Spring Security?
- **REST API** (JWT, OAuth2)
- **Web MVC приложения**
- **Корпоративные системы**
- **Микросервисы**
- **Интеграции с внешними провайдерами**:
    - Google
    - Keycloak
    - GitHub
    - Okta
    - Azure AD
    - LDAP/Active Directory
# 📦 Что Spring Security предоставляет "из коробки"?
- Авторизация на основе ролей/прав
- Формы логина и logout
- Basic и Bearer authentication
- OAuth2 login
- Защита от CSRF
- Защита от clickjacking (`X-Frame-Options`)
- Ограничение числа сессий
- Шифрование паролей (`BCryptPasswordEncoder`)
# 🧠 Важно понимать
Spring Security — это **не только логин/пароль**.  
Это **полноценный каркас безопасности**, который полностью контролирует доступ к вашему приложению.