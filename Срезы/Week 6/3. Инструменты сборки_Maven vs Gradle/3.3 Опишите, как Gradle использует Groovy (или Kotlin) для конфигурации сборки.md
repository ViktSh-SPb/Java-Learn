В Gradle **файлы сборки (`build.gradle` или `build.gradle.kts`) пишутся на Groovy или Kotlin**, что делает конфигурацию **гибкой, программируемой и читаемой**. Давай разберёмся подробнее.
## 1️⃣ Основная идея
- **Groovy DSL** (`build.gradle`) — динамический, лаконичный язык, близкий к Java, с упрощённой синтаксисической оболочкой для декларации задач.
- **Kotlin DSL** (`build.gradle.kts`) — статически типизированный язык, с поддержкой IDE-подсказок и проверки типов на этапе компиляции.
> В обоих случаях Gradle рассматривает файл как **скрипт, который строит граф задач (Task Graph)**.
## 2️⃣ Структура конфигурационного файла на Groovy
Пример `build.gradle`:
```groovy
plugins {
    id 'java'
    id 'application'
}

group = 'com.example'
version = '1.0.0'

repositories {
    mavenCentral()
}

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web:3.3.0'
    testImplementation 'org.junit.jupiter:junit-jupiter:5.10.0'
}

application {
    mainClass = 'com.example.Main'
}

tasks.register('hello') {
    doLast {
        println 'Hello, Gradle!'
    }
}
```
### Пояснение:
1. **`plugins`** — подключение плагинов (Java, Application и т.д.).
2. **`group` и `version`** — идентификация проекта.
3. **`repositories`** — где искать зависимости.
4. **`dependencies`** — подключение библиотек.
5. **`tasks.register('hello')`** — создание кастомной задачи с блоком `doLast`.
## 3️⃣ Структура конфигурационного файла на Kotlin
Пример `build.gradle.kts`:
```kotlin
plugins {
    java
    application
}

group = "com.example"
version = "1.0.0"

repositories {
    mavenCentral()
}

dependencies {
    implementation("org.springframework.boot:spring-boot-starter-web:3.3.0")
    testImplementation("org.junit.jupiter:junit-jupiter:5.10.0")
}

application {
    mainClass.set("com.example.Main")
}

tasks.register("hello") {
    doLast {
        println("Hello, Gradle!")
    }
}
```
- Отличие: **статическая типизация**, IDE подсказывает свойства, методы, типы задач.
- Используется `.set()` для свойств вместо присваивания напрямую.
## 4️⃣ Программируемый характер
В отличие от Maven (где `pom.xml` — чисто декларативный XML), Gradle с Groovy/Kotlin позволяет:
1. **Использовать условные конструкции и циклы**:
```groovy
(1..3).each { i ->
    tasks.register("task$i") {
        doLast { println "Task $i executed" }
    }
}
```
2. **Определять зависимости между задачами динамически**:
```groovy
tasks.register('buildAll') {
    dependsOn tasks.matching { it.name.startsWith('task') }
}
```
3. **Делать вычисления и логические проверки**:
```groovy
if (project.hasProperty('enableFeatureX')) {
    println 'Feature X enabled'
}
```
## 5️⃣ Преимущества использования Groovy/Kotlin DSL

|Плюс|Объяснение|
|---|---|
|Гибкость|Можно писать условия, циклы, вычисления прямо в скрипте сборки|
|IDE-подсказки (Kotlin)|Типизация и автодополнение в IntelliJ IDEA|
|Динамическая конфигурация|Можно генерировать задачи или зависимости на лету|
|Совместимость с плагинами|Любой плагин можно настроить через DSL|
|Инкрементальная сборка|DSL позволяет явно указывать `inputs` и `outputs` для задач|
### 📌 Краткий вывод
Gradle использует **Groovy или Kotlin DSL** как язык для описания **логики и структуры сборки**. Это делает проект:
- **гибким** — можно писать условия, циклы, вычисления;
- **интерактивным** — IDE подсказывает свойства и методы (Kotlin);
- **модульным** — легко создавать кастомные задачи, зависимости и плагины;
- **эффективным** — легко управлять инкрементальной сборкой и кэшированием.