## 🧱 1. **Утечки соединений**
📉 _соединения «зависают» и не возвращаются в пул → пул исчерпывается_
### 📌 Причина:
- Разработчик не закрыл `Connection`, `Statement` или `ResultSet`.
- Ошибки в логике транзакций (например, исключение без `finally` или `try-with-resources`).
- Долгие транзакции, которые держат соединения «заблокированными».
### ⚠️ Признаки:
- Ошибки вида:
    ```
    java.sql.SQLTransientConnectionException: HikariPool-1 - Connection is not available, request timed out after 30000ms.
    ```
- Постепенное увеличение использования соединений.
- Метрики пула показывают, что `activeConnections` = `maximumPoolSize`.
### ✅ Решение:
- Использовать `try-with-resources`:
    ```java
    try (Connection conn = dataSource.getConnection();
         PreparedStatement stmt = conn.prepareStatement(...);
         ResultSet rs = stmt.executeQuery()) {
        ...
    } // ресурсы автоматически закрываются
    ```
- Включить **утечкодетекцию**:
    ```yaml
    spring.datasource.hikari.leak-detection-threshold: 5000
    ```
    Hikari выведет в лог стек вызова, где соединение не было закрыто.
- Оптимизировать бизнес-логику, чтобы не держать транзакции дольше, чем нужно.
## 🧭 2. **Исчерпание пула соединений**
📉 _все соединения заняты — новые запросы ждут или падают по таймауту_
### 📌 Причина:
- Маленькое значение `maximumPoolSize` при высокой нагрузке.
- Потоки держат соединения слишком долго.
- Утечки соединений.
### ⚠️ Признаки:
- Ошибка:
    ```
    java.sql.SQLTransientConnectionException: Timeout after ...
    ```
- Рост времени ответа API.
- Метрики показывают высокий `activeConnections`.
### ✅ Решение:
- Увеличить `maximumPoolSize`, если БД и инфраструктура это позволяют.
- Сократить время жизни транзакций.
- Проверить код на утечки.
- Настроить правильный `connectionTimeout`, чтобы приложение не зависало бесконечно.
## 🧨 3. **“Протухшие” соединения** (stale / dead connections)
📉 _соединение в пуле больше не активно, но пул его не обновил вовремя_
### 📌 Причина:
- Соединение было прервано из-за сетевых проблем, таймаутов на стороне БД или перезапуска БД.
- Пул не успел отловить мёртвое соединение.
### ⚠️ Признаки:
- Ошибки вида:
    ```
    org.postgresql.util.PSQLException: This connection has been closed.
    ```
- Падения при первом обращении к соединению.
### ✅ Решение:
- Настроить `maxLifetime` немного меньше, чем таймаут БД (например, если `idle_in_transaction_session_timeout` в Postgres = 30 мин, то `maxLifetime` = 25 мин).
- Указать `connectionTestQuery` (если пул не делает проверку автоматически) или использовать `validationTimeout`.
- Включить “test on borrow” — проверку соединения при выдаче из пула.
Пример:
```yaml
spring.datasource.hikari.max-lifetime: 1500000
spring.datasource.hikari.validation-timeout: 3000
```
## 🕳 4. **Слишком долгие транзакции**
📉 _соединения надолго блокируются одним потоком_
### 📌 Причина:
- Бизнес-логика делает слишком много в одной транзакции.
- Ожидание внешних сервисов внутри транзакции.
- Блокировки на уровне БД.
### ⚠️ Признаки:
- Высокое время отклика.
- Рост числа активных соединений.
- Блокировки на уровне БД (`pg_locks`, `SHOW ENGINE INNODB STATUS`).
### ✅ Решение:
- Разбить операции на более мелкие транзакции.
- Уменьшить время жизни соединения в коде.
- Не вызывать внешние API внутри транзакций.
- Настроить таймаут транзакций.
## ⚠️ 5. **Неправильная конфигурация пула**
### 📌 Примеры ошибок:
- `maximumPoolSize` слишком велик → БД перегружается.
- `connectionTimeout` слишком велик → зависания под нагрузкой.
- `idleTimeout` слишком мал → постоянное пересоздание соединений.
- `maxLifetime` больше, чем таймаут на стороне БД → неожиданные обрывы.
### ✅ Решение:
- Подобрать параметры под **реальную нагрузку** (через нагрузочное тестирование).
- Учитывать ограничения самой БД (`max_connections` и т. д.).
- Следить за метриками пула.
## 🐢 6. **Блокировки и взаимные блокировки (deadlocks)**
### 📌 Причина:
- Неправильный порядок захвата ресурсов в транзакциях.
- Несогласованное использование соединений разными потоками.
### ⚠️ Признаки:
- Повторяющиеся ошибки транзакций.
- Зависания при высокой конкурентной нагрузке.
### ✅ Решение:
- Гарантировать одинаковый порядок операций при доступе к одним и тем же таблицам.
- Использовать таймауты транзакций.
- При необходимости — обрабатывать deadlock-ошибки с повтором операции.
## 🧰 7. **Мониторинг и диагностика отсутствуют**
Даже при хорошей конфигурации пул может вести себя непредсказуемо без мониторинга.
### ✅ Что делать:
- Включить метрики пула:
    - Hikari поддерживает Micrometer → Prometheus → Grafana.
    - Отслеживать `activeConnections`, `idleConnections`, `pendingThreads`.
- Добавить логирование ошибок пула.
- Использовать `leakDetectionThreshold`.
## 🏁 Резюме: типичные проблемы и решения

|Проблема|Причина|Решение|
|---|---|---|
|Утечки соединений|Не закрывают коннекты|try-with-resources, leak detection, ревью кода|
|Исчерпание пула|Маленький maxPoolSize, долгие транзакции|Увеличить пул, оптимизировать транзакции|
|Протухшие соединения|Обрывы сети, перезапуск БД|Настроить maxLifetime, validation|
|Долгие транзакции|Медленная логика|Разделить операции, не блокировать соединения|
|Неправильные параметры|Завышенные/заниженные значения|Тестировать и подбирать оптимальные значения|
|Deadlocks|Некорректный порядок захвата ресурсов|Обеспечить согласованный порядок, таймауты транзакций|
|Отсутствие мониторинга|Невидимые проблемы в проде|Подключить Micrometer / Grafana, логировать ошибки пула|
👉 **Вывод:**  
Пул соединений ускоряет работу приложения, но при этом требует:
- правильной конфигурации,
- дисциплинированной работы с транзакциями,
- мониторинга в продакшене.