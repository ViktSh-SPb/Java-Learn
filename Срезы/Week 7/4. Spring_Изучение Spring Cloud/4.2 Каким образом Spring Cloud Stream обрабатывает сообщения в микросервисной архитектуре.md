**Spring Cloud Stream** — это фреймворк, который упрощает работу с **асинхронными сообщениями** в микросервисной архитектуре, предоставляя единый абстрактный API поверх разных брокеров (Kafka, RabbitMQ, AWS SNS/SQS и др.).
# 🧩 **1. Основные концепции Spring Cloud Stream**
1. **Binder** – адаптер к конкретному брокеру сообщений.
    - Примеры: Kafka Binder, RabbitMQ Binder
    - Прячет детали конкретного брокера, давая единый API.
2. **Channel (канал)** – абстракция очереди сообщений.
    - **Input**: канал, на который приходит сообщение
    - **Output**: канал, через который отправляются сообщения
3. **Message** – единица данных, которая проходит через каналы.
    - Содержит `payload` и `headers` (например, тип контента, traceId)
4. **Binding** – привязка канала к брокеру.
    - Spring Cloud Stream автоматически создаёт биндинги через `application.yml`
# 🧩 **2. Поток обработки сообщения**
Простейший сценарий:
```
Message Producer → Output Channel → Broker (Kafka/Rabbit) → Input Channel → Message Consumer
```
1. **Производитель (Producer)** отправляет объект в Output Channel.
2. **Binder** сериализует объект (JSON, Avro, String) и отправляет в брокер.
3. **Брокер** хранит сообщение и передаёт его подписчикам (темам или очередям).
4. **Consumer** через Input Channel получает сообщение, десериализует его и вызывает обработчик.
5. **Message обработчик** выполняет бизнес-логику (например, обновляет базу данных, отправляет уведомление).
# 🧩 **3. Пример обработки сообщений**
### 🔹 Producer (отправка события)
```java
@EnableBinding(Source.class)
public class OrderProducer {

    private final MessageChannel output;

    public OrderProducer(Source source) {
        this.output = source.output();
    }

    public void sendOrder(Order order) {
        Message<Order> message = MessageBuilder.withPayload(order)
                                               .setHeader("type", "new-order")
                                               .build();
        output.send(message);
    }
}
```
- `Source` – интерфейс с Output Channel (`output`)
- `MessageChannel.send()` – отправка сообщения через Binder
### 🔹 Consumer (приём события)
```java
@EnableBinding(Sink.class)
public class OrderConsumer {

    @StreamListener(Sink.INPUT)
    public void handle(Order order) {
        System.out.println("Received order: " + order);
        // бизнес-логика
    }
}
```
- `Sink.INPUT` – канал для входящих сообщений
- `@StreamListener` автоматически подписывается на канал
> В новых версиях Spring Cloud Stream можно использовать **функциональный подход** через `@Bean Function`:
```java
@Bean
public Consumer<Order> orderConsumer() {
    return order -> System.out.println("Received order: " + order);
}
```
# 🧩 **4. Конфигурация через `application.yml`**
```yaml
spring:
  cloud:
    stream:
      bindings:
        input:
          destination: orders
          group: order-service
        output:
          destination: orders
      kafka:
        binder:
          brokers: localhost:9092
```
- `destination` – топик/очередь в брокере
- `group` – consumer group (для Kafka)
- binder конфигурирует подключение к брокеру
# 🧩 **5. Особенности обработки**
1. **Асинхронность**
    - Producer и Consumer работают независимо
    - Consumer обрабатывает сообщения по мере поступления
2. **Гарантия доставки**
    - Kafka: at-least-once
    - RabbitMQ: at-most-once / at-least-once (зависит от ack)
3. **Сериализация/десериализация**
    - JSON, Avro, Protobuf
    - Автоматически через Binder
4. **Обработка ошибок**
    - Retry
    - Dead Letter Queue (DLQ) для сообщений, которые не удалось обработать
5. **Event-driven архитектура**
    - Легко масштабировать микросервисы: подписчики могут появляться и исчезать
    - Сообщения могут быть потреблены несколькими сервисами
# 🧩 **6. Преимущества Spring Cloud Stream**
- Абстракция над брокером → можно менять Rabbit на Kafka без изменения кода
- Конфигурация через `application.yml`
- Поддержка функционального программирования (`Function`, `Consumer`, `Supplier`)
- Встроенные retry, DLQ, partitioning
- Интеграция с Spring Boot и Spring Cloud