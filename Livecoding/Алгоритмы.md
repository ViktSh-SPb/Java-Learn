# Алгоритмы
### Задача 1
#### Условие:
Верните длину самой длинной подстроки без повторов. Ожидаемая сложность О(n) по времени, О(min(n, alphabet)) по памяти. TODO: sliding window с HashMap/массивом последних позиций символов.
```java
public class Main {
    public static void main(String[] args) {
        String s = "pwwkew";
        int ans = lengthOfLongestSubstring(s); // TODO: реализуйте
        System.out.println("Input: " + s);
        System.out.println("Longest substring length = " + ans);
    }

    static int lengthOfLongestSubstring(String s) {
    ...
        return max;
    }
}
```
#### Решение:
```java
static int lengthOfLongestSubstring(String s) {  
    char[] chars = s.toCharArray();  
    Map<Character, Integer> unique = new HashMap<>();  
    int max = 0;  
    int start = 0;  
    for (int i = 0; i < chars.length; i++) {  
        Integer prev = unique.get(chars[i]);  
        if (prev != null && prev >= start) {  
            start = prev + 1;  
        }  
        max = Math.max(max, i - start + 1);  
        unique.put(chars[i], i);  
    }  
    return max;  
}
```
### Задача 2
#### Условие:
Дан массив целых чисел. Нужно найти второй по величине элемент.
```java
public static void main(String[] args) {  
    int[] nums = {5, 2, 9, 1, 7};  
    System.out.println("Second max: " + findSecondMax(nums));  
}  
  
private static int findSecondMax(int[] nums) {  
    return 0;  
}
```
#### Решение:
```java
code
```
### Задача 3
#### Условие:
Условие
```java
code
```
#### Решение:
```java
code
```
### Задача 4
#### Условие:
Условие
```java
code
```
#### Решение:
```java
code
```
### Задача 5
#### Условие:
Условие
```java
code
```
#### Решение:
```java
code
```
### Задача 6
#### Условие:
Условие
```java
code
```
#### Решение:
```java
code
```