Принцип **“Convention over Configuration”** (с англ. «соглашение вместо конфигурации») — это **подход к разработке программного обеспечения, при котором система использует разумные стандартные соглашения, чтобы сократить необходимость явной настройки**.
Иными словами: если ты следуешь «принятым стандартам», то дополнительных настроек почти не требуется.
## 1️⃣ Суть принципа
- **Конвенция (Convention)** — это набор стандартов и соглашений, принятых системой.
- **Конфигурация (Configuration)** — ручное указание всех параметров.
> Чем больше проект следует соглашениям, тем меньше нужно писать конфигурации вручную.
**Пример в общем виде:**
- Если файл лежит в стандартной папке `src/main/java`, система автоматически его компилирует.
- Если тесты находятся в `src/test/java`, система их автоматически запускает.
## 2️⃣ Как это реализовано в Maven
Maven сильно ориентирован на **Convention over Configuration**:
### 🔹 2.1 Структура проекта
Стандартная структура Maven-проекта:
```
my-app/
├── pom.xml
├── src/
│   ├── main/
│   │   ├── java/         ← исходный код
│   │   └── resources/    ← ресурсы
│   └── test/
│       ├── java/         ← тесты
│       └── resources/    ← тестовые ресурсы
```
- Maven **по умолчанию** знает, где искать исходники и тесты.
- Нет необходимости указывать пути в POM, если проект придерживается стандартной структуры.
### 🔹 2.2 Фазы сборки (Lifecycle)
Maven использует **предопределённые фазы**:

|Фаза|Что делает|
|---|---|
|`compile`|Компилирует код из `src/main/java`|
|`test-compile`|Компилирует код тестов из `src/test/java`|
|`test`|Запускает тесты|
|`package`|Упаковывает в JAR/WAR|
|`install`|Копирует артефакт в локальный репозиторий|
- Разработчику не нужно явно указывать, что компилировать или как запускать тесты — Maven использует **соглашение по умолчанию**.
### 🔹 2.3 Стандартизированные плагины
Maven поставляется с **стандартными плагинами**, которые следуют соглашениям:
- `maven-compiler-plugin` — компилирует код из `src/main/java`
- `maven-surefire-plugin` — запускает тесты из `src/test/java`
- `maven-jar-plugin` — собирает JAR-файлы
> Если структура проекта стандартная, можно не настраивать плагины вообще.
### 🔹 2.4 Пример «минимального» POM
```xml
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.example</groupId>
    <artifactId>my-app</artifactId>
    <version>1.0.0</version>
</project>
```
- Даже **без секции `<build>`** Maven сможет:
    - Скомпилировать код из `src/main/java`
    - Запустить тесты из `src/test/java`
    - Упаковать JAR-файл
## 3️⃣ Преимущества для разработчика
1. **Меньше конфигурации** — можно работать с «готовыми» стандартами.
2. **Предсказуемость** — любой Maven-проект имеет одинаковую структуру и поведение.
3. **Скорость разработки** — не тратим время на настройку сборки.
4. **Совместимость с плагинами** — большинство плагинов ориентированы на стандартную структуру.
### ✅ Краткий вывод
**Principle: Convention over Configuration** в Maven означает:
> «Если проект следует стандартной структуре и соглашениям Maven, почти не нужно писать конфигурацию — всё работает автоматически».
- Maven использует **стандартизированные директории, фазы сборки и плагины**.
- Разработчику нужно указывать только **координаты проекта и зависимости**, а сборка, тесты и упаковка выполняются по соглашению.