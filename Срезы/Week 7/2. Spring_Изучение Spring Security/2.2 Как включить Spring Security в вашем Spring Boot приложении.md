Включить **Spring Security** в Spring Boot очень просто — он автоматически активируется, как только вы добавляете нужный стартер.
# ✅ **1. Добавить зависимость**
## Maven:
```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-security</artifactId>
</dependency>
```
## Gradle:
```gradle
implementation 'org.springframework.boot:spring-boot-starter-security'
```
После добавления — **Spring Security автоматически включается**.
# 🔒 Что происходит после включения?
Spring Boot создает **security filter chain**, и теперь:
- **все запросы требуют аутентификацию**,
- создаётся **дефолтный пользователь** `user`,
- пароль выводится в логах при старте:
```
Using generated security password: 3f9d3c30-3e77-4b8e-a678-3ff0e231813e
```
# ✅ **2. Настроить свою конфигурацию безопасности (опционально)**
Обычно создают конфигурационный класс:
```java
@Configuration
@EnableWebSecurity
public class SecurityConfig {

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .csrf(csrf -> csrf.disable()) // пример: отключить CSRF для REST API
            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/public/**").permitAll()
                .anyRequest().authenticated()
            )
            .httpBasic(withDefaults()); // или formLogin(), или JWT

        return http.build();
    }
}
```
# 🔑 **3. Настроить пользователя (если не используете JWT/OAuth2)**
Простой in-memory пользователь:
```java
@Bean
public UserDetailsService userDetailsService() {
    UserDetails user = User.withDefaultPasswordEncoder()
        .username("admin")
        .password("password")
        .roles("ADMIN")
        .build();

    return new InMemoryUserDetailsManager(user);
}
```
# 🧩 Альтернативы:
- Настроить пользователей через **БД** (UserDetailsService + репозиторий)
- Использовать **JWT токены**
- Подключить **OAuth2 / OpenID Connect**
- Интеграция с **Keycloak**, **Azure AD**, **LDAP**
# 📌 Итог
Чтобы включить Spring Security:
1. Добавляете зависимость `spring-boot-starter-security`.
2. Настраиваете `SecurityFilterChain`, если нужно.
3. Опционально настраиваете пользователей или JWT.
Spring запускает security **по умолчанию без дополнительного кода**.