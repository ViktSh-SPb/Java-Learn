Аннотация **@SpringBootApplication** — это _главная_ аннотация Spring Boot, которая включает сразу несколько ключевых механизмов: запуск приложения, автоконфигурацию и сканирование компонентов.  
Она ставится на главный класс приложения:
```java
@SpringBootApplication
public class MyApplication {
    public static void main(String[] args) {
        SpringApplication.run(MyApplication.class, args);
    }
}
```
# 🧩 Что делает @SpringBootApplication?
Аннотация является составной и включает в себя:
```java
@SpringBootConfiguration
@EnableAutoConfiguration
@ComponentScan
public @interface SpringBootApplication {}
```
То есть она объединяет три важнейшие возможности.
# 1️⃣ @SpringBootConfiguration
Это по сути то же самое, что и `@Configuration`.
👉 Говорит Spring:  
"Этот класс — конфигурационный и может содержать @Bean методы".
Это — точка входа для всего контекста.
# 2️⃣ @EnableAutoConfiguration
Включает механизм **автоконфигурации Spring Boot**.
Это значит:
- Boot сканирует classpath,
- смотрит, какие библиотеки присутствуют,
- активирует нужные классы автоконфигурации из  
    `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports`.
Например:
- при наличии `spring-boot-starter-web` → настраивается Spring MVC + Tomcat
- при наличии `spring-boot-starter-data-jpa` → настраивается JPA/Hibernate
- при наличии `spring-boot-starter-security` → включается цепочка SecurityFilterChain
Автоконфигурация — ключевой механизм Spring Boot.
# 3️⃣ @ComponentScan
Указывает Spring сканировать пакеты **относительно класса, где стоит аннотация**.
То есть если главный класс лежит в `com.myapp`, Spring просканирует:
```
com.myapp.*
```
и найдёт:
- @Controller
- @RestController
- @Service
- @Repository
- @Component
- @Configuration
👉 Это позволяет Spring найти ваши бины автоматически.
# 🔗 Как всё работает вместе
При запуске:
1. `SpringApplication.run()` создаёт Spring Context
2. `@ComponentScan` находит ваши классы и поднимает бины
3. `@EnableAutoConfiguration` подключает автоконфигурации
4. `@SpringBootConfiguration` позволяет добавлять собственные @Bean
5. Если приложение веб-типа — стартует встроенный сервер
6. Контроллеры принимают запросы — сервисы — репозитории — БД
# 🧠 Если кратко
**@SpringBootApplication = @Configuration + @EnableAutoConfiguration + @ComponentScan**
И тем самым:
- определяет главный класс,
- запускает автоконфигурацию,
- включает DI-контейнер,
- запускает сканирование компонентов,
- поднимает всё приложение Spring Boot.