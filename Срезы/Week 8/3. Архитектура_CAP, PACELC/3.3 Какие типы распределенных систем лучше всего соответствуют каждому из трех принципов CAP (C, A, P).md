В теореме **CAP** каждый принцип (C, A, P) описывает один из аспектов распределённой системы. На практике разные типы распределённых систем проектируются с упором на один или два из этих принципов, особенно в условиях сетевых разделений (Partition, P).
# 1️⃣ **Consistency (C) — согласованность**
Системы, ориентированные на **строгую согласованность данных**, даже если это снижает доступность при сетевых сбоях.
- **Особенности:**
    - Все узлы видят одно и то же состояние данных в любой момент.
    - При Partition часть запросов может быть отклонена (жертвуется Availability).
- **Примеры:**
    - Zookeeper (конфигурации, координация)
    - Etcd (конфигурации, Kubernetes)
    - HBase (по умолчанию)
    - Google Spanner (строгая глобальная консистентность)
✅ Подходит для систем, где **важнее точность данных**, чем скорость ответа.
# 2️⃣ **Availability (A) — доступность**
Системы, ориентированные на **доступность**, продолжают обслуживать запросы даже в условиях сетевых разделений, иногда жертвуя строгой консистентностью.
- **Особенности:**
    - Каждый запрос получает ответ.
    - Данные могут быть слегка рассинхронизированы (eventual consistency).
- **Примеры:**
    - Cassandra
    - DynamoDB
    - Riak
    - Couchbase (по умолчанию)
✅ Подходит для систем, где **важна непрерывная работа**, а не мгновенная согласованность.
# 3️⃣ **Partition Tolerance (P) — устойчивость к разделению сети**
Практически все распределённые системы **должны быть P**, потому что сетевые сбои неизбежны.
- **Особенности:**
    - Система продолжает работать, даже если часть узлов недоступна или сеть разделена.
- **Примеры:**
    - Любая современная распределённая база данных или кластер: Cassandra, MongoDB, HBase, Etcd, Zookeeper, Riak.
⚠️ **Важно:** Partition Tolerance — не «выбор», а **обязательное свойство современных распределённых систем**.
# 🔑 Итоговая таблица CAP

|Принцип|Основное назначение|Примеры систем|
|---|---|---|
|**C — Consistency**|Строгая согласованность, жертвуется доступностью при сбое сети|Zookeeper, Etcd, Spanner|
|**A — Availability**|Доступность, жертвуется строгой согласованностью при сбое сети|Cassandra, DynamoDB, Riak|
|**P — Partition Tolerance**|Система продолжает работать при сетевых разделениях|Практически все современные распределённые базы данных|
