Основные HTTP-методы (или **verbs**) определяют, какое действие клиент хочет выполнить над ресурсом на сервере. Вот ключевые:
### 🔹 1. GET
- **Назначение:** Получить данные с сервера.
- **Характеристики:**
    - Идемпотентный (несколько одинаковых запросов дают один результат).
    - Не изменяет состояние сервера.
- **Пример:**
    ```
    GET /users/1 HTTP/1.1
    ```
### 🔹 2. POST
- **Назначение:** Создать новый ресурс или отправить данные на сервер.
- **Характеристики:**
    - Не идемпотентный (повторный запрос может создать дубликаты).
- **Пример:**
    ```
    POST /users HTTP/1.1
    Content-Type: application/json
    
    {
      "name": "Alice",
      "email": "alice@mail.com"
    }
    ```
### 🔹 3. PUT
- **Назначение:** Полностью обновить существующий ресурс.
- **Характеристики:**
    - Идемпотентный (несколько одинаковых запросов дают один и тот же результат).
- **Пример:**
    ```
    PUT /users/1 HTTP/1.1
    Content-Type: application/json
    
    {
      "name": "Alice Updated",
      "email": "alice@mail.com"
    }
    ```
### 🔹 4. PATCH
- **Назначение:** Частично обновить ресурс.
- **Характеристики:**
    - Идемпотентный, но меняет только указанные поля.
- **Пример:**
    ```
    PATCH /users/1 HTTP/1.1
    Content-Type: application/json
    
    {
      "name": "Alice Updated"
    }
    ```
### 🔹 5. DELETE
- **Назначение:** Удалить ресурс.
- **Характеристики:**
    - Идемпотентный (повторное удаление не меняет состояние).
- **Пример:**
    ```
    DELETE /users/1 HTTP/1.1
    ```
### 🔹 6. OPTIONS
- **Назначение:** Получить поддерживаемые методы для ресурса (часто используется в CORS).
- **Пример:**
    ```
    OPTIONS /users/1 HTTP/1.1
    ```
### 🔹 7. HEAD
- **Назначение:** Получить только заголовки ответа, без тела.
- **Пример:**
    ```
    HEAD /users/1 HTTP/1.1
    ```