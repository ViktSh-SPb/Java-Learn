В **Kubernetes** механизм автоматического масштабирования (Autoscaling) позволяет динамически менять количество подов или ресурсов кластера в зависимости от нагрузки. Основные виды автоскейлинга: **Horizontal Pod Autoscaler (HPA), Vertical Pod Autoscaler (VPA) и Cluster Autoscaler**.
## 1️⃣ **Horizontal Pod Autoscaler (HPA)** – горизонтальное масштабирование подов
- **Что делает:** Увеличивает или уменьшает количество реплик подов Deployment/ReplicaSet в зависимости от метрик нагрузки (CPU, memory или кастомные метрики).
- **Как работает:**
    1. HPA следит за метриками с помощью Metrics Server (например, CPU utilization).
    2. Сравнивает текущую загрузку с целевым значением.
    3. Если нагрузка выше порога → увеличивает количество реплик.
    4. Если нагрузка ниже порога → уменьшает количество реплик.
**Пример:**
```yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: myapp-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: myapp
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 50
```
- Здесь Kubernetes поддерживает **2–10 подов**, чтобы средняя загрузка CPU была около 50%.
## 2️⃣ **Vertical Pod Autoscaler (VPA)** – вертикальное масштабирование подов
- **Что делает:** Изменяет ресурсы подов (CPU, память) без изменения их количества.
- **Когда полезно:** Если приложение использует больше ресурсов, чем выделено, и запускать больше реплик не имеет смысла.
## 3️⃣ **Cluster Autoscaler** – масштабирование кластера
- **Что делает:** Добавляет или удаляет узлы (VM) в кластере на основе потребностей.
- **Когда полезно:** Если HPA хочет создать новые поды, а на текущих узлах нет свободных ресурсов.
## 🔹 Как это работает вместе
1. Пользователь определяет HPA для Deployment.
2. Metrics Server собирает данные о CPU, памяти и других метриках.
3. HPA увеличивает/уменьшает количество подов.
4. Если узлы перегружены → Cluster Autoscaler добавляет новые узлы.
5. Если узлы простаивают → Cluster Autoscaler удаляет лишние узлы.
## 🔹 Преимущества автоматического масштабирования
- Приложение **не простаивает** и всегда справляется с нагрузкой.
- Экономия ресурсов: меньше узлов и подов, когда нагрузка низкая.
- Высокая доступность и отказоустойчивость.