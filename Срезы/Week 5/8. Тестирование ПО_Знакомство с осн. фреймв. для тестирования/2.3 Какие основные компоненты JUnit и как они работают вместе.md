В **JUnit** есть несколько ключевых компонентов, которые вместе обеспечивают полноценное **модульное тестирование** Java-кода. Давай разберём их по сути и по взаимодействию.
## 1️⃣ **Test Class (Класс тестов)**
- Это обычный Java-класс, где пишутся тесты.
- Содержит **тестовые методы**, настройки и очистку окружения.
- Пример:
```java
class CalculatorTest {
    // тестовые методы
}
```
## 2️⃣ **Test Method (Тестовый метод)**
- Отдельный метод в тестовом классе, помеченный аннотацией `@Test`.
- Определяет **один конкретный сценарий тестирования**.
- Пример:
```java
@Test
void testAdd() {
    assertEquals(5, calculator.add(2, 3));
}
```
- **JUnit запускает каждый тестовый метод отдельно**.
## 3️⃣ **Annotations (Аннотации)**
JUnit использует аннотации для управления **жизненным циклом теста**:

|Аннотация|Назначение|
|---|---|
|`@Test`|Обозначает тестовый метод|
|`@BeforeEach`|Выполняется перед каждым тестом (инициализация)|
|`@AfterEach`|Выполняется после каждого теста (очистка ресурсов)|
|`@BeforeAll`|Выполняется один раз перед всеми тестами класса (статический метод)|
|`@AfterAll`|Выполняется один раз после всех тестов класса (статический метод)|
|`@Disabled`|Отключает тест|
## 4️⃣ **Assertions (Проверки)**
- Позволяют проверить **ожидаемый результат** против фактического:
```java
assertEquals(expected, actual);
assertTrue(condition);
assertThrows(Exception.class, () -> someMethod());
```
- Если assertion **не проходит**, тест считается **неуспешным**.
## 5️⃣ **Test Runner (Исполнитель тестов)**
- **JUnit Test Runner** — это компонент, который:
    1. Находит тестовые классы и методы.
    2. Создаёт экземпляры тестовых классов.
    3. Выполняет методы с учётом аннотаций `@BeforeEach`, `@AfterEach`, `@BeforeAll`, `@AfterAll`.
    4. Отмечает результаты тестов (успешно / неуспешно / пропущено).
- Test Runner встроен в IDE (IntelliJ, Eclipse) и CI/CD (Maven, Gradle, Jenkins).
## 6️⃣ **Test Suite (Набор тестов)**
- Позволяет запускать **несколько тестовых классов вместе**.
- Используется для организации тестов в большие пакеты и интеграцию с CI/CD.
```java
@RunWith(Suite.class)
@Suite.SuiteClasses({CalculatorTest.class, UserServiceTest.class})
public class AllTests {}
```
## 7️⃣ **Mocking / Stubs (опционально)**
- С помощью библиотек типа **Mockito** можно создавать **фиктивные зависимости** для тестируемого класса.
- Позволяет изолировать unit-тесты от реальных сервисов, баз данных и внешних API.
## 🔹 Как компоненты работают вместе
1. **Test Runner** находит **Test Classes**.
2. Для каждого тестового метода:
    - Выполняется `@BeforeAll` (один раз на класс), затем `@BeforeEach` перед методом.
    - Выполняется **тестовый метод** (`@Test`).
    - Выполняется `@AfterEach`.
3. После всех тестов класса выполняется `@AfterAll`.
4. **Assertions** проверяют результаты теста → Test Runner формирует отчёт.
```
Test Runner
   │
   ├─ Test Class
   │    ├─ @BeforeAll
   │    ├─ @BeforeEach
   │    ├─ @Test → Assertions
   │    ├─ @AfterEach
   │    └─ @AfterAll
   │
   └─ Test Report (успех / провал / пропуск)
```
