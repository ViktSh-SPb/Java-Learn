В Spring Boot **можно не указывать версии большинства зависимостей**, потому что за вас это делает **Spring Boot Dependency Management** — механизм управления версиями, встроенный в Spring Boot.
# 🌟 Почему версии зависимостей можно не указывать?
Потому что Spring Boot предоставляет **Bill of Materials (BOM)** — специальный набор заранее протестированных версий всех библиотек, которые Spring Boot рекомендует использовать.
Этот BOM расположен в артефакте:
```
org.springframework.boot:spring-boot-dependencies
```
И он автоматически подключается, когда вы используете типичный Spring Boot родительский POM:
```xml
<parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.3.0</version>
</parent>
```
После этого Maven:
- **берёт версии всех зависимостей из BOM**,
- позволяет вам писать только groupId и artifactId.
Например:
```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-web</artifactId>
</dependency>
```
Maven уже знает, что:
- Spring Web → нужная версия Spring,
- Jackson → подходящая версия,
- Tomcat → нужная версия,
- etc.
# 🔧 Как это работает технически?
1. В `spring-boot-starter-parent` есть секция:
```xml
<dependencyManagement>
    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-dependencies</artifactId>
            <version>${spring-boot.version}</version>
            <type>pom</type>
            <scope>import</scope>
        </dependency>
    </dependencies>
</dependencyManagement>
```
2. Она **импортирует BOM** `spring-boot-dependencies`.
3. BOM содержит список _версий_ всех зависимостей, например:
```xml
<properties>
    <jackson.version>2.17.0</jackson.version>
    <tomcat.version>10.1.19</tomcat.version>
    <hibernate.version>6.4.1.Final</hibernate.version>
</properties>
```
4. Поэтому если вы подключаете зависимость _без указания версии_:
```xml
<dependency>
    <groupId>com.fasterxml.jackson.core</groupId>
    <artifactId>jackson-databind</artifactId>
</dependency>
```
Maven возьмёт версию из BOM.
# ✅ Зачем это сделано?
### ✔ 1. Гарантированная совместимость
Все версии библиотек протестированы вместе командой Spring.
### ✔ 2. Меньше ошибок совместимости
Не нужно самому угадывать, какая версия Jackson работает с какой версией Spring.
### ✔ 3. Меньше кода в POM
POM становится чище — необязательно перечислять версии.
### ✔ 4. Легкое обновление
Обновление Spring Boot с 3.2 → 3.3 автоматически обновляет весь стек зависимостей.
# ❗Когда нужно указывать версию вручную?
Только если:
- вам нужна **нестандартная версия** библиотеки,
- её **нет в BOM** Spring Boot,
- или вы специально хотите обновить библиотеку раньше Spring Boot.
Тогда:
```xml
<dependency>
    <groupId>org.flywaydb</groupId>
    <artifactId>flyway-core</artifactId>
    <version>10.15.0</version>
</dependency>
```