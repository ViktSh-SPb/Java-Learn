В теореме **CAP** невозможно одновременно достичь **Consistency (C)** и **Availability (A)** **в условиях сетевого разделения (P)**.
То есть:
### ❗ При наличии Partition (разделения сети) система не может быть одновременно:
- **полностью согласованной (C)**,
- **и полностью доступной (A)**.
Вы должны выбрать **либо C, либо A**, но не оба сразу.
# 🔍 Почему?
Потому что:
- Чтобы сохранить **Consistency**, система должна **отказаться отвечать**, если не может синхронизировать данные между узлами (то есть жертвует Availability).
- Чтобы сохранить **Availability**, система должна **отвечать даже без синхронизации**, что нарушает Consistency.
# 🧠 Итог (очень коротко)
При P (partition):
> ❌ Нельзя одновременно обеспечить **C + A**  
> ✔ Можно только **CP** или **AP**