В Kubernetes **Horizontal Pod Autoscaler (HPA)** можно настроить не только на основе стандартных метрик (CPU, память), но и на **пользовательские (custom) метрики**, например количество запросов в очередь, длина очереди сообщений или специфические бизнес-показатели.
## 1️⃣ Подготовка к использованию пользовательских метрик
1. **Metrics Server** собирает стандартные метрики (CPU, память).  
    Для пользовательских метрик нужно подключить:
    - **Prometheus Adapter** для Kubernetes, если метрики хранятся в Prometheus.
    - Другие адаптеры для внешних источников (Stackdriver, Datadog и т.д.).
2. Метрики должны быть **доступны через API Kubernetes Metrics API** в формате, который HPA понимает.
## 2️⃣ Проверка доступных метрик
```bash
kubectl get --raw "/apis/custom.metrics.k8s.io/v1beta1"
```
- Вывод покажет список доступных пользовательских метрик для ваших объектов (Pod, Deployment, Service и т.д.).
## 3️⃣ Создание HPA с пользовательской метрикой
Пример: масштабирование Deployment `myapp` по пользовательской метрике `requests_per_second`:
```yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: myapp-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: myapp
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Pods
    pods:
      metric:
        name: requests_per_second
      target:
        type: AverageValue
        averageValue: 100
```
**Пояснения:**
- `type: Pods` → HPA масштабирует на основе метрики Pod-уровня.
- `metric.name` → имя метрики, которое зарегистрировано в Metrics API.
- `averageValue` → среднее значение на Pod, при превышении которого создаются новые реплики.
## 4️⃣ Альтернатива: метрика уровня объекта (Object)
Если метрика привязана к объекту (например, очередь Kafka, база данных), используйте:
```yaml
- type: Object
  object:
    metric:
      name: queue_length
    describedObject:
      apiVersion: apps/v1
      kind: Deployment
      name: myapp
    target:
      type: Value
      value: "500"
```
- HPA будет масштабировать Deployment `myapp`, если длина очереди превышает 500.
## 5️⃣ Проверка работы HPA
```bash
kubectl get hpa myapp-hpa
```
- Показывает текущее количество реплик, текущее значение метрики и целевое значение.
Для динамического тестирования можно увеличивать нагрузку и смотреть, как HPA увеличивает число Pod’ов.
## 🔹 Рекомендации
1. Убедитесь, что метрики **доступны через Metrics API**.
2. Метрики должны обновляться **часто и стабильно**, иначе HPA будет масштабировать некорректно.
3. Настройте **мин./макс. количество реплик**, чтобы предотвратить резкие колебания.
4. Можно комбинировать несколько метрик (CPU + custom), чтобы масштабирование было более гибким.