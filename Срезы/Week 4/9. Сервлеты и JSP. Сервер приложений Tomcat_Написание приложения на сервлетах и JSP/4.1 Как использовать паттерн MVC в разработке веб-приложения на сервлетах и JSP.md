Паттерн **MVC (Model–View–Controller)** помогает **разделить логику, представление и управление**, делая приложение **структурированным, масштабируемым и поддерживаемым**.
Разберём по шагам, как реализовать **MVC на сервлетах и JSP**.
## 🧩 1. Что такое MVC

|Компонент|Роль|Пример|
|---|---|---|
|**Model (Модель)**|Отвечает за бизнес-логику, работу с базой данных|Класс `User`, DAO, сервис|
|**View (Представление)**|Отвечает за отображение данных пользователю|JSP-страницы|
|**Controller (Контроллер)**|Обрабатывает запросы, вызывает модель и передаёт данные в представление|Сервлет|
## ⚙️ 2. Общая схема работы MVC
```
     [Клиент / Браузер]
               |
               v
        ┌──────────────────┐
        │   Controller     │  <-- Сервлет (обрабатывает запрос)
        └──────────────────┘
               |
               v
        ┌──────────────────┐
        │     Model        │  <-- Бизнес-логика, DAO, сервисы
        └──────────────────┘
               |
               v
        ┌──────────────────┐
        │      View        │  <-- JSP (отображает результат)
        └──────────────────┘
```
## ✅ 3. Пример простой реализации MVC
### 📘 Модель (`User.java`)
```java
package com.example.model;

public class User {
    private String username;
    private String email;

    // Конструктор
    public User(String username, String email) {
        this.username = username;
        this.email = email;
    }

    // Геттеры и сеттеры
    public String getUsername() { return username; }
    public String getEmail() { return email; }
}
```
### 📘 DAO или сервис (`UserService.java`)
```java
package com.example.service;

import com.example.model.User;
import java.util.List;

public class UserService {

    public List<User> getAllUsers() {
        return List.of(
            new User("admin", "admin@mail.com"),
            new User("user", "user@mail.com")
        );
    }
}
```
### 📘 Контроллер (`UserController.java`)
```java
package com.example.controller;

import com.example.service.UserService;
import jakarta.servlet.*;
import jakarta.servlet.annotation.WebServlet;
import jakarta.servlet.http.*;
import java.io.IOException;

@WebServlet("/users")
public class UserController extends HttpServlet {

    private UserService userService = new UserService();

    @Override
    protected void doGet(HttpServletRequest req, HttpServletResponse resp)
            throws ServletException, IOException {

        // Получаем данные из модели
        req.setAttribute("users", userService.getAllUsers());

        // Передаём управление на JSP (View)
        RequestDispatcher dispatcher = req.getRequestDispatcher("/WEB-INF/views/users.jsp");
        dispatcher.forward(req, resp);
    }
}
```
### 📘 Представление (`users.jsp`)
```jsp
<%@ taglib uri="http://java.sun.com/jsp/jstl/core" prefix="c" %>
<html>
<head><title>Список пользователей</title></head>
<body>
<h2>Список пользователей</h2>
<ul>
    <c:forEach var="user" items="${users}">
        <li>${user.username} — ${user.email}</li>
    </c:forEach>
</ul>
</body>
</html>
```
## 🧠 4. Основная логика MVC в действии
1. Клиент отправляет запрос `GET /users`
2. `UserController` (Controller) принимает запрос
3. Контроллер вызывает `UserService` (Model), получает список пользователей
4. Сохраняет их в `request` атрибуте:
    ```java
    req.setAttribute("users", ...);
    ```
5. Перенаправляет на `users.jsp` (View)
6. JSP получает данные через EL (`${users}`) и отображает их.
## 🚀 5. Преимущества MVC
✅ **Разделение обязанностей** — код легче поддерживать.  
✅ **Масштабируемость** — легко добавлять контроллеры и модели.  
✅ **Тестируемость** — можно тестировать бизнес-логику отдельно от UI.  
✅ **Переиспользуемость** — один и тот же контроллер можно использовать с разными JSP.
## ⚠️ 6. Распространённые ошибки

|Ошибка|Причина|Решение|
|---|---|---|
|Логика в JSP|JSP выполняет бизнес-логику|Перенеси в Controller/Service|
|Нет разграничения путей|Все запросы идут в один сервлет|Создай отдельные контроллеры|
|500 Internal Error при EL|Не установлен атрибут|Проверь `setAttribute()`|
|JSP имеет доступ напрямую (минуя контроллер)|JSP в `webapp/`|Храни представления в `/WEB-INF/views/`|
## 💡 7. Современные улучшения
В старых Java EE проектах MVC реализовывали вручную (как выше).  
В современных Spring-приложениях используется тот же принцип, но реализованный фреймворком:

|Компонент MVC|В Servlets/JSP|В Spring MVC|
|---|---|---|
|Model|Классы + DAO|`@Service`, `@Entity`|
|View|JSP|`Thymeleaf`, `FreeMarker`|
|Controller|`HttpServlet`|`@Controller`, `@RequestMapping`|
