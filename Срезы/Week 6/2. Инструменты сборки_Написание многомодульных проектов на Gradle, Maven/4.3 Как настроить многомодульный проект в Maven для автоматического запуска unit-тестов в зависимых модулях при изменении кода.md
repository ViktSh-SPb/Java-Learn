## 1️⃣ Структура многомодульного проекта
Предположим, у нас есть проект с родителем и двумя модулями:
```
my-app/
├── pom.xml              ← родительский POM
├── core/
│   └── pom.xml          ← модуль библиотеки
└── web/
    └── pom.xml          ← модуль приложения, зависит от core
```
- `core` содержит бизнес-логику;
- `web` зависит от `core`.
## 2️⃣ Настройка родительского POM
В корневом `pom.xml` подключаем модули и плагины для тестирования:
```xml
<project>
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.example</groupId>
    <artifactId>my-app</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>

    <modules>
        <module>core</module>
        <module>web</module>
    </modules>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.1.2</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
```
> **Surefire Plugin** отвечает за запуск unit-тестов (`src/test/java`).
## 3️⃣ Зависимости между модулями
В модуле `web/pom.xml` добавляем зависимость на `core`:
```xml
<dependencies>
    <dependency>
        <groupId>com.example</groupId>
        <artifactId>core</artifactId>
        <version>1.0.0</version>
    </dependency>
    <dependency>
        <groupId>org.junit.jupiter</groupId>
        <artifactId>junit-jupiter</artifactId>
        <version>5.10.0</version>
        <scope>test</scope>
    </dependency>
</dependencies>
```
> Maven автоматически учитывает порядок сборки: сначала `core`, затем `web`.
## 4️⃣ Автоматический запуск тестов при изменении кода
Maven **не отслеживает изменения файлов в реальном времени** по умолчанию.  
Есть несколько способов заставить его автоматически запускать тесты:
### Способ 1 — Использовать **Maven Lifecycle**
```bash
mvn clean install
```
- Сначала соберутся все модули;
- Потом будут запущены unit-тесты всех модулей (`core` и `web`).
**Проблема:** это **полная пересборка**. Любое изменение кода — приходится запускать вручную.
### Способ 2 — Использовать **maven-surefire-plugin** с **watch mode** через IDE
- Современные IDE (IntelliJ, Eclipse) позволяют автоматически запускать тесты при изменении исходников.
- Плагины Maven интегрированы с IDE — автоматически учитывают зависимости между модулями.
### Способ 3 — Использовать **`mvn test -pl moduleName -am`**
- `-pl` (projects list) — указывает модуль, который изменился.
- `-am` (also make) — автоматически собирает все зависимости модуля.
Пример:
```bash
mvn test -pl web -am
```
- Maven пересоберёт `core`, если `web` от него зависит, и запустит тесты `web`.
> 🔹 Это ключевой механизм для «тестов зависимых модулей» без полной сборки.
### Способ 4 — Использовать сторонние инструменты **для непрерывной сборки**
- **Maven + JRebel / Spring DevTools / maven-compiler-plugin in incremental mode**
- **Watchexec / Maven File Watcher**: отслеживает изменения и вызывает:
```bash
mvn test -pl <changed-module> -am
```
Пример shell-скрипта для Linux/macOS:
```bash
#!/bin/bash
while true; do
  inotifywait -r -e modify src/
  mvn test -pl web -am
done
```
- Будет автоматически запускать тесты при изменении любого `.java` файла.
## 5️⃣ Резюме по автоматическому тестированию зависимых модулей

|Подход|Преимущество|Недостаток|
|---|---|---|
|`mvn test -pl <module> -am`|Пересобирает только зависимости|Не следит за файлами в реальном времени|
|IDE (IntelliJ, Eclipse)|Авто-тестирование при сохранении|Требует IDE, не CLI|
|Watcher / CI скрипт|Полностью автоматическое отслеживание|Дополнительная настройка, сторонние утилиты|
|Полная сборка `mvn install`|Простая, гарантированная|Медленно для больших проектов|
✅ **Вывод:**
Чтобы **тесты зависимых модулей запускались автоматически при изменении кода**:
1. Используй **правильную зависимость между модулями** (`<dependency>` + `-am` при CLI).
2. Для непрерывного тестирования — настрой **watcher** или используй **IDE с автозапуском тестов**.
3. Maven сам **упорядочивает сборку модулей по зависимостям**, так что тесты зависимых модулей запускаются в правильной последовательности.