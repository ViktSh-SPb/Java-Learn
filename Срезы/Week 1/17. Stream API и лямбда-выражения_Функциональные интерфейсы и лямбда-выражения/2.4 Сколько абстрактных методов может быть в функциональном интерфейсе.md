В **функциональном интерфейсе в Java может быть ровно один абстрактный метод**.
### Коротко
👉 **1 абстрактный метод** — ни больше, ни меньше.
### Что **не считается** абстрактными методами
Функциональный интерфейс **может содержать**:
- `default`-методы
- `static`-методы
- методы, унаследованные от `Object` (`equals`, `hashCode`, `toString`)
Пример — всё корректно:
```java
@FunctionalInterface
public interface Printer {
    void print(String s);   // один абстрактный метод

    default void log() {
        System.out.println("log");
    }

    static void info() {}

    @Override
    String toString(); // метод Object — не считается
}
```
### Ошибка компиляции
Если абстрактных методов больше одного:
```java
@FunctionalInterface
interface Bad {
    void a();
    void b(); // ❌ не функциональный интерфейс
}
```