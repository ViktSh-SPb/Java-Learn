**Kubernetes** (часто сокращают как **K8s**) — это платформа для **автоматизации развертывания, масштабирования и управления контейнеризированными приложениями**.
Проще говоря, это **система оркестрации контейнеров**, которая помогает запускать и управлять приложениями, упакованными в Docker или другие контейнеры, на кластере серверов.
## 🔹 Основные задачи Kubernetes
1. **Оркестрация контейнеров**
    - Автоматически запускает контейнеры на подходящих узлах.
    - Контролирует состояние контейнеров (перезапускает, если они упали).
2. **Масштабирование**
    - Легко увеличить или уменьшить количество контейнеров.
    - Горизонтальное масштабирование (`replicas`) в зависимости от нагрузки.
3. **Автообновления и откаты**
    - Rolling updates: обновляет контейнеры без простоя приложения.
    - Можно откатить версию, если новая не работает.
4. **Распределение нагрузки**
    - Сервис Kubernetes автоматически распределяет трафик между контейнерами.
5. **Управление конфигурацией и секретами**
    - ConfigMaps и Secrets позволяют безопасно хранить настройки и пароли.
6. **Хранение данных**
    - Persistent Volumes (PV) и Persistent Volume Claims (PVC) для работы с данными вне контейнера.
7. **Самовосстановление**
    - Если контейнер или узел падает, Kubernetes перезапускает или переносит контейнеры на другие узлы.
## 🔹 Основные компоненты Kubernetes

|Компонент|Назначение|
|---|---|
|**Pod**|Минимальная единица развертывания — один или несколько контейнеров, которые делят сеть и хранилище|
|**Node**|Физический или виртуальный сервер в кластере|
|**Cluster**|Совокупность всех узлов и компонентов управления|
|**Deployment**|Управляет созданием и обновлением подов|
|**Service**|Определяет, как доступаться к подам (load balancer внутри кластера)|
|**ConfigMap / Secret**|Управление конфигурациями и чувствительными данными|
|**PersistentVolume**|Хранилище данных, независимое от жизненного цикла контейнера|
## 🔹 Зачем использовать Kubernetes
- Для **масштабируемых приложений** (микросервисы).
- Для **управления множеством контейнеров** на нескольких серверах.
- Для **автоматизации деплоя и обновлений**.
- Для обеспечения **отказоустойчивости** и высокой доступности.