Для мониторинга и управления работающими Java-приложениями в реальном времени в составе JDK предоставляется **jconsole** и **jcmd**, но наиболее корректный ответ — **jconsole**, так как это основной инструмент именно для _мониторинга_ в реальном времени.
### ✅ **Командный инструмент JDK для мониторинга Java-приложений — `jconsole`**
**JConsole** — это графический инструмент (запускаемый из командной строки), который позволяет в реальном времени смотреть:
- загрузку процессора,
- использование памяти (включая heap и metaspace),
- состояние потоков,
- работу GC,
- MBeans через JMX.
Запуск:
```bash
jconsole
```
Он подключается к локальным и удалённым JVM-процессам.
### 🔄 Альтернативные инструменты, также входящие в JDK
Хотя `jconsole` — более точный ответ, есть и другие инструменты:
#### **1. jcmd**
Мощный инструмент командной строки:
```bash
jcmd <pid> GC.heap_info
jcmd <pid> Thread.print
jcmd <pid> VM.system_properties
```
Используется для диагностики, запуска VM-команд, получения heap dumps и т. д.
#### **2. jstat**
Монитор использования памяти и GC:
```bash
jstat -gc <pid> 1s
```
#### **3. jps**
Список запущенных JVM-процессов.
### 📌 Итог
Если вопрос:  
**"Какой инструмент в JDK используется для мониторинга и управления Java-приложениями в реальном времени?"**
➡ **Ответ: `jconsole`**.
Если допускается CLI-вариант:
➡ **Также: `jcmd`**.