В **Kubernetes** архитектура делится на **контролирующую плоскость (Control Plane)** и **рабочие узлы (Worker Nodes)**. Ниже — основные компоненты и их назначение.
## 1️⃣ **Контролирующая плоскость (Control Plane)**
Это мозг кластера, отвечает за управление состоянием всего кластера.

|Компонент|Назначение|
|---|---|
|**API Server (`kube-apiserver`)**|Принимает все REST-запросы от пользователей, CLI (`kubectl`) и других компонентов. Это центральная точка взаимодействия.|
|**etcd**|Распределённое хранилище ключ-значение, где хранится вся информация о состоянии кластера (конфигурации, Pods, Deployments).|
|**Controller Manager (`kube-controller-manager`)**|Контроллеры, которые следят за состоянием кластера и исправляют его: например, ReplicaSet следит, чтобы было нужное количество подов.|
|**Scheduler (`kube-scheduler`)**|Решает, на каком узле запускать новые Pod’ы, исходя из ресурсов, меток, ограничений и правил доступности.|
## 2️⃣ **Рабочие узлы (Worker Nodes)**
На них реально запускаются контейнеры и приложения.

|Компонент|Назначение|
|---|---|
|**Kubelet**|Агент, который управляет Pod’ами на узле, следит за их состоянием и сообщает контролирующей плоскости.|
|**Kube-proxy**|Сервис сетевого взаимодействия и балансировки нагрузки между Pod’ами.|
|**Container Runtime**|Система, которая реально запускает контейнеры (Docker, containerd, CRI-O).|
## 3️⃣ **Объекты Kubernetes**
Хотя это не физические компоненты, они составляют архитектуру на уровне API:

|Объект|Назначение|
|---|---|
|**Pod**|Минимальная единица развертывания, может содержать один или несколько контейнеров.|
|**Deployment**|Обеспечивает управление количеством реплик Pod’ов и обновлениями (Rolling Update, Rollback).|
|**Service**|Определяет доступ к Pod’ам и балансировку нагрузки.|
|**ConfigMap / Secret**|Хранение конфигураций и чувствительных данных.|
|**PersistentVolume / PersistentVolumeClaim**|Управление постоянным хранилищем данных.|
## 🔹 Итог
Kubernetes разделяет управление и исполнение:
- **Control Plane** → управляет состоянием кластера.
- **Worker Nodes** → выполняют контейнеры.
- **Объекты Kubernetes** → описывают, _что_ должно работать и _как_.