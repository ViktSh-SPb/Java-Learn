# Алгоритмы
### Задача 1
#### Условие:
Верните длину самой длинной подстроки без повторов. Ожидаемая сложность О(n) по времени, О(min(n, alphabet)) по памяти. TODO: sliding window с HashMap/массивом последних позиций символов.
```java
public class Main {
    public static void main(String[] args) {
        String s = "pwwkew";
        int ans = lengthOfLongestSubstring(s); // TODO: реализуйте
        System.out.println("Input: " + s);
        System.out.println("Longest substring length = " + ans);
    }

    static int lengthOfLongestSubstring(String s) {
    ...
        return max;
    }
}
```
#### Решение:
```java
```