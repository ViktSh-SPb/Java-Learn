## 🧩 **1. Общая идея**
Обычно данные формируются в **сервлете (Controller)**,  
а отображаются в **JSP (View)**.
Передача данных происходит через объект **`HttpServletRequest`**  
или, реже, через **`HttpSession`** или **`ServletContext`**.
## ✅ **2. Передача данных через `request` (наиболее распространённый способ)**
### 🔹 Шаг 1: Устанавливаем атрибут в сервлете
```java
@WebServlet("/hello")
public class HelloServlet extends HttpServlet {
    @Override
    protected void doGet(HttpServletRequest request, HttpServletResponse response)
            throws IOException, ServletException {

        // Передаём данные
        request.setAttribute("message", "Привет, мир из сервлета!");

        // Перенаправляем на JSP
        RequestDispatcher dispatcher = request.getRequestDispatcher("/WEB-INF/views/hello.jsp");
        dispatcher.forward(request, response);
    }
}
```
### 🔹 Шаг 2: Получаем данные в JSP
```jsp
<!-- /WEB-INF/views/hello.jsp -->
<html>
<body>
    <h2>${message}</h2> <!-- EL (Expression Language) -->
</body>
</html>
```
> 💡 Благодаря **Expression Language (EL)** можно просто писать `${message}`  
> вместо старого `<%= request.getAttribute("message") %>`.
## ✅ **3. Передача коллекций или объектов**
Сервлет:
```java
List<String> fruits = List.of("Яблоко", "Банан", "Груша");
request.setAttribute("fruits", fruits);
request.getRequestDispatcher("/WEB-INF/views/fruits.jsp").forward(request, response);
```
JSP:
```jsp
<ul>
    <c:forEach var="fruit" items="${fruits}">
        <li>${fruit}</li>
    </c:forEach>
</ul>
```
> Здесь используется **JSTL (JavaServer Pages Standard Tag Library)** — стандартная библиотека тегов для JSP.  
> Не забудь подключить её вверху страницы:
> 
> ```jsp
> <%@ taglib uri="http://java.sun.com/jsp/jstl/core" prefix="c" %>
> ```
## ✅ **4. Альтернатива: через `session` (если нужно сохранить между запросами)**
```java
HttpSession session = request.getSession();
session.setAttribute("user", "Viktor");
response.sendRedirect("profile.jsp");
```
```jsp
<p>Пользователь: ${sessionScope.user}</p>
```
## ✅ **5. Через `context` (глобально для всего приложения)**
```java
getServletContext().setAttribute("appName", "My Web App");
```
```jsp
<p>Имя приложения: ${applicationScope.appName}</p>
```
## ⚙️ **6. Разница между `forward` и `redirect`**

|Метод|Что делает|Передаются ли данные (`request`)?|Пример|
|---|---|---|---|
|**`forward()`**|Перенаправляет внутри сервера (без нового запроса)|✅ Да|`dispatcher.forward(request, response)`|
|**`sendRedirect()`**|Отправляет клиенту новый URL (новый запрос)|❌ Нет|`response.sendRedirect("page.jsp")`|
## 🧠 **Итог**

|Цель|Используй|
|---|---|
|Передать данные на JSP в рамках одного запроса|`request.setAttribute()` + `forward()`|
|Сохранить данные между запросами (например, сессия пользователя)|`session.setAttribute()`|
|Сделать данные доступными всему приложению|`servletContext.setAttribute()`|
