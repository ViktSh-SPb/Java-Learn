**SOAP (Simple Object Access Protocol)** — это **протокол обмена сообщениями**, используемый для взаимодействия между приложениями через сеть, чаще всего по протоколу **HTTP** (иногда SMTP или другие).  
Он определяет строгие правила того, **как клиент и сервер обмениваются структурированными XML-сообщениями**.
### 🧩 Основная идея
SOAP позволяет разным системам (даже написанным на разных языках и работающим на разных платформах) **взаимодействовать стандартизированным способом**, обмениваясь XML-сообщениями с чётко описанными структурами данных.
### ⚙️ Структура SOAP-сообщения
SOAP-сообщение — это XML-документ с фиксированной структурой:
```xml
<soap:Envelope xmlns:soap="http://schemas.xmlsoap.org/soap/envelope/">
  <soap:Header>
    <!-- Дополнительная информация (например, аутентификация) -->
  </soap:Header>
  <soap:Body>
    <!-- Основное содержимое запроса или ответа -->
  </soap:Body>
</soap:Envelope>
```
- **Envelope** — корневой элемент (определяет сообщение как SOAP).
- **Header** — необязательный, содержит метаданные (например, токен безопасности, транзакции и т.п.).
- **Body** — основное содержимое запроса/ответа.
- **Fault** — элемент, используемый для описания ошибок.
### 🌐 Как работает SOAP
1. Клиент формирует SOAP-запрос (XML).
2. Отправляет его на сервер (обычно POST-запрос к endpoint’у).
3. Сервер обрабатывает запрос и возвращает SOAP-ответ (тоже XML).
4. Оба участника используют **WSDL (Web Services Description Language)** — файл-описание веб-сервиса, где указано:
    - какие методы доступны;
    - какие параметры принимает каждый метод;
    - какие типы данных используются.
### 🛡️ Преимущества SOAP
- ✅ Строгая спецификация и стандарты (WSDL, XSD, WS-Security и т.д.).
- ✅ Поддержка **безопасности** и **транзакций** на уровне протокола.
- ✅ Независимость от платформ и языков.
- ✅ Подходит для **корпоративных интеграций**, где важна надёжность и формализация.
### ⚠️ Недостатки
- ❌ Большой объём XML (избыточность по сравнению с JSON).
- ❌ Сложнее в настройке и сопровождении.
- ❌ Менее гибкий, чем REST или GraphQL.
- ❌ Производительность ниже из-за XML-парсинга и строгих стандартов.
### 📘 Пример SOAP-запроса
```xml
POST /weather HTTP/1.1
Host: example.com
Content-Type: text/xml; charset=utf-8
SOAPAction: "GetWeather"

<soap:Envelope xmlns:soap="http://schemas.xmlsoap.org/soap/envelope/">
  <soap:Body>
    <GetWeather xmlns="http://example.com/weather">
      <City>Amsterdam</City>
    </GetWeather>
  </soap:Body>
</soap:Envelope>
```