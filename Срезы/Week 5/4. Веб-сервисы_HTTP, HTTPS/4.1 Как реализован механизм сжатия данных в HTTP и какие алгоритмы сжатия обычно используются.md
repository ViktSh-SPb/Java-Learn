В HTTP **механизм сжатия данных** позволяет уменьшить объём передаваемой информации между сервером и клиентом, что **ускоряет загрузку страниц и снижает нагрузку на сеть**.
### 🔹 Как это работает
1. **Клиент указывает поддерживаемые алгоритмы сжатия**
    - В HTTP-запросе используется заголовок `Accept-Encoding`:
    ```
    GET /index.html HTTP/1.1
    Host: example.com
    Accept-Encoding: gzip, deflate, br
    ```
    Это значит: клиент может принимать сжатые данные в форматах `gzip`, `deflate` или `brotli (br)`.
2. **Сервер выбирает подходящий алгоритм**
    - Если сервер поддерживает один из указанных форматов, он сжимает ответ и добавляет заголовок `Content-Encoding`:
    ```
    HTTP/1.1 200 OK
    Content-Encoding: gzip
    Content-Type: text/html
    ```
3. **Клиент разжимает данные**
    - Браузер или HTTP-клиент автоматически распаковывает тело ответа перед обработкой.
### 🔹 Основные алгоритмы сжатия

|Алгоритм|Особенности|
|---|---|
|**gzip**|Самый распространённый; хорошая компрессия для текста и HTML/CSS/JS; поддерживается всеми браузерами|
|**deflate**|Быстрое сжатие, но немного хуже поддержка в старых браузерах|
|**brotli (br)**|Современный алгоритм; лучшее сжатие для текстовых данных; поддержка в современных браузерах|
|**compress (LZW)**|Редко используется; устаревший|
### 🔹 Преимущества сжатия
- Сокращение объёма данных, передаваемых по сети.
- Более быстрая загрузка страниц и API-ответов.
- Снижение нагрузки на сервер и канал связи.
✅ **Итог:** HTTP сжатие реализуется через **согласование алгоритмов между клиентом и сервером** (`Accept-Encoding` / `Content-Encoding`) и использование современных алгоритмов вроде `gzip` и `brotli`.