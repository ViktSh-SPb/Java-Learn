Хотя REST сегодня чаще используется, **SOAP** по-прежнему имеет сильные стороны, особенно в корпоративной среде, где важны надёжность и безопасность.
## Основные преимущества SOAP перед REST**:
### 🧱 1. **Строгая спецификация и формальный контракт (WSDL)**
- SOAP использует **WSDL (Web Services Description Language)** — файл, описывающий все методы, типы данных и параметры.
- Клиенты могут **автоматически генерировать код** для вызова сервиса (stub’ы, proxy-классы).
- Это делает интеграции **предсказуемыми и самодокументированными**, что особенно важно в больших организациях.
> ✅ REST обычно требует отдельной документации (например, Swagger/OpenAPI), тогда как SOAP уже самодокументирован.
### 🔒 2. **Продвинутая безопасность (WS-Security)**
SOAP поддерживает стандарты безопасности на уровне протокола:
- Подпись сообщений (digital signature),
- Шифрование (XML Encryption),
- Аутентификация (через токены, SAML, Kerberos),
- Управление доступом на уровне сообщений.
> ✅ REST чаще полагается на транспортную безопасность (HTTPS + OAuth/JWT), но не имеет встроенной поддержки подписей и шифрования _внутри_ сообщений.
### 🔁 3. **Надёжность доставки (WS-ReliableMessaging)**
SOAP поддерживает **гарантированную доставку сообщений** — даже при сбоях сети:
- Повторная отправка сообщений при ошибке,
- Контроль дубликатов,
- Сохранение порядка сообщений.
> ✅ REST обычно полагается на HTTP и не обеспечивает таких гарантий.
### 💼 4. **Поддержка транзакций (WS-Transaction)**
SOAP имеет спецификации для **распределённых транзакций**:
- Можно выполнить несколько операций как одну логическую единицу (commit/rollback).
- Важно для банковских и финансовых систем.
> ✅ REST не имеет встроенной поддержки транзакций между сервисами.
### 🧩 5. **Независимость от транспортного протокола**
SOAP может работать не только поверх HTTP, но и через:
- SMTP (электронная почта),
- JMS (очереди сообщений),
- TCP.
> ✅ REST жёстко привязан к HTTP.
### 🧰 6. **Стандартизованное описание ошибок**
SOAP имеет встроенный механизм ошибок через элемент `<Fault>`:
- Код ошибки,
- Описание,
- Подробности.
> ✅ В REST ошибки выражаются произвольными HTTP-кодами и форматами (например, JSON), что требует дополнительных соглашений.
### 📊 Итого

|Критерий|SOAP|REST|
|---|---|---|
|Формальный контракт|✅ WSDL|⚠️ Опционально (Swagger/OpenAPI)|
|Безопасность|✅ WS-Security|⚠️ HTTPS, OAuth|
|Транзакции|✅ WS-Transaction|❌|
|Гарантированная доставка|✅ WS-ReliableMessaging|❌|
|Формат данных|XML|JSON, XML, др.|
|Простота и скорость|❌ Сложный, медленнее|✅ Лёгкий и быстрый|
