В RESTful веб-сервисах кеширование реализуется **через стандартные механизмы HTTP**, чтобы уменьшить нагрузку на сервер и ускорить отклик для клиента. Основные моменты:
## 1. Механизмы кеширования
### 🔹 HTTP-заголовки
- **Cache-Control** – управляет временем жизни и доступностью кеша.
    ```
    Cache-Control: max-age=3600, public
    ```
    → ресурс можно кешировать 1 час на клиенте или прокси.
- **ETag** – уникальный идентификатор версии ресурса.
    ```
    ETag: "abc123"
    ```
    Клиент может использовать условный запрос:
    ```
    If-None-Match: "abc123"
    ```
    → сервер возвращает `304 Not Modified`, если ресурс не изменился, экономя трафик.
- **Last-Modified** – дата последнего изменения ресурса.
    ```
    Last-Modified: Wed, 04 Nov 2025 15:00:00 GMT
    ```
    Клиент может использовать `If-Modified-Since` для условного запроса.
### 🔹 Типы кеширования
- **Client-side** – кеш браузера или приложения.
- **Proxy/CDN** – промежуточные узлы, которые хранят копии ответов.
- **Server-side** – сервер хранит часто запрашиваемые данные для ускорения генерации ответа.
## 2. Влияние на производительность
- **Снижение нагрузки на сервер** – меньше запросов к БД и вычислений.
- **Уменьшение задержек для клиента** – ответы берутся из локального кеша или ближайшего прокси.
- **Масштабируемость** – сервис выдерживает больше клиентов без увеличения ресурсов.
✅ **Вывод:** правильное кеширование в REST позволяет **ускорить работу клиента**, **сэкономить ресурсы сервера** и **обеспечить масштабируемость**, но требует внимательного управления сроками жизни кеша и контроля актуальности данных.