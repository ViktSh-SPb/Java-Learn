**Inversion of Control (IoC)** — это один из **основных принципов Spring Framework**, который означает, что **контроль над созданием и управлением объектами (бинами)** передаётся **фреймворку**, а не осуществляется вручную разработчиком.
## 🔁 1️⃣ Что значит «инверсия управления»?
Обычно в обычном Java-коде **вы сами создаёте зависимости**:
```java
public class UserService {
    private UserRepository repository = new UserRepository();
}
```
→ Здесь `UserService` сам **контролирует** создание `UserRepository`.
В Spring это меняется:
- Контейнер Spring сам создаёт объект `UserRepository`;
- сам решает, **когда и куда** его внедрить;
- а вы просто **описываете зависимость**.
```java
@Service
public class UserService {
    private final UserRepository repository;

    @Autowired
    public UserService(UserRepository repository) {
        this.repository = repository;
    }
}
```
📌 Теперь **управление зависимостями инвертировано** — программист описывает, _что_ нужно, а Spring решает, _как и когда_ это предоставить.
## ⚙️ 2️⃣ Как это работает внутри Spring
1. **Spring Container (IoC-контейнер)** 
    — основной компонент, который управляет жизненным циклом всех объектов (бинов).  
    Он создаётся при запуске приложения.
2. **Конфигурация зависимостей**  
    — контейнер читает конфигурацию:
    - из **аннотаций** (`@Component`, `@Service`, `@Repository`, `@Controller`);
    - из **Java-конфигурации** (`@Configuration`, `@Bean`);
    - или из **XML-файла** (`applicationContext.xml`).
3. **Создание бинов (Bean Creation)**  
    — контейнер сканирует классы, создаёт экземпляры и внедряет зависимости.
4. **Внедрение зависимостей (Dependency Injection, DI)**  
    — контейнер подставляет нужные объекты в поля, конструкторы или сеттеры.
## 🧩 3️⃣ Виды внедрения зависимостей (DI)

|Вид внедрения|Пример|Особенности|
|---|---|---|
|**Через конструктор**|`@Autowired` в конструкторе|Рекомендуемый способ, удобен для final полей|
|**Через сеттер**|`@Autowired` на методе set|Гибко, можно менять зависимости|
|**Через поле**|`@Autowired` на поле|Просто, но хуже тестируется|
## 🧠 4️⃣ Преимущества IoC
✅ **Слабая связанность** — классы не знают, как создаются их зависимости.  
✅ **Легкость тестирования** — можно подменять зависимости (например, моки).  
✅ **Повторное использование кода** — можно менять реализации без изменения бизнес-логики.  
✅ **Гибкость конфигурации** — зависимости можно задавать аннотациями, XML или Java.
## 💡 5️⃣ Кратко

|Что делает IoC|Результат|
|---|---|
|Контейнер управляет созданием объектов|Меньше шаблонного кода|
|Контейнер внедряет зависимости|Классы становятся независимыми|
|Разработчик описывает зависимости декларативно|Код легче поддерживать и тестировать|
**Простыми словами:**  
🧩 _IoC в Spring означает, что вместо того чтобы вы создавали и связывали объекты вручную, Spring делает это за вас — автоматически и централизованно._