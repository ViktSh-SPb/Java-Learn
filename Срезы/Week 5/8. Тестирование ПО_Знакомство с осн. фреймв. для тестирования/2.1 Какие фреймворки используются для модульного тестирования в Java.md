## 1️⃣ **JUnit**
- **Самый популярный фреймворк для юнит-тестов в Java.**
- Последняя версия — **JUnit 5 (Jupiter)**.
- Позволяет писать тестовые методы с аннотациями:
```java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertEquals;

class CalculatorTest {

    @Test
    void testAdd() {
        Calculator calc = new Calculator();
        assertEquals(5, calc.add(2, 3));
    }
}
```
**Особенности JUnit:**
- Аннотации: `@Test`, `@BeforeEach`, `@AfterEach`, `@BeforeAll`, `@AfterAll`
- Assertions: `assertEquals`, `assertTrue`, `assertThrows`
- Поддержка **параметризованных тестов** и **nested тестов**
## 2️⃣ **TestNG**
- Альтернатива JUnit, ориентированная на **гибкую конфигурацию тестов**.
- Поддерживает **группы тестов, зависимости между тестами и параллельное выполнение**.
Пример:
```java
import org.testng.annotations.Test;
import static org.testng.Assert.assertEquals;

public class CalculatorTest {

    @Test
    public void testAdd() {
        Calculator calc = new Calculator();
        assertEquals(calc.add(2, 3), 5);
    }
}
```
- Используется реже для простых юнит-тестов, чаще для **интеграционных тестов или сложных сценариев**.
## 3️⃣ **Mockito**
- **Библиотека для мокирования зависимостей**.
- Позволяет **создавать фиктивные объекты**, чтобы тестировать класс в изоляции.
Пример:
```java
import static org.mockito.Mockito.*;

class UserServiceTest {

    @Test
    void testGetUser() {
        UserRepository repo = mock(UserRepository.class);
        when(repo.findById(1)).thenReturn(new User(1, "Alice"));

        UserService service = new UserService(repo);
        User user = service.getUser(1);

        assertEquals("Alice", user.getName());
    }
}
```
- Часто используется **вместе с JUnit**.
## 4️⃣ **AssertJ**
- Современная библиотека для **читаемых assertions**.
- Позволяет писать тесты в стиле fluent API:
```java
import static org.assertj.core.api.Assertions.assertThat;

@Test
void testAdd() {
    Calculator calc = new Calculator();
    assertThat(calc.add(2, 3)).isEqualTo(5);
}
```
- Используется вместе с JUnit или TestNG.
## 5️⃣ **Hamcrest**
- Библиотека для **матчер-выражений** в assertions.
- Позволяет писать более гибкие проверки, например:
```java
import static org.hamcrest.MatcherAssert.assertThat;
import static org.hamcrest.Matchers.*;

assertThat(list, hasItem("Alice"));
```
## 6️⃣ **Spring Test / Spring Boot Test**
- Для тестирования **Spring-приложений**.
- Позволяет поднимать **контекст приложения**, тестировать beans и REST-контроллеры.
Аннотации:
- `@SpringBootTest` — поднимает весь контекст приложения.
- `@WebMvcTest` — тестирование только контроллеров.
- `@DataJpaTest` — тестирование JPA репозиториев.
### 🔹 Итог
Для **модульного тестирования Java** чаще всего используют комбинацию:
1. **JUnit 5** — для написания самих тестов.
2. **Mockito** — для мокирования зависимостей.
3. **AssertJ или Hamcrest** — для удобных и читаемых assertions.
4. **Spring Test / Spring Boot Test** — если проект на Spring.