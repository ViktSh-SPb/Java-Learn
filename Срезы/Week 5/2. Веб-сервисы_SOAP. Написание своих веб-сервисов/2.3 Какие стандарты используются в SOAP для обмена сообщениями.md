SOAP (Simple Object Access Protocol) — это не просто формат сообщений, а целая **экосистема стандартов**, которые обеспечивают надежный, безопасный и совместимый обмен данными между системами.
### 🧱 1. **SOAP (Simple Object Access Protocol)**
**Главный стандарт**, определяющий:
- Формат XML-сообщения (envelope, header, body, fault);
- Правила сериализации данных;
- Механизм передачи сообщений по различным транспортам (HTTP, SMTP, JMS и др.).
📘 Пример структуры SOAP-сообщения:
```xml
<soap:Envelope xmlns:soap="http://www.w3.org/2003/05/soap-envelope">
  <soap:Header>...</soap:Header>
  <soap:Body>...</soap:Body>
</soap:Envelope>
```
### 🗂 2. **WSDL (Web Services Description Language)**
Стандарт описания веб-сервиса:
- Определяет, **какие операции** доступны;
- Какие **форматы сообщений и типы данных** используются;
- По каким **протоколам** происходит взаимодействие.
> 💡 WSDL — это своего рода «контракт» между клиентом и сервером.
### 🔐 3. **WS-Security**
Набор стандартов безопасности, который описывает:
- Шифрование сообщений (XML Encryption),
- Цифровые подписи (XML Signature),
- Аутентификацию с помощью токенов (SAML, X.509, Kerberos).
> 💡 Позволяет защищать данные **на уровне сообщений**, а не только на уровне транспортного канала (в отличие от HTTPS).
### 📦 4. **WS-Addressing**
Позволяет указывать **метаинформацию об адресации сообщений**:
- Кто отправитель и получатель;
- Как обрабатывать ответы и ошибки;
- Идентификатор сообщений (MessageID).
> 💡 Это делает SOAP независимым от конкретного транспортного протокола.
### 🔁 5. **WS-ReliableMessaging**
Обеспечивает **гарантированную доставку сообщений**:
- Повторная отправка при ошибке;
- Контроль порядка доставки;
- Устранение дубликатов.
> 💡 Критически важно для финансовых и банковских систем, где потеря сообщения недопустима.
### 💼 6. **WS-Transaction / WS-Coordination**
Обеспечивают **распределённые транзакции**:
- Несколько сервисов могут участвовать в одной логической операции;
- Возможен откат (rollback), если один из участников завершился неудачно.
### 🧩 7. **WS-Policy**
Позволяет описать **политику сервиса** — какие требования должны быть выполнены:
- Требуется ли шифрование,
- Какие токены использовать для аутентификации,
- Какие версии протоколов поддерживаются.
### 📡 8. **WS-Discovery**
Позволяет сервисам **находить друг друга в сети**:
- Работает как «поиск» доступных SOAP-сервисов;
- Полезно в корпоративных SOA-средах.
### 🧰 Итого — основные стандарты SOAP

|Категория|Стандарт|Назначение|
|---|---|---|
|Формат сообщений|**SOAP**|Структура XML-сообщений|
|Описание сервиса|**WSDL**|Контракт между клиентом и сервером|
|Безопасность|**WS-Security**|Подписи, шифрование, токены|
|Адресация|**WS-Addressing**|Маршрутизация и метаданные сообщений|
|Надёжность|**WS-ReliableMessaging**|Гарантированная доставка|
|Транзакции|**WS-Transaction**|Координация распределённых операций|
|Политики|**WS-Policy**|Описание требований и условий|
|Обнаружение|**WS-Discovery**|Автоматический поиск сервисов|
