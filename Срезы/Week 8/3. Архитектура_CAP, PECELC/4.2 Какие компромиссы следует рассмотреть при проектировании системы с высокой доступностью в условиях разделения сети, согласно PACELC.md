# 1️⃣ **Компромисс при Partition (P) — выбор между A и C**
Если сеть разделена, система **не может одновременно быть полностью доступной и полностью согласованной**.
- **Выбор доступности (A) → жертва согласованностью (C)**
    - Система продолжает отвечать на запросы, даже если узлы не синхронизированы.
    - Возможна **eventual consistency** (данные могут временно расходиться).
    - Примеры: Cassandra, DynamoDB, Riak.
    - Подходит для: высоконагруженных систем, где критична непрерывная работа.
- **Выбор согласованности (C) → жертва доступностью (A)**
    - Система блокирует часть запросов до восстановления связи между узлами.
    - Данные всегда согласованы, но часть клиентов может получить ошибки или задержку.
    - Примеры: Zookeeper, Etcd, Google Spanner.
    - Подходит для: систем, где критична точность данных (финансы, конфигурации).
# 2️⃣ **Компромисс при отсутствии Partition (Else) — выбор между Latency (L) и Consistency (C)**
Даже когда сеть работает нормально, система должна решать:
- **L (низкая задержка)**
    - Отдача ответа быстрее, иногда за счёт временной рассогласованности.
    - Подходит для онлайн-сервисов с высокой нагрузкой (социальные сети, кэширование).
- **C (строгая согласованность)**
    - Все узлы синхронизированы перед ответом.
    - Может увеличивать время ответа, особенно на распределённых кластерах.
# 3️⃣ **Другие аспекты компромиссов при проектировании высокой доступности**
1. **Репликация и синхронизация**
    - Асинхронная репликация → быстрее, но возможна рассинхронизация.
    - Синхронная репликация → согласованность, но увеличивает задержку.
2. **Выбор стратегии согласованности на уровне операций**
    - Read/Write Quorums (например, R + W > N)
    - Eventual consistency vs Strong consistency
3. **Обработка конфликтов данных**
    - Last-write-wins, vector clocks, CRDT
4. **Сетевые таймауты и ретраи**
    - Нужно настроить retry-policy, circuit-breaker, чтобы сохранить доступность.
5. **Масштабирование и балансировка нагрузки**
    - Высокая доступность требует горизонтального масштабирования, но оно увеличивает риск Partition → нужно учитывать компромисс CAP/PACELC.
# 🔑 **Итог**
Согласно PACELC, проектируя систему с **высокой доступностью при сетевых разделениях**, нужно учитывать следующие компромиссы:

|Ситуация|Компромисс|
|---|---|
|Partition (P)|A vs C → выбрать доступность или согласованность|
|Нет Partition (Else)|L vs C → выбрать скорость отклика или строгую согласованность|
В реальной практике высокодоступные системы чаще выбирают **A при P** и **L при нормальной работе**, обеспечивая **eventual consistency** и минимальные задержки.