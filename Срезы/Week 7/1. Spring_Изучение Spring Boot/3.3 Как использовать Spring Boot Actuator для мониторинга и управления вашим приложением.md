Spring Boot **Actuator** — это мощный модуль, который предоставляет готовые эндпойнты для мониторинга, диагностики и управления приложением. Он помогает наблюдать за здоровьем, метриками, состоянием и конфигурацией приложения — всё _без лишнего кода_.
# ✅ 1. Подключить зависимость Actuator
В `pom.xml`:
```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-actuator</artifactId>
</dependency>
```
После этого Spring Boot автоматически активирует базовые endpoints.
# ✅ 2. Включить нужные endpoints в настройках
По умолчанию внешнему доступу открыт только `/actuator/health`.
Чтобы включить остальные, добавьте в `application.yml`:
```yaml
management:
  endpoints:
    web:
      exposure:
        include: "*"
```
Или включить выборочно:
```yaml
management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics
```
# ✅ 3. Основные Actuator endpoints

|Эндпойнт|Назначение|
|---|---|
|**/actuator/health**|Проверка состояния приложения (DB, disk, mail и т.д.)|
|**/actuator/info**|Информация о приложении|
|**/actuator/metrics**|Метрики JVM, CPU, памяти, HTTP|
|**/actuator/prometheus**|Метрики для Prometheus|
|**/actuator/env**|Переменные окружения и настройки|
|**/actuator/beans**|Все Spring-бины|
|**/actuator/mappings**|Все HTTP mappings|
|**/actuator/loggers**|Управление уровнями логирования|
|**/actuator/threaddump**|Дамп потоков|
|**/actuator/shutdown**|Завершает приложение (нужно включать явно)|

Пример включения shutdown:
```yaml
management:
  endpoint:
    shutdown:
      enabled: true
```
# ✅ 4. Добавить информацию в `/actuator/info`
В `application.yml`:
```yaml
info:
  app:
    name: My App
    version: 1.0.0
```
После этого `/actuator/info` покажет ваши данные.
# ✅ 5. Подключение Health Indicators
Actuator автоматически проверяет:
- базу данных,
- MQ (RabbitMQ, Kafka),
- дисковое пространство,
- кэши,
- SMTP.
Если нужно добавить свой Health Check:
```java
@Component
public class MyHealthIndicator implements HealthIndicator {
    @Override
    public Health health() {
        boolean serverIsUp = checkServer();
        return serverIsUp ? Health.up().build() : Health.down().withDetail("error", "Server unreachable").build();
    }
}
```
# ✅ 6. Метрики и мониторинг
Actuator предоставляет огромное количество метрик:
- HTTP запросы
- Пулы соединений (HikariCP)
- JVM: heap, threads, GC
- CPU usage
- Кэш: hits/misses
- Spring beans
Пример запроса:
```
GET /actuator/metrics/jvm.memory.used
```
Чтобы получить значения, добавьте параметр:
```
GET /actuator/metrics/http.server.requests
```
# ✅ 7. Интеграция с графиками и мониторингом
Actuator может работать с:
### ✔ **Prometheus**
Через `/actuator/prometheus`.
### ✔ **Micrometer**
Он встроен и работает под капотом.
### ✔ **Grafana**
Часто используется вместе с Prometheus.
# ✅ 8. Управление логированием
Например, изменить уровень логов на лету:
```
POST /actuator/loggers/com.example
{
  "configuredLevel": "DEBUG"
}
```
# 📌 Итог: что даёт Spring Boot Actuator?
✔ Проверка состояния приложения  
✔ Мониторинг производительности  
✔ Информация о конфигурации  
✔ Динамическое изменение логов  
✔ Диагностика (threaddump, env, mappings, beans)  
✔ Интеграция с Prometheus/Grafana  
✔ Собственные health-checks и метрики  
✔ Минимум кода — максимум пользы