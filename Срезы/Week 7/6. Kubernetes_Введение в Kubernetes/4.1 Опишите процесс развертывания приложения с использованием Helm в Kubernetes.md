**Helm** — это менеджер пакетов для Kubernetes, который позволяет **автоматизировать развертывание, управление и обновление приложений** в кластере с помощью шаблонов.
## 1️⃣ **Установка Helm**
На локальной машине или CI/CD агенте нужно установить Helm:
```bash
# для Linux/macOS
curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash

# проверка
helm version
```
## 2️⃣ **Добавление Helm репозитория**
Helm использует репозитории с “charts” (шаблонами приложений):
```bash
helm repo add bitnami https://charts.bitnami.com/bitnami
helm repo update
```
- `bitnami` — популярный репозиторий с готовыми приложениями (MySQL, Redis, Nginx и др.).
## 3️⃣ **Создание собственного Chart (необязательно)**
Если приложение своё, создаём структуру Helm Chart:
```bash
helm create myapp
```
Структура:
```
myapp/
  Chart.yaml       # метаданные Chart
  values.yaml      # значения по умолчанию
  templates/       # Kubernetes манифесты с шаблонами
```
- В `templates/` находятся Deployment, Service, ConfigMap и другие манифесты с шаблонизацией (`{{ .Values.param }}`).
## 4️⃣ **Настройка значений (values.yaml)**
- В `values.yaml` указываются параметры для шаблонов: имя образа, версия, количество реплик, ресурсы, порты и т.д.
Пример:
```yaml
replicaCount: 3
image:
  repository: myapp
  tag: 1.0
service:
  type: ClusterIP
  port: 8080
```
- Можно переопределить значения при установке через командную строку:
```bash
helm install myapp ./myapp --set replicaCount=5
```
## 5️⃣ **Установка/развертывание Chart в Kubernetes**
```bash
helm install <release_name> <chart_path_or_repo>
```
Пример:
```bash
helm install myapp bitnami/nginx
```
- `<release_name>` — уникальное имя установки, можно иметь несколько экземпляров одного Chart.
- Helm автоматически создаёт Deployment, Service, ConfigMap и другие ресурсы в кластере.
## 6️⃣ **Просмотр и управление установкой**
- Проверка статуса:
```bash
helm list
helm status myapp
```
- Обновление приложения:
```bash
helm upgrade myapp ./myapp --set image.tag=1.1
```
- Удаление приложения:
```bash
helm uninstall myapp
```
## 🔹 **Преимущества использования Helm**
1. Шаблонизация Kubernetes манифестов → меньше копирования YAML.
2. Лёгкое обновление и откаты приложений (`helm upgrade` / `helm rollback`).
3. Переиспользуемые и версионированные Charts → стандартизация.
4. Автоматическая установка зависимостей (например, база данных для приложения).