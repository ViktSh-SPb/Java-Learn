# 🧩 Что такое «свой стартер»?
Ваш Spring Boot Starter — это **отдельный модуль**, который:
1. Подключается как зависимость в других проектах.
2. Автоматически добавляет нужные бины, конфигурации, проперти.
3. Работает как обычный Starter (`spring-boot-starter-web`, `…-jpa`, и т. д.)
Он включает:
- **starter** (зависимости)
- **autoconfigure** (автоконфигурация)
# 🛠 Структура проекта
Обычно создают **два артефакта**:
### 1. `my-module-spring-boot-starter`
Содержит _только зависимости_.
### 2. `my-module-spring-boot-autoconfigure`
Содержит:
- классы автоконфигураций
- проперти
- регистрацию автоконфигурации
Это полностью соответствует подходу Spring Boot.
# 🚀 Шаг 1. Создать модуль автоконфигурации
Например:  
`my-feature-spring-boot-autoconfigure`
В `pom.xml`:
```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-autoconfigure</artifactId>
</dependency>
```
Если нужно поддержать внешние проперти:
```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-configuration-processor</artifactId>
    <optional>true</optional>
</dependency>
```
# 🚀 Шаг 2. Создать класс пропертей
```java
@ConfigurationProperties(prefix = "my.feature")
public class MyFeatureProperties {

    private boolean enabled = true;
    private String text = "Hello";

    // getters/setters
}
```
# 🚀 Шаг 3. Создать класс автоконфигурации
```java
@Configuration
@EnableConfigurationProperties(MyFeatureProperties.class)
@ConditionalOnProperty(name = "my.feature.enabled", havingValue = "true", matchIfMissing = true)
public class MyFeatureAutoConfiguration {

    @Bean
    @ConditionalOnMissingBean
    public MyService myService(MyFeatureProperties props) {
        return new MyService(props.getText());
    }
}
```
### Что здесь происходит?
- `@EnableConfigurationProperties` — подключает проперти.
- `@ConditionalOnProperty` — включает автоконфигурацию при включённом флаге.
- `@ConditionalOnMissingBean` — не создаёт бин, если пользователь создал свой.
Так ведут себя _все стандартные стартеры Spring Boot_.
# 🚀 Шаг 4. Зарегистрировать автоконфигурацию (обязательно!)
С Spring Boot 3+ используется:
```
src/main/resources/META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports
```
Создайте файл:
```
com.example.autoconfigure.MyFeatureAutoConfiguration
```
Это список классов автоконфигурации — один в строке.
# 🚀 Шаг 5. Создать модуль Starter
Второй модуль:  
`my-feature-spring-boot-starter`
Внутри будет только `pom.xml`.
```xml
<dependency>
    <groupId>com.example</groupId>
    <artifactId>my-feature-spring-boot-autoconfigure</artifactId>
</dependency>
```
Этот стартер добавьте как обычную зависимость.
# 🚀 Шаг 6. Использовать стартер в любом Spring Boot проекте
В `pom.xml` приложения:
```xml
<dependency>
    <groupId>com.example</groupId>
    <artifactId>my-feature-spring-boot-starter</artifactId>
</dependency>
```
И всё — Spring Boot автоматически:
✓ подключит проперти  
✓ создаст ваши бины  
✓ включит функциональность
При необходимости изменить поведение:
```yaml
my:
  feature:
    enabled: true
    text: "Custom message"
```
# 🔥 Итог: ваш Starter состоит из 3 элементов

|Элемент|Роль|
|---|---|
|**autoconfigure**|Логика, бины, проперти|
|**starter**|Подключает зависимости и автоконфигурацию|
|**AutoConfiguration.imports**|Регистрирует автоконфигурации в Spring Boot|
