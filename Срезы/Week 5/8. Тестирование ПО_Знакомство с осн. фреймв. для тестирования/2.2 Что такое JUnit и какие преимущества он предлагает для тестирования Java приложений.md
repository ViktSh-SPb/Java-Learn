**JUnit** — это один из самых популярных фреймворков для **модульного тестирования (unit testing) в Java**. Он позволяет проверять отдельные классы и методы в изоляции от остальной системы.
## 1️⃣ Основные возможности JUnit
1. **Написание тестов**
    - Позволяет создавать тестовые методы с помощью аннотации `@Test`.
    ```java
    import org.junit.jupiter.api.Test;
    import static org.junit.jupiter.api.Assertions.assertEquals;
    
    class CalculatorTest {
    
        @Test
        void testAdd() {
            Calculator calc = new Calculator();
            assertEquals(5, calc.add(2, 3));
        }
    }
    ```
1. **Настройка и очистка окружения**
    - `@BeforeEach` / `@AfterEach` — выполняются перед и после каждого теста.
    - `@BeforeAll` / `@AfterAll` — выполняются один раз для всего класса тестов.
2. **Assertions (проверки)**
    - Встроенные методы для проверки результатов:
        - `assertEquals(expected, actual)`
        - `assertTrue(condition)`
        - `assertThrows(Exception.class, () -> ...)`
3. **Параметризованные тесты**
    - Позволяют запускать один тест с разными входными данными.
## 2️⃣ Преимущества JUnit

|Преимущество|Описание|
|---|---|
|**Простота и популярность**|Легко начать использовать, широкая документация и поддержка IDE.|
|**Автоматизация тестирования**|Интеграция с Maven, Gradle, CI/CD (Jenkins, GitHub Actions).|
|**Изоляция тестов**|Позволяет проверять отдельные методы или классы независимо от остальной системы.|
|**Интеграция с другими библиотеками**|Например, Mockito для мокирования зависимостей.|
|**Поддержка современных возможностей Java**|JUnit 5 поддерживает лямбды, nested тесты, параллельное выполнение.|
|**Расширяемость**|Плагинами и аннотациями можно настраивать поведение тестов.|
## 3️⃣ Пример полного тестового класса
```java
import org.junit.jupiter.api.*;

import static org.junit.jupiter.api.Assertions.*;

class CalculatorTest {

    Calculator calc;

    @BeforeEach
    void setUp() {
        calc = new Calculator();
    }

    @Test
    void testAdd() {
        assertEquals(5, calc.add(2, 3));
    }

    @Test
    void testSubtract() {
        assertEquals(1, calc.subtract(3, 2));
    }

    @AfterEach
    void tearDown() {
        // Очистка ресурсов, если нужно
    }
}
```
### 🔹 Итог
**JUnit** — это **фреймворк для юнит-тестирования**, который помогает:
- проверять код на корректность;
- изолировать тесты от внешних зависимостей;
- автоматизировать тестирование в CI/CD;
- создавать надежные и поддерживаемые тестовые наборы для Java-приложений.