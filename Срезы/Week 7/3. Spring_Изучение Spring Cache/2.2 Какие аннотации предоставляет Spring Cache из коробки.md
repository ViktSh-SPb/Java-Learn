Spring Cache предоставляет простой и декларативный способ кэширования данных при помощи набора аннотаций, которые управляют тем, **когда метод должен читать данные из кэша, сохранять их, обновлять или удалять**.
# ✅ **Основные аннотации Spring Cache**

## **1. `@Cacheable` — чтение из кэша**
Используется для обозначения метода, результат которого должен быть закэширован.
Если значение уже есть в кэше → метод **не будет вызван**, вернётся значение из кэша.
Если значения нет → Spring вызовет метод и положит результат в кэш.
**Пример:**
```java
@Cacheable(value = "users", key = "#id")
public User getUser(Long id) {
    return userRepository.findById(id).orElse(null);
}
```
## **2. `@CachePut` — обновление кэша**
Метод **всегда выполняется**, но результат **обновляет существующее значение в кэше**.
Используется, когда нужно сохранение/обновление.
**Пример:**
```java
@CachePut(value = "users", key = "#user.id")
public User updateUser(User user) {
    return userRepository.save(user);
}
```
## **3. `@CacheEvict` — удаление из кэша**
Удаляет значение из кэша (по ключу или целиком).
**Пример удаления только одного элемента:**
```java
@CacheEvict(value = "users", key = "#id")
public void deleteUser(Long id) {
    userRepository.deleteById(id);
}
```
**Пример очистки всего кэша:**
```java
@CacheEvict(value = "users", allEntries = true)
public void clearUsersCache() {}
```
## **4. `@Caching` — группировка нескольких кэш-аннотаций**
Позволяет применить несколько кэш-операций к одному методу.
**Пример:**
```java
@Caching(
    evict = { @CacheEvict(value = "users", key = "#id") },
    put = { @CachePut(value = "logs", key = "#id") }
)
public void process(Long id) { ... }
```
## **5. `@CacheConfig` — общая конфигурация для класса**
Позволяет не дублировать `cacheNames`, `keyGenerator`, и т. д.
**Пример:**
```java
@CacheConfig(cacheNames = "users")
public class UserService {

    @Cacheable
    public User get(Long id) { ... }

    @CacheEvict(key = "#id")
    public void delete(Long id) { ... }
}
```
# 📌 Дополнительно (не аннотация, но часть механизма)
## **`@EnableCaching`**
Включает механизм кэширования в приложении.
Обычно ставится в `@SpringBootApplication` или в конфигурации:
```java
@EnableCaching
@SpringBootApplication
public class App {}
```
# 💡 Итого

|Аннотация|Назначение|
|---|---|
|`@Cacheable`|Читать из кэша / класть в кэш|
|`@CachePut`|Всегда выполнять метод и обновлять кэш|
|`@CacheEvict`|Удалять записи из кэша|
|`@Caching`|Комбинировать несколько аннотаций|
|`@CacheConfig`|Общая конфигурация для класса|
|`@EnableCaching`|Включает кэширование|
