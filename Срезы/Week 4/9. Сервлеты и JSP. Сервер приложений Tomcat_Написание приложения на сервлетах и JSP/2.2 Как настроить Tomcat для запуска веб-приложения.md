## 🧩 **1. Установка Apache Tomcat**
1. Скачайте последнюю версию Tomcat с официального сайта:  
    🔗 [https://tomcat.apache.org/download-10.cgi](https://tomcat.apache.org/download-10.cgi)
2. Распакуйте архив в удобное место, например:
    ```
    C:\apache-tomcat-10
    ```
3. Убедитесь, что установлена **Java (JDK 17+)**, и добавьте в переменные среды:
    ```
    JAVA_HOME = C:\Program Files\Java\jdk-17
    PATH += %JAVA_HOME%\bin
    ```
4. Проверьте Tomcat, запустив:
    ```
    C:\apache-tomcat-10\bin\startup.bat
    ```
    Затем откройте браузер:
    ```
    http://localhost:8080
    ```
    ✅ Если видите страницу приветствия Tomcat — сервер работает.
## ⚙️ **2. Подготовка веб-приложения**
Tomcat ожидает приложения в формате **WAR-файла** или в **распакованной папке**.
Структура стандартного Java веб-приложения:
```
myapp/
 ├─ WEB-INF/
 │   ├─ web.xml
 │   ├─ classes/
 │   └─ lib/
 ├─ index.jsp
 └─ ...
```
Если вы используете **Maven**, то после сборки (`mvn package`) WAR-файл появится в:
```
target/myapp.war
```
## 🚀 **3. Деплой приложения**
### ✅ **Вариант 1: Копирование вручную**
Скопируйте ваш `myapp.war` в:
```
<TOMCAT_HOME>\webapps\
```
Tomcat автоматически:
- Развернёт приложение в папке `myapp/`;
- Будет доступно по адресу:
    ```
    http://localhost:8080/myapp
    ```
### ✅ **Вариант 2: Использование Tomcat Manager**
1. Включите доступ к **Tomcat Manager App**:
    - Откройте файл:
        ```
        <TOMCAT_HOME>\conf\tomcat-users.xml
        ```
    - Добавьте пользователя с правами управления:
        ```xml
        <role rolename="manager-gui"/>
        <role rolename="manager-script"/>
        <user username="admin" password="admin" roles="manager-gui,manager-script"/>
        ```
2. Перезапустите Tomcat.
3. Перейдите в браузере:
    ```
    http://localhost:8080/manager/html
    ```
4. Авторизуйтесь и выберите **"Deploy"** — загрузите свой `myapp.war` прямо из интерфейса.
### ✅ **Вариант 3: Автоматический деплой из IDE**
**Eclipse / IntelliJ IDEA** позволяют интегрировать Tomcat напрямую:
- Добавьте Tomcat Server через “Run → Edit Configurations → Add New Configuration → Tomcat”.
- Укажите путь к `<TOMCAT_HOME>`.
- IDEA автоматически соберёт и задеплоит WAR при запуске.
## 🧾 **4. Проверка конфигурации**
После деплоя:
- Проверьте логи в:
    ```
    <TOMCAT_HOME>\logs\catalina.out
    ```
- Приложение должно быть доступно по адресу:
    ```
    http://localhost:8080/myapp
    ```
## 🔐 **5. Частые донастройки**

|Цель|Что изменить|
|---|---|
|Изменить порт|В файле `<TOMCAT_HOME>\conf\server.xml`, тег `<Connector port="8080" />`|
|Настроить кодировку UTF-8|`<Connector URIEncoding="UTF-8" />`|
|Добавить DataSource|Через `context.xml` (JNDI)|
|Настроить SSL|Добавить `<Connector port="8443" protocol="HTTP/1.1" SSLEnabled="true" ... />`|
|Автозапуск при старте ОС|Использовать `Tomcat Service Installer` или `systemd` (Linux)|
## ✅ **6. Проверка на примере простого сервлета**
```java
@WebServlet("/hello")
public class HelloServlet extends HttpServlet {
    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws IOException {
        resp.getWriter().println("Hello from Tomcat!");
    }
}
```
После компиляции и деплоя — приложение будет доступно по адресу:
```
http://localhost:8080/myapp/hello
```
