## 1️⃣ **Pod**
- **Описание:** Минимальная единица развертывания в Kubernetes.
- **Содержит:** Один или несколько контейнеров, которые делят сеть и хранилище.
- **Используется для:** Размещения контейнеров приложения.
- **Пример:** Один pod может содержать контейнер с приложением и sidecar-контейнер для логирования.
## 2️⃣ **Deployment**
- **Описание:** Управляет созданием и обновлением Pod’ов.
- **Используется для:**
    - поддержания нужного количества реплик (replicas),
    - обновления приложения без простоя (Rolling Update),
    - отката на предыдущую версию (Rollback).
- **Пример:** Deployment для веб-сервиса с 3 репликами Pod’ов.
## 3️⃣ **Service**
- **Описание:** Определяет, как получить доступ к Pod’ам.
- **Используется для:**
    - балансировки нагрузки между Pod’ами,
    - стабильного доступа к Pod’ам по DNS-имени.
- **Типы:** ClusterIP, NodePort, LoadBalancer, ExternalName.
- **Пример:** Service с типом LoadBalancer для веб-приложения, доступного извне.
## 4️⃣ **ConfigMap**
- **Описание:** Хранит конфигурационные данные в виде ключ-значение.
- **Используется для:** Передачи конфигурации в Pod без пересборки контейнера.
## 5️⃣ **Secret**
- **Описание:** Безопасное хранение чувствительных данных (пароли, токены, ключи).
- **Используется для:** Подключения к базам данных, API, другим сервисам без хранения в коде.
## 6️⃣ **PersistentVolume (PV)**
- **Описание:** Ресурс хранилища, который предоставляет постоянное хранилище данных.
- **Используется для:** Хранения данных вне жизненного цикла Pod’ов.
## 7️⃣ **PersistentVolumeClaim (PVC)**
- **Описание:** Запрос Pod’а на использование PersistentVolume.
- **Используется для:** Подключения Pod’а к конкретному объёму хранилища.
## 8️⃣ **Namespace**
- **Описание:** Логическая изоляция ресурсов в кластере.
- **Используется для:** Разделения среды разработки, тестирования и продакшена в одном кластере.
## 9️⃣ **ReplicaSet**
- **Описание:** Обеспечивает нужное количество реплик Pod’ов.
- **Используется для:** Поддержания стабильного состояния приложения.
- **Примечание:** Deployment автоматически создаёт ReplicaSet.
## 1️⃣0️⃣ **StatefulSet**
- **Описание:** Управляет Pod’ами с сохранением идентичности и хранилища.
- **Используется для:** Баз данных и сервисов, которым важно постоянное имя и хранилище (например, PostgreSQL, Kafka).
## 1️⃣1️⃣ **DaemonSet**
- **Описание:** Запускает один Pod на каждом узле кластера.
- **Используется для:** Логирования, мониторинга и агентов, которые должны быть на всех узлах.
## 1️⃣2️⃣ **Job / CronJob**
- **Job:** Выполняет задачу один раз или заданное количество раз.
- **CronJob:** Выполняет задачи по расписанию (как cron в Linux).
### 🔹 Итог
Kubernetes объекты делятся на три группы:
1. **Compute**: Pod, Deployment, ReplicaSet, StatefulSet, DaemonSet, Job, CronJob
2. **Networking**: Service, Ingress
3. **Storage & Config**: PV, PVC, ConfigMap, Secret
4. **Организационные**: Namespace